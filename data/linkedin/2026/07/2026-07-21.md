# LinkedIn Highlights — 2026-07-21

_Collector: ai-linkedin. Skipped this run._

_Reason:_ Browser selection required. Two Chrome browsers are connected and the browser tooling mandates explicit user confirmation of which one to drive (and forbids auto-picking). This is a non-interactive (cron) run, so that confirmation cannot be obtained. No deviceId is pinned for the LinkedIn collector in sources.json, and both browsers carry generic names ("Browser 1"/"Browser 2") whose name↔deviceId mapping even swaps between runs, so the signed-in LinkedIn session cannot be disambiguated. Auto-selecting risks driving the wrong authenticated LinkedIn session and reading a different identity's private feed, so the run was skipped to avoid a cross-identity privacy leak. No sign-in was attempted.

## Sources scanned
- mcp__Claude_in_Chrome__list_connected_browsers — 2 browsers connected (Browser 1 [340a83b0-b5a5-47f4-a940-a531d0cdd0a5], Browser 2 [f6b216be-7192-4620-bcf7-f046ec6509d3]); could not auto-select without user consent, so no navigation was performed. Fix: leave only one Chrome browser connected for the daily run, or pin a default deviceId for the LinkedIn collector.
