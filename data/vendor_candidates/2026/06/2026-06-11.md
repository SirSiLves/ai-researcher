# Vendor sweep — 2026-06-11

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

_No changes to `sources.json` this run._

Why nothing applied, on a heavy news day (Anthropic Fable 5, SpaceX/xAI record IPO, SAP↔Salesforce agent standoff, Gemini 3.5 Flash/Omni):

- **Hot events — all real ones blocked.** Today's loudest capital-markets stories are genuine hot events, but none can be auto-applied:
  - `spacex` (record ~$1.75T IPO priced at $135, ticker SPCX) — classified `hot_event`, but `coverage: uncovered` with **no `blog_url_hint`** → Step C.5 reject (`rejected_no_blog_url`). No URL to seed `blog_urls=[…]`.
  - `oracle` (OpenAI models/Codex via Oracle Cloud credits) — `hot_event`, uncovered, **no `blog_url_hint`** → reject.
  - `interconnects` (Nathan Lambert's Fable 5 critique) — `hot_event` by mention volume + IPO-keyword co-occurrence, but it is a media/blog outlet with no vendor `blog_url_hint` → reject.
  - `databricks` ($165–175B valuation round) — `hot_event`, but already in `enterprise_vendors`; no add needed.
  - `xai` (SpaceX–xAI merger, Grok goes public by proxy) — already in `enterprise_vendors`; no add. (Velocity is *cooling* 0.28×; the IPO noise is attributed to the SpaceX entity.)
- **Sustained promotions — none qualify.** Three real orgs crossed `promote` thresholds today (`cern`, `nexthink`, `pwc`), but each was at `hot_event` the prior three days, so today is only **1/2** consecutive days at promote — the sustained-day gate is not met. They also carry **no `blog_url_hint`**, so even at 2/2 they would be Step C.5 rejects. The longer-sustained promote slugs in the tally (`ey`, `intuit`, `nous-research`, `roche`, `stripe`, plus protocol/section/surname debris like `a2a-protocol`, `annex-iii`, `eugene`, `postgres`, `spring`) all lack a `blog_url_hint` and are correctly never applied.
- **Expired hot events — none today.** All TTL'd `_auto_added` entries still have `_expires_on ≥ 2026-06-11`; the earliest expiries (2026-06-13: `tomoro`, `sierra`, `cerebras`, `aisi-uk`, `groq`, `ubs`, `moonshot`, `aleph-alpha`, `perplexity`) remove over the next few days.
- **Deep-watch demotion — no eligible candidates.** `enterprise_vendors` sits at **97/30** (OVERFLOW regime, eff. silence 14d, target ≤45, budget 5/run). Every over-cap `_auto_added` entry that is silent ≥14 days is still inside its `_expires_on` TTL, which Step C.6 criterion 4 exempts (let the TTL walk the count down instead). No forced purge this run.

## ⏳ Pending — needs one more day at promote tier (3)
Orgs that hit promote thresholds today but have not yet held that tier for the required 2 consecutive days (they were `hot_event` yesterday). Will auto-apply tomorrow only if they stay at `promote` **and** acquire a `blog_url_hint`.

**cern** — 392 mentions (window, aggregator-inflated), 4 src types, days at promote so far: 1/2 · no blog_url_hint
**nexthink** — 66 mentions, 3 src types, days at promote so far: 1/2 · no blog_url_hint
**pwc** — 63 mentions, 4 src types, days at promote so far: 1/2 · no blog_url_hint

## ⚡ Velocity signals (4 accelerating, 5 cooling)
Orgs (mentioned today) whose posting cadence shifted vs. their 4-week baseline. Independent of tier. No org reached the `surging` (≥5.0) hot-event threshold today.

**Accelerating (4):**
**bank-j-safra-sarasin** — velocity_ratio 4.0× (35 mentions/7d vs. 8.75/wk baseline) · informational; watch tomorrow
**govern-ai** — velocity_ratio 4.0× (15 mentions/7d vs. 3.75/wk baseline) · informational; watch tomorrow
**die-mobiliar** — velocity_ratio 4.0× (9 mentions/7d vs. 2.25/wk baseline) · informational; watch tomorrow
**crypto-finance** — velocity_ratio 4.0× (5 mentions/7d vs. 1.25/wk baseline) · informational; watch tomorrow

**Cooling (5, informational — not a demote trigger):**
**deepseek** — 0.23× (19/7d vs. 81.75/wk) · post-spike normalization
**huggingface** — 0.28× (11/7d vs. 38.75/wk)
**xai** — 0.28× (21/7d vs. 76.25/wk) · IPO attention routed to the SpaceX entity
**cognition** — 0.32× (9/7d vs. 28.0/wk)
**cursor** — 0.32× (8/7d vs. 25.0/wk)

## 👀 Watch list (top 20)
Orgs trending upward but below promotion thresholds (3–7 mentions in window). Slugs that are concepts/sections rather than firms (`annex-i`, `consilium`) are tracked but never promotable.

**adobe** — 7 mentions / 2 src types · velocity 0.0× · —
**aijobs** — 7 mentions / 1 src types · velocity 0.0× · —
**annex-i** — 7 mentions / 3 src types · velocity 0.0× · (EU AI Act section — not a vendor)
**apertus** — 7 mentions / 4 src types · velocity 0.0× · swiss-model
**azure-ai-foundry** — 7 mentions / 3 src types · velocity 0.0× · (Microsoft product)
**brookfield** — 7 mentions / 3 src types · velocity 0.0× · infra-capital
**caisi** — 7 mentions / 1 src types · velocity 0.0× · governance
**claude-cowork** — 7 mentions / 3 src types · velocity 0.0× · (Anthropic product)
**coinbase** — 7 mentions / 2 src types · velocity 0.0× · fintech
**consilium** — 7 mentions / 2 src types · velocity 0.0× · (EU body slug — not a vendor)
**constellation** — 7 mentions / 2 src types · velocity 0.0× · energy
**datadog** — 7 mentions / 4 src types · velocity 0.0× · observability
**gates** — 7 mentions / 3 src types · velocity 0.0× · —
**iapp** — 7 mentions / 2 src types · velocity 0.0× · governance
**latham** — 7 mentions / 2 src types · velocity 0.0× · legal
**muse-spark** — 7 mentions / 1 src types · velocity 0.0× · —
**oci** — 7 mentions / 2 src types · velocity 1.14× · (Oracle Cloud)
**peax** — 7 mentions / 2 src types · velocity 4.0× · vertical-app
**pentagon** — 7 mentions / 2 src types · velocity 0.0× · gov
**priore** — 7 mentions / 2 src types · velocity 0.0× · —

## ⚠️ Silent covered vendors (0)
No `_auto_added` enterprise vendor crossed the 60-day silence threshold this run (auto-demote stays disabled regardless). The soft-cap deep-watch path is the active mechanism here, but it had no eligible candidates today (all over-cap silent entries are still inside their hot-event TTLs).

## Tally summary
- Orgs tracked: 1465
- Covered (priority): 6 (priority_vendors list)
- Covered (enterprise — incl. 89 auto-added): 97
- Covered (informal URL list): host-matched via `vendor_blogs` / `governance_sources` (not separately counted this run)
- Uncovered (coverage field): 1363
- New orgs added to tally today: 0 (every org in today's files was already tracked; radar breadth surfaced only noise/duplicates — `alephalpha`→existing `aleph-alpha`, plus `cma`/`dbs`/`ft`/`colorado`/`connecticut` etc. rejected by the "what's an org" rules; `fonio.ai` appeared once-per-file, below the 2×-in-file bar)
- Hot-event records appended to tally today: 29 (mention-level; only `databricks`/`interconnects`/`oracle`/`spacex` reached `hot_event` tier, and none met the apply bar)

## Sources scanned this run
- discovered_orgs.json (prev last_updated 2026-06-10)
- Today's source files: news/2026/06/2026-06-11.md, papers/2026/06/2026-06-11.md, blogs/2026/06/2026-06-11.md, jobs/2026/06/2026-06-11.md, linkedin/2026/06/2026-06-11.md (stub — Chrome unavailable), daily/2026/06/2026-06-11.md
- Radar JSONs in window: 30 (2026-05-13 … 2026-06-10)
- sources.json snapshot before edit → sources.json.vendor.bak (0 changes applied — backup unchanged from prior run)
