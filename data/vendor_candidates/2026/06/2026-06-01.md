# Vendor sweep — 2026-06-01

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back this run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 0 promotion(s).** No sustained-promote candidate could be applied — see Pending below. Every org at `promote` tier today lacks a `blog_url_hint`, so per the apply gate (Step C.5) there is no URL to put in `blog_urls=[...]` and the entry would be useless to the news collector. All such candidates were rejected (`rejected_no_blog_url`) and are NOT logged.
- **Added 2 hot-event vendor(s):**
  - **decart** — `blog_urls=["https://www.decart.ai"]`, expires **2026-07-01**. Trigger: "valuation" / funding. Israeli AI firm Decart closed a **$300M round at ~$4B valuation** (led by Radical Ventures; NVIDIA, Sequoia, Benchmark, Andrej Karpathy as angel) for inference-optimization software and real-time generative "world models." New org added to tally today.
  - **exa** — `blog_urls=["https://exa.ai/blog"]`, expires **2026-07-01**. Trigger: "Series C" / "raised" / "valuation". AI-native search/retrieval startup Exa raised a **$250M Series C at a $2.2B valuation** (a16z-led) to scale agent-facing retrieval infrastructure. New org added to tally today.
- **Removed 0 expired hot-event entry(s).** Earliest existing auto-added expiry is 2026-06-13; nothing has lapsed yet.
- **Demoted 0 to deep-watch.** `enterprise_vendors` is at 91/30 (well over the soft cap), but deep-watch demotion additionally requires an `_auto_added` entry silent for ≥ 60 days. The pipeline is only ~18 days old, so the most-silent auto-added entry is 14 days — none qualify. No demotions this run. (Cap pressure will resolve as the first 2026-05-14 hot events expire on 2026-06-13.)
- **Re-promoted 0 from deep-watch.** `deep_watch_vendors` is empty.
- **Skipped 0 silent vendor(s).** No covered vendor crossed its silence threshold today.

## ⏳ Pending — promote-tier orgs that cannot auto-apply (0 applied)

102 orgs sit at `promote` tier today, but **none has a `blog_url_hint`**, so none can be applied (Step C.5 rejects them to `rejected_no_blog_url`). They are tracked for situational awareness only — to promote any of these, add a canonical `blog_url_hint` to its entry in `discovered_orgs.json` and it will auto-apply on the next sweep once it holds promote tier for 2 consecutive days. Genuine, recognizable companies/institutions among them:

**agentforce** — ~76 mentions, 3 src types · velocity 3.63× (accelerating)
**codex** — ~66 mentions, 4 src types · velocity 3.33× (accelerating)
**oracle** — ~60 mentions, 5 src types · velocity 1.80×
**amazon** — ~83 mentions, 4 src types · velocity 1.78×
**nexthink** — ~47 mentions, 3 src types · velocity 1.87×
**ey** — ~33 mentions, 6 src types · velocity 1.82×
**pwc** — multi-source Swiss-cluster hiring signal
**alibaba**, **gartner**, **langgraph**, **stripe**, **mongodb**, **chase**, **mastercard**, **ramp** — all promote-tier, all missing a blog URL hint.

(The bulk of the 102 are tally artifacts — concept/product/person/publisher slugs leaked into `discovered_orgs.json` by earlier LLM runs: `arxiv`, `mcp`, `claude`, `willison`, `switzerland`, `medium`, `linkedin`, `every`, `mind`, `iso`, `gemini`, `qwen`, `github`. These are not corporate entities and are never promotable. A `consolidate-memory`-style cleanup of the tally would sharpen these numbers.)

## ⚡ Velocity signals (94 accelerating, 985 cooling)

Posting cadence shifts, independent of tier. Recognizable companies accelerating today:

**Genuine accelerating:**
**adobe** — 4.0× (4 mentions/7d vs. 1.0/wk baseline) · informational; watch tomorrow
**agentforce** — 3.63× (69/7d vs. 19.0/wk) · Salesforce's agent layer; watch closely
**codex** — 3.33× (55/7d vs. 16.5/wk) · OpenAI coding agent (brand → OpenAI at build time)
**gpt-5** — 3.11× (21/7d vs. 6.75/wk) · brand → OpenAI
**slack** — 3.06× (13/7d vs. 4.25/wk)
**huawei** — 3.0× (3/7d vs. 0.75/wk) · agent-memory research mentions
**promptarmor** — 3.0× (3/7d vs. 0.75/wk) · prompt-injection/exfiltration demo drove the day's top HN AI thread

No org hit `surging` (≥ 5.0×), so no velocity-path hot_event auto-applied this run. 985 orgs are flagged `cooling` (informational only; the post-holiday-weekend mention drop is broad — not a demote trigger).

## 👀 Watch list (150)

Orgs trending upward but below promotion thresholds (3–7 recent mentions). Recognizable, genuinely-new ones worth a look:

**adobe** — watch · velocity 4.0× · investing/world-models angle (Decart backer)
**huawei** — watch · velocity 3.0× · self-evolving agent-memory paper (MUSE-Autoskill)
**promptarmor** — watch · velocity 3.0× · agent-security researcher (Sheets exfiltration exploit)
**plaid**, **letta** — uncovered, low-but-steady; both legitimate vendors in the agentic-finance / agent-memory space, holding in watch.

(Remaining ~145 are dominated by tally-noise slugs; no genuine vendor crossed into promotable range today.)

## ⚠️ Silent covered vendors (0)

None. Every priority and enterprise vendor has a mention within its silence window.

## Tally summary

- Orgs tracked: 1387
- Covered (priority): 5
- Covered (enterprise — incl. 83 auto-added of 91 total): 91
- Covered (informal URL list): tracked via `vendor_blogs` / `governance_sources` host lists
- Uncovered: 1290
- New orgs added to tally today: 2 (decart, exa)
- Hot events recorded today: 2 (decart, exa)

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-05-31 → now 2026-06-01)
- Today's source files: news, papers, blogs, jobs, linkedin, daily (all 2026-06-01)
- Radar JSONs in 30-day window: 80 (breadth_orgs_7d consumed; no new genuine org slugs beyond existing tally + decart/exa)
- sources.json snapshot before edit → sources.json.vendor.bak (2 change(s) applied: +2 hot events)
