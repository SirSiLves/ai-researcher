# Vendor sweep — 2026-06-18

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back this run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 0 promotion(s).** No `promote`-tier org cleared the auto-apply gate: the only slugs at `promote` tier today (e.g. `roche`, `stripe`, `ey`, `nous-research`, plus several non-org artifacts) carry no `blog_url_hint`, so there is no URL to register with the news collector. Rejected per Step C.5 (`rejected_no_blog_url`); not logged.
- **Added 2 hot-event vendor(s):**
  - **cognition** — `blog_urls=["https://cognition.ai/blog"]`, expires 2026-07-18. Trigger: Cognition raises **$1B at a $26B valuation** (roughly doubling in 8 months); coding-agent vendor (Devin). Genuine in-window funding event.
  - **cursor** — `blog_urls=["https://www.cursor.com/blog"]`, expires 2026-07-18. Trigger: Cursor / Anysphere **$50B valuation reset** and enterprise-momentum coverage in the window. Genuine in-window event.
- **Removed 5 expired hot-event entry(s):** `manus`, `pinecone`, `weaviate`, `isomorphic-labs`, `stainless` — all `_expires_on=2026-06-17 < today`, TTL elapsed. Removed and logged as `expire-remove`.
- **Demoted 0 to deep-watch (96/30 cap).** The enterprise list sits at 96 (well over both the 30 soft cap and the 45 overflow line, so the OVERFLOW regime is active with a 14-day silence bar). However, **every** silent auto-added entry still carries a future `_expires_on` TTL, and per the rule TTL-bearing hot-event entries are exempt from overflow demotion — the TTL is left to remove them. A large cluster of TTLs lands 2026-06-23 → 2026-06-27, so the cap will converge sharply over the next ~9 days without forced demotions.
- **Re-promoted 0 from deep-watch.** `deep_watch_vendors` is empty.
- **Skipped 0 silent vendor(s).** No covered vendor crossed its silence threshold this run (auto-demote remains disabled by default).

## ⏳ Pending — needs one more day at promote tier (0)

No org is one sustained day away from a promotion. The `promote`-tier slugs today are either non-org artifacts (e.g. `annex-i`, `annex-iii`, `vs-code`, `postgres`, `spring`, `eugene`) or real firms without a `blog_url_hint` (`roche`, `stripe`, `ey`, `nous-research`) — none applicable until a blog URL is attached. (These artifacts are flagged for the next discovered_orgs cleanup wave; the keyword over-matching in historical `hot_events` arrays is the root cause.)

## ⚡ Velocity signals (3 accelerating, 2 cooling)

Among orgs mentioned today (independent of tier):

**Accelerating (3):**
- **ona** — velocity_ratio 4.0× (56 mentions/7d vs ~14/wk baseline) · already enterprise-covered (OpenAI acquisition target for Codex runtimes); watch closely.
- **google-deepmind** — velocity_ratio 4.0× (8/7d vs 2/wk) · noisy partial slug; canonical coverage is the `google_deepmind` priority vendor. Informational.
- **linux-foundation** — velocity_ratio 3.67× (11/7d vs 3/wk) · already enterprise-covered (MCP/Agentic AI Foundation, DNS-AID). Informational.

**Cooling (2):** `aspaara` (0.25×), `openrouter` (0.25×) — informational; not a demote trigger.

No org hit the surging threshold (ratio ≥ 5.0), so no velocity-path hot events this run.

## 👀 Watch list (20)

Uncovered orgs trending upward but below promotion thresholds (3–7 mentions / 30d). Several entries are low-confidence slugs (people surnames / publisher fragments) carried from prior extraction noise; the genuinely org-shaped ones to keep an eye on:

- **datadog** — 7 mentions / 4 src types · velocity 0.0× (cooling) · observability/AI-monitoring
- **dbs** — 7 / 2 · velocity 4.0× (accelerating) · note: `dbs-bank` is already enterprise-covered (likely the same actor; alias-merge candidate)
- **oci** — 7 / 3 · steady · Oracle Cloud Infrastructure
- **ollama** — 7 / 2 · cooling · local-LLM runtime
- **atlassian** — 6 / 2 · steady
- **bmw** — 6 / 2 · steady · industrial-AI (Mistral/Airbus/BMW deals)
- **mulesoft** — 6 / 2 · cooling
- **daytona** — 5 / 4 · cooling · agent dev-environments
- **pgvector / pgvector-style infra** — 6 / 1 · cooling
- _(remaining slots filled by low-signal surname/publisher fragments: `priore`, `dong`, `elammari`, `garrytan`, `hoke`, `malvik`, `squalli`, `comparis`, `alignmt`, `axpo`, `bytebridge` — flagged for tally cleanup, not promotion candidates.)_

## ⚠️ Silent covered vendors (0)

No covered vendor crossed its silence threshold this run. (Silent auto-added entries over the soft cap would normally be deep-watch-demoted, but all are TTL-protected this run — see above.)

## Tally summary
- Orgs tracked: 1512
- Covered (priority): 5
- Covered (enterprise — 88 auto-added of 94 enterprise-coverage tags; sources.json enterprise_vendors = 96 incl. 8 manual): 94
- Covered (informal URL list): 0
- Uncovered: 1413
- New orgs added to tally today: 0 (today's new candidates — Samsung SDS, Infoblox, KAIST, Partners Group, Hitachi Energy — did not each reach the 2-mentions-in-a-single-file bar; skipped per "when in doubt, skip". Will resurface via radar if real.)
- Hot events recorded today: 2 applied (cognition, cursor); 3 candidates with blog URLs rejected as spurious (huggingface, langchain, llamaindex — keyword matches against "Sources scanned"/co-mention noise, no real event); 327 hot-tier candidates rejected for missing blog_url_hint (not logged).

## Sources scanned this run
- discovered_orgs.json (last_updated 2026-06-17 → 2026-06-18)
- Today's source files: news/2026/06/2026-06-18.md, papers/2026/06/2026-06-18.md, blogs/2026/06/2026-06-18.md, jobs/2026/06/2026-06-18.md, linkedin/2026/06/2026-06-18.md, daily/2026/06/2026-06-18.md
- Radar JSONs in window: 30 (2026-05-20 → 2026-06-17); breadth feedback yielded 0 valid new orgs (3 raw candidates — `aaif`, `cryptofinance`, `ft` — all rejected per the org-filter rules)
- sources.json snapshot before edit → sources.json.vendor.bak (7 change(s) applied: -5 expired, +2 hot events)
