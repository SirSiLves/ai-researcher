# Vendor sweep — 2026-06-24

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 0 promotions.** Two orgs classified `promote` today. **elastic** held promote for 2 consecutive days (06-23 → 06-24) and was re-promoted from deep-watch (see below) rather than added fresh. **cognizant** also held promote 2 days (48 recent mentions, 3 src types) but has **no `blog_url_hint`** in `discovered_orgs.json`, so it is rejected at apply (`rejected_no_blog_url`) — it would create an unusable `blog_urls=[]` entry. Needs a news/blog URL on its org record before it can be promoted.
- **Added 0 hot-event vendors (3 rejected — no blog URL).** Today's funding roundup carried three genuine fresh hot events — **stark-defence** (€500M raise, >€3.5B valuation; Sequoia, Founders Fund), **hadrian** (round quadrupling value to ~$7.5B), **peregrine** ($250M Series D at $2.5B) — all newly discovered orgs with no reliable `blog_url_hint` on disk. Per the anti-ghost-promotion rule (Step C.5) they were recorded in the tally with `hot_events` entries but **not applied** to `sources.json`. They will auto-apply on a future sweep if a blog URL surfaces.
- **Removed 1 expired hot-event entry:** **lakera** (`_expires_on` 2026-06-23 < today) — hot-event TTL elapsed. `expire-remove`.
- **Re-promoted 1 from deep-watch:** **elastic** — returned via sustained promote (held `promote` tier 06-23 and 06-24). Moved from `deep_watch_vendors` back into `enterprise_vendors`, keeping its `blog_urls=["https://www.elastic.co/blog/"]`; `_expires_on` dropped (sustained promote, not a TTL'd hot event). `deep-watch-promote`.
- **Demoted 1 to deep-watch (92/30 cap, OVERFLOW regime):** **blackstone** — `_auto_added`, silent 14 days (last_seen 2026-06-10), no live TTL. List was 92 post-adds, over both the soft cap (30) and the overflow threshold (45), so overflow demotion ran at effective silence 14d, budget 5/run. Only blackstone qualified: every other silent `_auto_added` entry still carries a live `_expires_on` TTL and is exempt (let the TTL expire it instead). Moved to `deep_watch_vendors` with `_demoted_*` fields; `blog_urls` preserved for exact re-promotion. `deep-watch-demote`.

Net effect on `enterprise_vendors`: 92 → 91 (−lakera, +elastic, −blackstone). Manual entries untouched.

## ⏳ Pending — needs one more day at promote tier (0 applyable)

- **cognizant** — 48 mentions, 3 src types (tech_news, jobs, daily), days at promote: 2/2 (gate met) but **no `blog_url_hint`** → blocked at apply. Add a URL to the org record to unblock. Not a timing issue — it cleared the consecutive-day gate today.

## ⚡ Velocity signals (4 accelerating, 0 cooling of note)
Orgs whose posting cadence changed significantly today. Independent of tier.

**Accelerating (≥3.0×):**
- **cognizant** — velocity_ratio 4.0× (48 mentions/7d vs. 12.0/wk baseline) · at promote tier but unapplyable (no URL); watch closely.
- **z-ai** — velocity_ratio 4.0× (28/7d vs. 7.0/wk) · GLM-5.2 (MIT-licensed) is the story; informational, watch tomorrow.
- **baidu** — velocity_ratio 4.0× (6/7d vs. 1.5/wk) · Unlimited OCR open-source push; informational.
- **krea** — velocity_ratio 4.0× (6/7d vs. 1.5/wk, off a brand-new base) · Krea 2 open-weights image model; informational.
- **sonarsource** — velocity_ratio 4.0× (5/7d vs. 1.25/wk) · Swiss job-posting signal (Sonar Geneva role).

No `surging` (≥5.0×) org this run, so no velocity-path hot-event auto-applied.

## 👀 Watch list (157 total; genuine orgs trending below promote)
Uncovered orgs with 3–7 recent mentions. Shown below filtered to clear corporate entities (the raw 157 includes name/concept leakage — `consilium`, `jin`, `lee`, `zhao`, `priore`, `khayyam` etc. — left for a future tally cleanup, not promotion candidates).

- **datadog** — 7 mentions / 4 src types · observability vendor
- **infosys** — 7 mentions / 3 src types · IT-services adopter
- **stripe** — 7 mentions / 3 src types · payments/agentic-commerce
- **constellation** — 7 mentions / 2 src types
- **general-catalyst** — 7 mentions / 2 src types · VC
- **ontinue** — 7 mentions / 4 src types · velocity 2.86× · enterprise-deployer signal, near accelerating threshold

## ⚠️ Silent covered vendors (0)
No priority/enterprise vendor classified `covered_silent` this run (all have a mention within their silence window). Silent `_auto_added` entries over the soft cap are handled by deep-watch demotion above, not flagged here.

## Tally summary
- Orgs tracked: 1540
- Covered (priority): 5
- Covered (enterprise — incl. 83 auto-added): 91
- Covered (deep-watch holding tier): 3 (deloitte, palantir, blackstone)
- Uncovered: 1443
- New orgs added to tally today: 6 (krea, stark-defence, hadrian, peregrine, solo-io, andile)
- Hot events recorded today: 3 (stark-defence, hadrian, peregrine — all recorded, none applyable for lack of a blog URL)

## Sources scanned this run
- discovered_orgs.json (last_updated before run: 2026-06-23)
- Today's source files: news/, papers/, blogs/, jobs/, linkedin/, daily/ (all 2026-06-24)
- Radar JSONs in window: 29 (2026-05-26 → 2026-06-23)
- sources.json snapshot before edit → sources.json.vendor.bak (3 changes applied: 1 expire-remove, 1 deep-watch-promote, 1 deep-watch-demote)
