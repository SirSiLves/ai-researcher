# Vendor sweep — 2026-08-03

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

**3 mutations. Added 0 promotions, added 2 hot-event vendors, removed 1 expired entry.**

**Added 0 promotions.** No slug cleared the sustained-day gate (`min_consecutive_days_at_promote: 2`). Everything at `promote` tier today is either at day 1 of its streak (see Pending) or has no `blog_url_hint` to put in `blog_urls=[...]`.

**Added 2 hot-event vendors** — both keyword-path M&A/funding, both TTL → **2026-09-02**:

- **`zenity`** — `https://www.zenity.io` · **$125M Series C led by Norwest**, with SoftBank Vision Fund 2, Qumra, Hitachi Ventures and LG Technology Ventures, taking it to ~$185M total on revenue that tripled in each of the last two years. The product secures enterprise AI agents. New to the tally today: 5 mentions across `tech_news` + `daily_synthesis`.
- **`horizon3`** — `https://www.horizon3.ai` · **$250M Series E led by NightDragon and NEA at a $2B valuation**, more than tripling in 14 months. NodeZero runs continuous AI-driven penetration tests against live production for ~7,200 customers, approaching $100M ARR at 120% YoY. New to the tally today: 7 mentions across the same two source types.

Both land in **the fastest-moving topic on the board** — the 08-02 radar has *AI security & offensive-security agents* surging at **+67.9% 7d** — and the news file's own read is that "capital is moving to the agent-security layer faster than to agent frameworks themselves."

> ⚠️ **Both `blog_urls` are root domains inferred from the company name, not fetched** (this skill never fetches). That is the same convention used for `etched` → `etched.com` and `norm-ai` → `norm.ai` on 2026-07-26, and the root domain is chosen deliberately over a guessed `/blog` path so the first collector fetch cannot fail on a path that never existed. `fallback_search` is set to `site:` on the same domain, so the entry degrades to a working search even if the root is wrong. **Verify on tomorrow's news run.**

**Removed 1 expired hot-event entry:**

- **`together-ai`** — `_expires_on: 2026-08-02 < 2026-08-03`. Yesterday's file predicted this exactly ("the rule fires on `_expires_on < TODAY`, so it reaps tomorrow"). It had been 22 days silent. Coverage reset to `uncovered`, `removed_on: 2026-08-03`.

**Demoted 0 to deep-watch (62/cap 30, overflow regime).** The regime computed correctly — `current_count` 62 > `overflow_threshold` 45, so the silence bar dropped to 14 days, target 45, budget 5 — and found **zero eligible candidates for the third run running**. But the composition of the blockage changed, and mostly for good reasons:

| slug | silent | why not demoted |
|---|---|---|
| `prime-intellect` | 23d | TTL 2026-08-09 — exempt by rule |
| `manus` | 20d | TTL 2026-08-13 — exempt by rule |
| `unitree` | 19d | TTL 2026-08-14 — exempt by rule |
| `baseten` | ~~28d~~ **0d** | genuinely re-mentioned today (collector fetched 1 item) |
| `sambanova` | ~~21d~~ **0d** | genuinely re-mentioned today |
| `kilo-code` | ~~18d~~ **0d** | **alias repair** — see below; it was never actually silent |
| `together-ai` | 22d | reaped by TTL before C.6 ran |

The shielded list shrank from **seven** entries yesterday to **three**. The next non-shielded candidate is `swiss-re` at 11 days — three short of the bar.

**Re-promoted 0 from deep-watch.** `kpmg` is the only deep-watch slug at `promote` tier today and it is at day 1 of 2 (see Pending).

**Skipped 0 silent vendors.** No entry on any list is past its silence threshold (45d priority / 60d enterprise). See the dead-feed section for why that zero is not the reassurance it looks like.

Net effect: `enterprise_vendors` **61 → 62** (54 auto-added, 8 manual — manual entries untouched). `deep_watch_vendors` **14 → 14**. `priority_vendors` untouched, as always.

## 🚨 Dead or dying vendor feeds — flagged, NOT applied (7)

**This section exists because the automated silent-vendor check cannot see any of these.** `covered_silent` is computed from `discovered_orgs.last_seen` — "was this name mentioned anywhere in the corpus" — not from feed health. Every vendor below returns **HTTP 200 with stale content**, so it reads `covered_healthy`. Today's run makes the failure mode unusually visible: **`lakera` reads `covered_healthy` with `last_seen: 2026-08-03` because the only place its name appears today is the news collector's own report that its feed is dead.** That is the metric grading itself.

No mutation and **no log line** was emitted for any of these. Step D's canonical verb table has no verb for a URL repair and this run was instructed not to invent one (2026-08-02 emitted a one-off `url-repair`; it is deliberately not repeated). **These need a human edit to `sources.json`.**

| Entry | List / ownership | Dead URL | Verified replacement | Evidence (direct fetch, today) |
|---|---|---|---|---|
| `lakera` | enterprise, `_auto_added` 07-17 | `www.lakera.ai/blog` | **none — company appears absorbed** | no dated post since **Nov 2025**; "recent news" renders empty; footer and company links now point to `checkpoint.com` |
| `kilo-code` | enterprise, `_auto_added`, TTL 08-15 | `kilocode.ai/blog` | `blog.kilo.ai/` | domain **retired** after the Anaconda acquisition; the collector already pulled **2 items** from `blog.kilo.ai` today, so only the configured URL is wrong |
| `sap` | enterprise **(MANUAL — sacred)** | `news.sap.com/topics/artificial-intelligence/` | `news.sap.com/` | **~80 days stale**, newest 15 May — far worse than the "~10 day lag" previously recorded |
| `salesforce` | enterprise **(MANUAL — sacred)** | `salesforce.com/news/category/products/ai-features/` **and** `salesforce.com/blog/category/artificial-intelligence/` | `salesforce.com/news/content-types/press-releases/` **+** `salesforce.com/blog/category/agentforce/` | **both configured URLs 404**; the replacements are the ones the collector actually fetched successfully today |
| `xai` | enterprise, `_auto_added` 05-14 | `x.ai/news` | `releasebot.io/updates/xai` (the fallback used today) | renders only through **29 May** |
| `deepseek` | **`priority_vendors` (SACRED)** | `api-docs.deepseek.com/updates` | `huggingface.co/deepseek-ai` model cards | stale at **24 Apr** despite a confirmed 31 Jul release; `/news` 301s to the docs home |
| — | `tech_news_sites` URL list | `venturebeat.com/category/ai/feed/` | `venturebeat.com/feed/` (already configured, working) | stale since **19 May**; the generic feed carried today's GraphRAG piece, so the AI-category feed just needs dropping |

**Priority order for the human edit.** `salesforce` first — it is the only entry where *neither* configured URL works, and this is the **second consecutive day** it has been reported. Then `sap` (80 days stale while its own largest AI move of the week, the Prior Labs close, is absent from its newsroom). Then `lakera`, which is a different decision from the rest: it is not a broken URL but a **company that no longer publishes independently**. `check-point` is already in the tally (11 mentions, `blog_url_hint: https://blog.checkpoint.com`) and hit `promote` tier today at day 1 of 2 — if it holds tomorrow it auto-promotes, at which point retiring `lakera` becomes a clean swap rather than a coverage loss.

**Two structural notes.** (1) `lakera` and `xai` both show up in today's *cooling* velocity list at 0.25× — the mention-based signal does register the decline, it just never crosses into `covered_silent` because the 60-day enterprise window is far longer than a feed takes to die. (2) A real fix needs a new SKILL step that consumes the collector's `(failed)` / `(stale)` markers from its "Sources scanned" block, plus a canonical verb. Flagged, not actioned — that is a spec change, not a sweep decision.

## 🧬 Alias repairs (4 regressions + 6 additions)

Same class as yesterday's `Qwen`/`GLM`/`Kimi` repair: an entry whose `aliases` list is empty or wrong falls back to matching the bare slug, which never appears in prose, so the vendor accrues **false silence** and drifts toward a demotion it did not earn.

- **`kilo-code`** ← `"Kilo"`. **This one was live.** The vendor is written simply "Kilo" since the Anaconda acquisition (blog now `blog.kilo.ai`), and the alias `["Kilo Code"]` matched **0** of today's 3 mentions. It was reading **18 days silent** on a day it was the subject of a named story ("Kilo analysed 10,643 AI code reviews"). Now `last_seen: 2026-08-03`.
- **`swiss-re`** ← `"Swiss Re"`. No aliases at all, so matching fell back to the hyphenated slug. Was at 11 days silent and would have hit the 14-day overflow bar on **2026-08-06**.
- **`together-ai`** ← `"Together AI"`. Aliases were `["togetherai"]`, a form that appears nowhere in the corpus. Expired by TTL today regardless, but the tally entry survives and is now matchable.
- **`artificial-analysis`** ← `"Artificial Analysis"`. 29 mentions, uncovered, cited in three files today as the benchmark house behind the DeepSeek V4-Flash pricing story — and the hyphenated slug never matched the written two-word form.
- Ordinary additions: `giotto` ← `"Giotto AI"` / `"Giotto.ai"`, plus `novartis`, `adobe`, `stripe`, `visa`, `sequoia`.

## ⏳ Pending — needs one more day at promote tier (5)

Hit promote thresholds today but at day 1 of the required 2 consecutive days. Auto-applies tomorrow if the tier holds.

- **`moonshot`** — **288** mentions, 5 src types, 22 distinct days over 30d · 1/2 · `https://platform.moonshot.cn`. **The largest pending candidate this file has carried.** It only became visible because yesterday's run added the `Kimi`/`Moonshot` aliases; before that it was invisible to the matcher and pinned out of the promote path by a stale in-window hot event. Velocity 0.75× (cooling), which is expected — the volume is a month of K3 coverage, not a spike.
- **`owasp`** — 12 mentions, 4 src types, 4 days · 1/2 · `https://owasp.org/news/` · velocity 2.67×. Carried by the Top 10 for Agentic Applications (ASI06 memory poisoning) appearing in both the LinkedIn and papers slices.
- **`check-point`** — 11 mentions, 3 src types, 4 days · 1/2 · `https://blog.checkpoint.com` · velocity 0.36×. See the dead-feed section — this is the natural successor to `lakera`.
- **`enisa`** — 9 mentions, 3 src types, 4 days · 1/2 · `https://www.enisa.europa.eu/news` · velocity 1.33×. Anthropic granting it access to Mythos put it in the AI Act enforcement story two days running.
- **`kpmg`** — 9 mentions, 4 src types, 6 days · 1/2 · `https://kpmg.com/xx/en/our-insights.html` · velocity 4.0× · currently in **`deep_watch_vendors`**, so this would be a **re-promotion**, not a new entry.

~100 further slugs sit at `promote` tier with no `blog_url_hint` and are structurally unapplicable — these are legacy tally pollution (`a2a`, `commission`, `gemini-3`, `eu-ai-act`, `swe-bench`, author surnames) inherited from pre-2026-05-23 extraction runs. They are filtered from this list, never logged, and cannot produce ghost promotions.

## ⚡ Velocity signals (109 accelerating, 0 surging, 1,323 cooling)

**Surging: 0.** No vendor cleared the 5.0× velocity path into `hot_event` today, so both hot-event adds came through the keyword path.

**Accelerating — top of the real-org list.** Note the **4.0× ceiling is an artifact**, not a reading: `velocity_ratio = v7 / max(v28_avg, 1.0)`, and a 4-week baseline that skips the un-swept 2026-07-31 clamps the denominator. Trust the sub-4.0 values.

- **`pictet`** 4.0× (36/7d vs 9.0/wk) · **`mem0`** 4.0× (22 vs 5.5, and at `promote` tier) · **`vector8`** 4.0× (22 vs 5.5) · **`odyssey`** 4.0× (20 vs 5.0) · **`qualcomm`** 4.0× (20 vs 5.0, covered) · **`stripe`** 4.0× (18 vs 4.5 — the Kai/Deep Agents case study carried three files) · **`gema`** 4.0× (17 vs 4.25) · **`suno`** 4.0× (16 vs 4.0) · **`anyscale`** 4.0× (14 vs 3.5) · **`bafin`** 4.0× (14 vs 3.5) · **`netflix`** 4.0× (14 vs 3.5, covered).

**Cooling, and worth reading — every one of these is a covered vendor:** `cerebras` 0.10× · `cognition` 0.19× · `sambanova` 0.24× · `google-deepmind` 0.25× · **`lakera` 0.25×** · **`xai` 0.25×** · `broadcom` 0.27× · `anaconda` 0.31× · `google_deepmind` (priority) 0.31×. Cooling is informational and never a demote trigger on its own — but `lakera` and `xai` cooling while their feeds are confirmed dead is the mention-signal quietly agreeing with the dead-feed section above.

## 👀 Watch list (157 tracked, top 20 shown)

Trending upward but below promotion thresholds.

- **`superblocks`** — 7 mentions / 2 src types · 4.0× · app-generation. **New today.** AWS multi-year joint marketing agreement puts it inside customers' own VPCs (Aurora instead of Supabase, inference via Bedrock). 50 employees, $60M raised — the raise is cumulative, not a today event, so deliberately not a hot event.
- **`kandou`** — 7 / 2 · 4.0× · semiconductor. **New today.** Saint-Sulpice (VD); first Swiss listing to make neuro-symbolic reasoning a hard requirement.
- **`unique-ai`** — 7 / 2 · 4.0× · enterprise-genai (CH) · **`viseca`** 7 / 2 · 4.0× · **`ti-m`** 7 / 1 · 4.0× · **`swissquote`** 7 / 2 · 4.0× · **`v-zug`** 7 / 2 — the Swiss enterprise-adopter layer keeps filling out from the jobs slice.
- **`the runtime`** 7 / 5 · 3.43× · **`chase`** 7 / 4 · 2.29× · **`plaid`** 7 / 4 · 1.14× · **`black-forest-labs`** 7 / 2 · 1.14× (frontier-lab) · **`bundeskartellamt`** 7 / 2 · 4.0× (regulator) · **`lm-studio`** 7 / 3 · **`naver`** 7 / 2 · **`schunk`** 7 / 2 · **`hu`** 7 / 3 · **`gergely-orosz`** 7 / 4 · **`theregister`** 7 / 2 · **`work-louder`** 7 / 2 · **`bjak`** 7 / 1 · **`kong`** 6 / 5 · 3.33×.

## ⚠️ Silent covered vendors (0)

No vendor on `priority_vendors` or `enterprise_vendors` is past its silence threshold (45d / 60d). **Read this together with the dead-feed section** — seven vendor feeds are confirmed dead or stale by direct fetch today and all seven read `covered_healthy`, because this check measures name mentions, not feed health.

## 🚫 Considered and not applied

**Hot-event tier, rejected at Step C.5 (no `blog_url_hint`) — not logged:**

- **`visa`** (17 mentions in window, 4 src types) and **`biocatch`** (7, new today) — Visa's **$2.4B cash acquisition of BioCatch** from Permira, its largest cybersecurity deal ever, covering 1.8B devices and 760M users across 350+ banks. Both classify `hot_event` correctly; neither has a URL on disk. Judgment note: BioCatch is the *target* and stops being an independent publishing source (same call as `oasis-security` 07-29, `permiso`/`anyscale` 07-30), and Visa is a payments network whose newsroom is not an LLM/GenAI source. Add a `blog_url_hint` to either if you want them applied on a future event.
- **`figure`** — 4th consecutive `promote` day, block held. Alias was constrained on 2026-08-01, but the pre-correction counters (16 of 18 hits were the English word "figure") stay inside the 30-day window for another ~2 weeks.

**Real transactions that did not reach the ≥2-genuine-mentions bar** (URL slugs and markdown link-text repeats of the same headline do not count — precedent set 2026-08-02): **OLIX** $312M at $3.3B (photonic inference chips, Arm/Hudson River Trading/Reed Hastings), **DeepX** Series D at ~$2.2B (Korean edge NPU), **Valar Atomics** $1B at $6B (Sequoia; NVIDIA as first commercial deployment), **Yellow.ai** $550M SPAC merger with Bluerock, **DesignArena** $7.9M, **June** $20M pre-seed. Each has exactly one in-prose mention inside a compute-funding roundup. They will surface on their own if the story continues.

**Not hot events:** Superblocks' cumulative "$60M raised"; Big Tech passing **$1tn** of AI capex with $745bn planned for 2026 (magnitudes, not deals); the SAP/Prior Labs close (both already covered, so classification short-circuits); OpenAI "prepares a possible IPO" (priority vendor, unconfirmed); EU AI Act €15M/3% ceilings and the "$250k/year H100" figure (amounts, not transactions).

**New orgs — 11 inserted.** `zenity`, `horizon3`, `biocatch` (the three agent-security deals); `superblocks`; `interpol` (African Cyberthreat Assessment 2026 — AI in 55% of reported cybercrime across 36 countries, losses $192M → $484M); `ofcom` (opening a sweeping Big Tech oversight review); `simon-institute` (Geneva think tank working frontier AI governance × international diplomacy, 5 posts in one day — a new institutional shape for this file); `kandou`, `lonza`, `kyndryl` (Swiss/CH jobs layer); `hoplite` (YC S26, managed cloud sandboxes for coding agents).

**Deliberately not inserted:** `giotto-ai` — **duplicate**; `giotto` has been the slug for Giotto.ai Lausanne since 2026-05-22 (17 mentions), so the alias was added to the existing entry rather than minting a second slug. `june` (**alias hazard** — 4 of its 8 corpus hits today are calendar dates: "June 2026", "the June 2 executive order"; exactly the `figure` failure mode). `arm` (alias hazard — matched "the Lidl/Kaufland tech **arm**" in the same file). `cogent-ai` (alias hazard — "Cogent" is an English adjective and a US network operator; VR-1's own figures are labelled preliminary and access is gated). `embabel` and `spring` (agent *framework* and its host framework, not legal entities). `actian`, `project-vail`, `bluerock` (both hits are a URL path plus a byline / a SPAC shell). `jobmaps` (a job board is a collector data source, not an AI vendor). Investors named in a round (Norwest, NightDragon, NEA, Permira, Fundomo) are never the subject of the event.

**Radar breadth:** 332 of 701 candidates rejected, **0 inserted** — the usual mix of GitHub handles (`affaan-m`, `farion1231`, `different-ai`), product brands with a parent (`cursor`, `kiro`, `astra`), and concept slugs.

## 📉 Cap outlook

`enterprise_vendors` is at **62** against `max_enterprise_vendors: 30` and the overflow line of 45. The demoter has now spent **0 of its 5-per-run budget on three consecutive runs**, but the reason has shifted from "everything is TTL-shielded" to "the long-silent entries came back": three of yesterday's seven shielded entries genuinely re-published, and a fourth (`kilo-code`) was never silent at all — a matcher bug.

TTL expiry alone walks the list down without any demotion: **62 → 45 by 26 August** (crossing the overflow line), **→ 41 by 29 August**, **→ 39 on 2 September** when today's two adds lapse. 31 auto entries carry no TTL and are the only ones the deep-watch path can ever reach. Convergence remains throughput-limited, not broken.

## Tally summary

- Orgs tracked: **1,734** (+11 today)
- Covered (priority): **6**
- Covered (enterprise — incl. **54** auto-added, 8 manual): **62**
- Deep-watch (holding tier, not fetched): **14**
- Covered (informal URL list): 0
- Uncovered: **1,651**
- New orgs added to tally today: **11**
- Orgs whose counters moved today: **252**
- Hot events recorded today: **3 transactions across 4 orgs** (Zenity Series C, Horizon3 Series E, Visa/BioCatch)
- Alias repairs: **4 regressions + 6 additions**

## Sources scanned this run

- `discovered_orgs.json` (last_updated 2026-08-02 → **2026-08-03**)
- Today's source files (6): `news/2026/08/2026-08-03.md`, `papers/…`, `blogs/…`, `jobs/…`, `linkedin/…`, `daily/…`
- Radar JSONs in window: **30** (2026-07-02 → 2026-08-03)
- `sources.json` snapshot before edit → `sources.json.vendor.bak` (**3** changes applied)
- Audit: 3 lines appended to `vendor_changes.log`, all canonical verbs (`hot-event-add` ×2, `expire-remove` ×1). Log↔sources consistency re-verified after the write: `log_only_not_in_sources` **[]**, `sources_only_not_in_log` **[]**, deep-watch both **[]**.
