# Vendor sweep — 2026-08-02

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

**Added 1 promotion** — crossed the sustained-day gate (2 consecutive runs at `promote`):

- **`modular`** — `https://www.modular.com/blog` · 29 mentions, 6 source types, 10 distinct days over 30d. Days at promote: 2/2 (2026-08-01, 2026-08-02). `expire-remove`d on 2026-07-30 when its hot-event TTL elapsed; the Qualcomm acquisition close (29 Jul) kept it in the file, and it now re-enters through the sustained-promote path **without an expiry**. Yesterday's sweep listed it as pending and predicted exactly this. Velocity `accelerating` (3.03×, 22 mentions/7d vs 7.25/wk). Caveat carried forward: Modular is now owned by Qualcomm, so if Mojo/MAX announcements migrate to `qualcomm.com` this entry becomes a duplicate — worth re-checking at its next silence check rather than now, since AWS/Qualcomm both stated the products continue under their own brand.

**Added 0 hot-event vendors.** No qualifying funding / M&A / IPO event in any of the six files. Explicitly **not** hot events today: Alibaba's ~7% Hong Kong share move on the Qwen3.8-Max launch (a price move, not a transaction); Big Tech capex passing **$1.1T** and Amazon guiding AI spend to **$220B** (magnitudes and guidance, not deals); Thinking Machines' finetuning service reportedly at "hundreds of millions in annual revenue" (revenue, not funding); Situational Awareness "**may have** sold its public portfolio" (unconfirmed, and a fund's own book); the EU AI Act's €35M/7% fine ceilings and OpenAI's "$2,000 per problem" figure (amounts, not deals). Nscale/Anyscale, Okta/Permiso and Cyera/Oasis appear only as web-search strings inside the Sources-scanned block and were actioned on 2026-07-29/07-30.

**Removed 0 expired hot-event entries.** Nothing was due: the earliest expiry is `together-ai` on **2026-08-02**, and the rule fires on `_expires_on < TODAY`, so it reaps tomorrow.

**Demoted 0 to deep-watch (61/cap 30, overflow regime).** The regime computed correctly — `current_count` 61 > `overflow_threshold` 45, so the silence bar dropped to 14 days and the target was 45 with a budget of 5 — but **zero entries qualified**. Every `_auto_added` entry silent ≥14 days is shielded by a live `_expires_on` TTL, which the rules explicitly exempt ("let the TTL expire those instead"):

| slug | silent | TTL |
|---|---|---|
| `baseten` | 27d | 2026-08-05 |
| `prime-intellect` | 22d | 2026-08-09 |
| `together-ai` | 21d | 2026-08-02 |
| `sambanova` | 20d | 2026-08-09 |
| `manus` | 19d | 2026-08-13 |
| `unitree` | 18d | 2026-08-14 |
| `kilo-code` | 17d | 2026-08-15 |

The next non-shielded entry was `zhipu` at 13 days — one day short of the bar, and **it would have been a false demotion** (see the alias-regression section below).

**Re-promoted 1 from deep-watch:**

- **`minimax`** — returned via `sustained_promote` (17 mentions, 4 src types, 6 distinct days over 30d; promote on 2026-08-01 and 2026-08-02). Demoted 2026-07-27 for 14-day silence; MiniMax H3 shipping open weights with day-0 ComfyUI support (192 pts on HN) is what brought it back. Entry carried forward exactly — same `blog_urls`, `fallback_search` and history; `_demoted_*` keys dropped, `_added_on` refreshed, no expiry.

**Skipped 0 silent vendors.** No entry on any list is past its silence threshold (45d priority / 60d enterprise) — see §"Silent covered vendors" for why that number understates the problem.

**Rejected 1 candidate that met thresholds but was not applied:**

- **`figure`** — still reads `promote` (3rd consecutive day) off counters accumulated *before* its alias was constrained on 2026-08-01. The correction stopped new over-matches ("five-**figure** invoice", "nine-**figure** bet") but the inflated history stays inside the 30-day window for another few weeks. Block held.

Net effect: `enterprise_vendors` 59 → **61** (+`modular`, +`minimax`). `deep_watch_vendors` 15 → **14**.

## 🔧 Feed-health repairs

New this run, prompted by the news collector confirming **nine dead or wrong vendor URLs by direct fetch** (see the "Feed-health notes" block at the end of `news/2026/08/2026-08-02.md`). Yesterday's sweep flagged this class and called it out of remit; this run splits it by ownership.

**Applied (1)** — the only one of the nine that sits on an entry this skill created itself, which is exactly the scope SKILL.md grants ("the auto-applier touches ONLY entries it added itself"):

- **`alibaba-qwen`** — `https://qwenlm.github.io/blog/` → **`https://qwen.ai/blog`**. Evidence: `qwenlm.github.io/blog/` *(failed — dead feed, redirects to qwen.ai/research, newest post 2025-09-23)*. `fallback_search` re-pointed to `site:qwen.ai`, and `blog_url_hint` in `discovered_orgs.json` updated so a future re-promotion is exact. Logged as `url-repair` (see the deviation note below).

**Deviation on record.** Step D's canonical verb table has no verb for a URL repair, and the SKILL says not to invent verbs. Leaving a real mutation unlogged is the worse failure, so this run emits `url-repair`. Verified safe: `pipeline/scripts/rebuild_change_logs.py` matches verbs against a fixed `VERB_RE` and returns `None` for anything else, so the line is ignored by the machine consistency check and cannot produce `log_only_not_in_sources` drift. If the URL-repair path is kept, `url-repair` should be added to the SKILL's verb table and to `VERB_RE`.

## 🚨 Dead feeds — needs a `sources.json` fix (8, NOT applied)

The remaining eight are **outside this skill's mandate as written**: six land on manual (non-`_auto_added`) `enterprise_vendors` entries, one on `priority_vendors`, one on the `governance_sources` URL list. SKILL.md is emphatic in two separate places — *"Manual entries are sacred… never modify it, never remove it"* and *"`priority_vendors` is sacred too. The auto-applier NEVER writes to `priority_vendors`"* — so a daily automated sweep rewriting hand-curated vendor config is precisely the failure mode that rule exists to prevent. **These need a human edit.** Every replacement below was verified by direct fetch by the news collector this morning:

| Entry | List | Dead URL | Verified replacement | Status |
|---|---|---|---|---|
| `sap` | enterprise (manual) | `sap.com/products/artificial-intelligence/news.html` | `news.sap.com/feed/` | 403 then 404 — page does not exist |
| `salesforce` | enterprise (manual) | `salesforce.com/news/category/products/ai-features/` **and** `salesforce.com/blog/category/artificial-intelligence/` | `salesforce.com/blog/category/ai/feed/` + `salesforce.com/news/feed/` | both 404 — **neither configured URL works** |
| `redhat_ibm` | enterprise (manual) | `ibm.com/think/topics/artificial-intelligence` | `ibm.com/think/news` + `newsroom.ibm.com/announcements?pagetemplate=rss` | alive but an evergreen explainer, zero dated items — never a news feed |
| `servicenow` | enterprise (manual) | `servicenow.com/company/media/press-room.html` **and** `servicenow.com/blogs/category/now-on-now/ai.html` | `newsroom.servicenow.com` | both unreachable — **neither configured URL works** |
| `n8n` | enterprise (manual) | `n8n.io/blog/` | `blog.n8n.io` | provenance-blocked. **Lowest urgency: the working URL is already the 2nd entry on this vendor** — only the dead one needs dropping |
| `workday_oracle` | enterprise (manual) | `blog.workday.com/en-us/topics/artificial-intelligence.html` | `blog.workday.com/en-us/artificial-intelligence.html` | 404 on the `topics/` path (replacement loads but renders no dates) |
| `deepseek` | **priority_vendors** | `api-docs.deepseek.com/news` | `api-docs.deepseek.com/updates` | 301 to docs home; archive stale since 2026-04-24 |
| — | `governance_sources` list | `artificialintelligenceact.eu/news/` | the AI Act Substack newsletter | 404, URL no longer exists |

Two of these (`salesforce`, `servicenow`) have **no working configured URL at all** — the collector only produced items via ad-hoc fallbacks. Those two are the ones to fix first.

Also **stale but not broken**, so no action proposed: `anthropic.com/engineering` (23 Apr), NIST AI RMF (7 Apr), GovAI Updates (24 Mar), Apollo Research (13 May), `cloud.google.com/.../whats-new-google-cloud` (19 Jun). VentureBeat is effectively unfetchable (Vercel checkpoint + stale RSS).

## 🧬 Alias regression — three covered vendors were reading silent for a matching bug

Found while auditing why `alibaba-qwen` sat 12 days silent on the day **Qwen3.8-Max was the single biggest item in the file**. Three entries carry `alias_hits` proving an earlier sweep *did* match their brand names, but have **no `aliases` list**, so matching silently fell back to the bare slug — which never appears in prose:

| slug | historical `alias_hits` | matched today (before) | aliases added |
|---|---|---|---|
| `alibaba-qwen` | `Qwen: 73`, `Alibaba: 34` | **0** (slug `alibaba-qwen` appears nowhere) | `Qwen`, `Alibaba Qwen` |
| `zhipu` | `Zhipu: 2` | 0 — while **GLM 5.2 is named 8×** today | `GLM` |
| `moonshot` | `Kimi: 134`, `Moonshot: 96` | partial (bare slug only) | `Kimi`, `Moonshot` |

After the fix all three have `last_seen: 2026-08-02`. Without it, `zhipu` crossed the 14-day overflow bar **tomorrow** and `alibaba-qwen` two days later — both would have been demoted for "silence" while publishing the week's biggest open-weights story. Deliberately **not** added: `Alibaba` to `alibaba-qwen` (that alias belongs to the separate `alibaba` entry; adding it would double-count). Flagged for next run: `GLM` could over-match "generalized linear model" — the `figure` lesson. All 8 hits today are "GLM 5.2"; check the hit profile tomorrow before trusting the count.

## ⏳ Pending — needs one more day at promote tier (1)

- **`cloudflare`** — 1/2 days · `https://blog.cloudflare.com/` · 15 mentions, 4 src types, 4 distinct days over 30d · velocity `accelerating` 3.56×. Agents Week 2026 opened today (day one named zero products and asked readers to put "what is an Agent Cloud" to their own agents; the week runs to 7 August with per-day embargoes). It has bounced in and out of `promote` since 2026-07-12 and been rejected each time; a week of daily Agents Week posts would be the first sustained run.

`weaviate` was pending yesterday and **fell out** of `promote` today (7 mentions/30d, below the 8 bar) — the streak resets. The other five slugs at `promote` for one day (`decoder`, `ec`, `ey`, `huawei`, `openrouter`) have no `blog_url_hint` and would be rejected at Step C.5 regardless.

## ⚡ Velocity signals (125 accelerating, 1,318 cooling, 0 surging)

No org reached `surging` (≥5.0×), so nothing was auto-promoted on the velocity path.

**Accelerating — top by absolute volume (real orgs only):**

- **`amazon`** — 3.76× (62/7d vs 16.5/wk) · Bedrock Agents Classic EOL + the $220B AI-spend guidance
- **`marktechpost`** — 3.24× (60/7d vs 18.5/wk) · trade press, not a vendor — carried the Onton and AMD Instella items
- **`pictet`** — 4.0× (30/7d vs 7.5/wk) · Geneva private bank, AI Security Engineer naming MCP servers
- **`kaiko`** — 3.23× (25/7d vs 7.75/wk) · Zurich health-AI, 12 open roles
- **`modular`** — 3.03× (22/7d vs 7.25/wk) · **promoted today**
- **`supabase`** — 3.83× (22/7d vs 5.75/wk) · already `enterprise` (TTL 2026-08-05) — worth watching, an expiry three days out on an accelerating vendor
- **`vector8`** 4.0× (19/7d) · **`odyssey`** 4.0× (18/7d) · **`qualcomm`** 4.0× (18/7d, TTL 2026-08-29) · **`perplexity`** 3.33× (15/7d)
- **`anyscale`** 4.0× (14/7d) · absorbed by Nscale; deliberately still not added — an acquired company stops being an independent publishing source
- **`gema`** 4.0× (14/7d) · **`netflix`** 4.0× (14/7d, promoted 08-01) · **`minimax`** 3.06× (13/7d, re-promoted today) · **`suno`** 4.0× (13/7d)
- **`mem0`** 4.0× (12/7d) · agent-memory vendor; published "Agent Memory is the Attack Surface", and the GitHub slice has flagged the untracked agent-memory cluster five days running

**Caveat on the 4.0× ceiling:** the pipeline skipped 2026-07-31, so a 7-day window with a thin 4-week baseline saturates at a hard 4.0×. Anything reported at exactly 4.0× is low-information; the ratios *below* 4.0 (amazon 3.76, supabase 3.83, marktechpost 3.24, kaiko 3.23, minimax 3.06, modular 3.03) are the trustworthy half.

**Cooling with real activity (7):** `cognition` 0.10× · `stratechery` 0.13× · `anaconda` 0.16× · `semianalysis` 0.19× · `google-deepmind` 0.25× · `venturebeat` 0.27× · `google_deepmind` 0.32×. Informational only — never a demote trigger. `cognition` was promoted on 08-01 while cooling; that is still the right call (the 30-day volume is real, just front-loaded) but it is now on the watch-for-demotion path.

## 👀 Watch list (154 total, top 20 shown)

Orgs trending upward but below promotion thresholds.

- **`black-forest-labs`** — 7 mentions / 2 src types · 3.43× · frontier lab, Code of Practice signatory
- **`bundeskartellamt`** — 7 / 2 · 4.0× · German competition regulator
- **`chase`** — 7 / 4 · 2.86× · enterprise adopter
- **`supsi`** — 7 / 2 · 4.0× · **new today** — University of Applied Sciences and Arts of Southern Switzerland (Lugano); first Italian-speaking-Switzerland employer in this pipeline
- **`bjak`** — 7 / 1 · 4.0× · **`flexion-robotics`** — 7 / 1 · 4.0× · **`v-zug`**, **`schunk`**, **`work-louder`** — 7 / 2 each · enterprise adopters
- **`lm-studio`** — 7 / 3 · local-inference tooling · **`naver`** — 7 / 2 · Korean lab
- **`ai-office`** — 6 / 4 · 0.8× · the EU body that started enforcing today
- **`bafin`** — 6 / 1 · 4.0× · German financial regulator putting insurers' AI use under supervisory scrutiny
- **`enisa`** — 6 / 3 · EU cybersecurity agency · **`japan-meti`** — 6 / 2 · government agency
- **`debian`** — 6 / 2 · 4.0× · **`grant-thornton`** — 6 / 2 · 4.0× · **`blp-digital`** — 6 / 2 · 4.0×
- **`oecd`** — 6 / 2 · 2.4× · published the five-step AI-evaluation-gap roadmap

## ⚠️ Silent covered vendors (0)

No entry on `priority_vendors` or `enterprise_vendors` is past its silence threshold. **That number is still measuring the wrong thing** — `covered_silent` reads `discovered_orgs.last_seen` ("was this org mentioned anywhere"), not whether the vendor's own feed resolves. This run addressed the gap two ways rather than restating it:

1. The nine confirmed dead URLs are enumerated above with verified replacements — one applied, eight escalated.
2. The alias-regression fix removes the *inverse* error: three vendors whose feeds are fine but whose mention counts read zero because the matcher was broken.

What is still missing structurally: no SKILL step consumes the collectors' "Sources scanned" failure lines, so feed health only reaches this file when a human pastes it in, as happened today. A `feed_health` block written by the news collector and read at §4 would close it.

## 🔍 List-health items reported but not actioned

1. **The cap will not move this week without the TTL drain.** Overflow regime allows 5 demotions/run at a 14-day bar; 0 qualified today because all seven ≥14-day-silent entries hold live TTLs. The unaided drain, assuming no new adds: `08-02` together-ai → 60 · `08-05` baseten, supabase → 58 · `08-09` prime-intellect, sambanova → 56 · `08-13` helsing, manus, tencent → 53 · `08-14` unitree → 52 · `08-15` anaconda, kilo-code → 50 · `08-18` prior-labs → 49 · `08-19` fireworks-ai → 48 · `08-21` amd, microsoft → 46 · `08-25` etched, norm-ai → 44 · `08-26` safe-superintelligence → 43 · `08-28` cyera → 42 · `08-29` nscale, okta, qualcomm → **39**. The list crosses the 45 overflow line around 25 August and reaches 30 during September. This is a throughput limit, not a bug.
2. **~90 tally slugs sit permanently at `promote` and are rejected every run.** Unchanged from yesterday. Legacy junk (`a2a`, `agent-memory`, `azure`, `commission`, `eth`, `gpt-5`, `iso`, `linkedin`, `stanford`, `workspace`) survives because the "what is an org" rules gate *insertion*, not existing entries. A real subset would be promotable if someone filled in a `blog_url_hint`: `bytedance`, `sakana`, `box`, `gartner`, `slack`, `workday`, `oracle`, `databricks`, `alibaba`, `huawei`, `openrouter`.
3. **Stale in-window hot events still pin real vendors out of the promote path.** `moonshot` (49 mentions/7d after today's alias fix), `cursor`, `spacex`, `ibm`, `accenture`, `uber` and ~18 others classify `hot_event` off events up to five weeks old, so §4's tier order never evaluates them for sustained promote. Every run since 2026-07-18 has applied a freshness gate that the SKILL's Step B does not specify; this run kept it. The clean fix is a SKILL change — narrow the hot-event window, or let a stale hot-event candidate fall through to the promote test.
4. **List drift, three lists, unchanged.** `web_search_queries` frozen at 60/60, `watched_repos` at 137 vs cap 80, and the LinkedIn collector reported `pulse_topic_queries` has grown to **95** entries — mostly auto-added fragments with no standalone search value ("you can", "use cases", and, revealingly, "returned empty", a collector's own error string harvested back into the query list). Same off-by-one root cause: the demotion comparison fires on `len > cap`, never on `len == cap`. None of these lists belong to this sweep; the fix is the comparison operator, not hand-pruning.
5. **Silent parser failures are a shared blind spot.** The GitHub collector's trending regexes returned 0 repos from all five pages **without erroring** and were still reported "ok". Today's alias regression is the same failure class inside this skill: a matcher that returns zero looks identical to a vendor that went quiet. Worth a "matched 0 aliases but has non-empty `alias_hits` history" assertion in a future revision.

## Tally summary

- Orgs tracked: **1723** (was 1714)
- Covered (priority): 6
- Covered (enterprise — incl. 53 auto-added of 61): **61**
- Deep-watch: **14**
- Covered (informal URL list): 0 _(the tally has never populated `informal_covered`; `vendor_blogs` / `governance_sources` hosts are not back-mapped to slugs)_
- Uncovered: 1641
- New orgs added to tally today: **9**
- Hot events recorded today: **0**
- Orgs matching at least once today: **204**

**New orgs (9):** `epoch-ai` (research institute; the nine-month AI-chip doubling figure — 2× news) · `onton` (neurosymbolic product search; Ontology 1 beats Google Shopping/Amazon on precision@10 — 3× news) · `unique-ai` (Swiss GenAI for financial institutions, hiring a lead for an existing global FDE team — 4× jobs) · `supsi` (Lugano; LLM/agentic-AI-for-finance posts — 5× jobs) · `swisslog` (KUKA group, internal LangChain/RAG stack — 3× jobs) · `akina`, `destinus` (Zurich, 3–4× jobs each) · `lufthansa`, `getty-images` (EU Code of Practice **deployer** signatories — the half the collector called the interesting one).

**Deliberately not inserted:** `c2pa` (a provenance *standard* — "C2PA said today…" fails the journalist test) · `bulgari` (brand of LVMH; brand→parent rule, and pure signatory-list fill) · `seedance` (ByteDance product brand) · `graphify` (surfaces only as a GitHub repo path) · `iberdrola`, `fastweb`, `synthesia` (never 2 hits in one file) · `giotto-ai`, `fhnw` (exactly 1 genuine mention each — the "2nd hit" is the same headline's URL slug) · `cequence`, `bedrock-data`, `crogl` (1 hit each, and the collector marks the whole block aggregator-sourced and unverified) · `viitorcloud`, `vthink`, `moor-insights`, `otto-group` (LinkedIn Pulse bylines, not article subjects) · `rapid-claw`, `trump-media`, `rocken`, `yousty`, `10x-partners`, `cradle`, `kuka`, `psi`, `harmattan-ai` (1 hit each).

**Radar breadth:** 643 distinct `breadth_orgs_7d` slugs across 30 radar JSONs; 349 already in the tally, **294 rejected** — 4 junk-list, 4 too short, 1 brand→parent, 285 skipped as GitHub handles, geographies or variant spellings. Zero breadth-derived insertions, consistent with the last several runs.

**Alias maintenance:** added `Mem0`→`mem0`, `BaFin`→`bafin`, `OECD`→`oecd`; plus the three regression repairs above (`Qwen`/`Alibaba Qwen`→`alibaba-qwen`, `GLM`→`zhipu`, `Kimi`/`Moonshot`→`moonshot`). The `figure` constraint from 2026-08-01 is holding; its promote block stays until the pre-correction counters age out of the 30-day window.

## Sources scanned this run

- `discovered_orgs.json` (last_updated 2026-08-01 → 2026-08-02)
- Today's source files (6): `news/`, `papers/`, `blogs/`, `jobs/`, `linkedin/`, `daily/` — all `2026/08/2026-08-02.md`. `github/` and `hackernews/` files exist for today but SKILL.md §2/§3 define exactly six source directories and their `src_type` mapping, so they were read for context only and never tallied (same call as every prior run — changing the source set mid-streak would silently reset every sustained-day gate).
- The `Feed-health notes` block at the end of `news/2026/08/2026-08-02.md` — read and verified against `sources.json` entry-by-entry before acting.
- Radar JSONs in window: 30 (`2026-07-01` → `2026-08-02`)
- `sources.json` snapshot before edit → `sources.json.vendor.bak` (3 changes applied: 1 promote-add, 1 deep-watch-promote, 1 url-repair)
