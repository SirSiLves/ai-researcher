# Vendor sweep — 2026-08-14

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

> **Single day, no re-run.** `discovered_orgs.json.last_updated` was **2026-08-13** and no `vendor_candidates/2026/08/2026-08-14.md` existed, so exactly one day was outstanding. The script asserts that **no org carried a `2026-08-14` counter before this run** and aborts otherwise — the assertion passed on all 1,785 pre-existing orgs, so counters were **accumulated once**, not reconciled. Six source files scanned.
>
> **`linkedin/` is a full stub and is NOT vendor silence.** Chrome was denied at the permission layer before `list_connected_browsers` could return, so all four LinkedIn layers returned zero **by construction**. The `linkedin_network_post` source type contributed nothing to any org this run. That matters mechanically: `|source_types| ≥ 3` is a promotion gate, so today's promote tier is *understated* for every org that normally scores there. It is not a signal about any vendor.
>
> **`daily/`'s `BRIEFING_START`/`BRIEFING_END` block was stripped before counting**, as were every collector's `Sources scanned` section and the news collector's `Priority/Enterprise vendor coverage` telemetry blocks (the latter for the editorial-vs-telemetry diagnostic only).

## 📋 What changed in sources.json today

**4 mutations applied.**

- **Added 1 promotion:**
  - **`swiss-ai-initiative`** — `blog_urls=["https://www.swiss-ai.org"]`. **37 mentions, 3 source types, 10 distinct days over 30d**, and it held `promote` for **2 consecutive days** (08-13 → 08-14), exactly meeting `min_consecutive_days_at_promote`. This is the org flagged as the sole pending candidate in yesterday's file, and it converted. The Apertus / Swiss AI Initiative entity (CHF 20m through 2028). **First sustained-promote application since `isomorphic-labs` on 08-11**, and it cleared the 3-source-type gate *despite* the LinkedIn layer contributing nothing.
- **Added 0 hot-event vendors.** There is **no funding event in today's corpus.** See the hot-event section below — the one strong-keyword paragraph is a price *rise*, and the run's one real transaction is deliberately held.
- **Removed 3 expired hot-event entries:** `manus`, `tencent`, `helsing` — all three carried `_expires_on: 2026-08-13`, flagged in yesterday's file as surviving by exactly one day because Step B tests `_expires_on < TODAY`. They expired on schedule.
- **Demoted 0 to deep-watch (67/30 cap, OVERFLOW regime).** The regime fired correctly (67 > 45 overflow threshold, effective silence dropped to 14 days, budget 5) and then found **zero candidates**. See defect 3 — this is the second consecutive run where the cap machinery runs and cannot converge.
- **Re-promoted 0 from deep-watch.**
- **Skipped 0 silent vendors** — `covered_silent` came back **empty for the fourth consecutive run**. That number is not trustworthy; see defect 3.

`enterprise_vendors`: **69 → 70 (add) → 67 (3 expiries)**, against a cap of **30**. `deep_watch_vendors`: 14, unchanged. Auto-added share: **59 of 67**; the 8 manual entries (`n8n`, `nvidia`, `redhat_ibm`, `salesforce`, `sap`, `servicenow`, `snowflake`, `workday_oracle`) are sacred and were not touched.

**TTLs expiring within 7 days:** `unitree` (2026-08-14 — survives today, expires tomorrow), `anaconda` + `kilo-code` (08-15), `prior-labs` (08-18), `fireworks-ai` (08-19), `amd` + `microsoft` (08-21). Seven more entries will drain from the list in the next week without any demotion firing — **TTL expiry, not the soft cap, is doing all of the actual cap reduction.**

## 🔥 Hot events — none, and one deliberate hold worth reading

**The corpus contains exactly one paragraph carrying a strong funding/M&A term this run**, in `news/` and restated in `daily/`:

> "DeepSeek simultaneously launched its V4 Pro flagship **and raised** API prices — some workloads up as much as 1,100%"

`raised` there is a **price increase, not a round.** The keyword allowlist was therefore empty and **no `hot_events` record was written for any org.** The only other keyword hit in the run is `billion` inside the Shieldstral item, which is a parameter/downloads magnitude.

**The run's one genuine corporate transaction is a closing, and it is held:**

> **`spacex`** — *"Cursor is now a part of SpaceX"* (first-party post `cursor.com/blog/joining-spacex`, 81 pts / 78 comments on HN). The **$60B all-stock Anysphere acquisition SpaceX announced on 2026-06-16 formally completed today.**

`spacex` still classifies `hot_event` legitimately — it is `uncovered`, has 232 lifetime mentions, and carries a `hot_events` row from 2026-07-23 that is still inside the 30-day window. It also has a `blog_url_hint`. Under the bare SKILL rules it would apply. **It is held anyway, and today the reason changed.**

For the previous 14 runs `spacex` was held as *stale* — a June event still classifying in August. Today the event is fresh, so that argument is gone. The hold now rests on **coverage gain**: both AI assets the transaction concerns are **already covered** — `cursor` and `xai` are both `enterprise_vendors` entries — and `spacex`'s only hint is `https://www.spacex.com/updates/`, a rocket-launch feed that is off-slice for this pipeline. Applying it would consume cap headroom on a list at **67/30** while adding **no AI coverage**.

This is the mirror image of yesterday's `decart` case. There, the acquirer was sacred so the coverage landed on the target. Here the target is already covered, so there is nowhere for the coverage to land. **Worth recording as a category: a major AI M&A event can produce zero coverage change and that can be the correct outcome.** `_added_reason` was not fabricated and no `hot_events` row was invented for a keyword that does not appear in the text.

Also held under the same "no fresh transaction this run" rule (21 others, all carrying stale in-window hot-event rows): `accenture`, `anyscale`, `blackstone`, `cxmt`, `fidelity`, `fractional-ai`, `georgia-power`, `ibm`, `oasis-security`, `permiso`, `recursive-superintelligence`, `taalas`, `vera-rubin`, `visa`.

## 🐞 Known defects exercised this run

### 1. `blog_url_hint` blocker — 119 orgs blocked, and it cost 0 promotions today (for once)

**125 orgs classified `promote` today. Exactly 4 of them have a `blog_url_hint` at all** — and one of those four (`swiss-ai-initiative`) is precisely the one that applied. The other **119** are rejected **solely** by Step C.5 because `blog_url_hint` is empty and nothing in the pipeline ever populates it.

**This is the first run in weeks where the blocker did not zero out the promotion count**, because the one org that mattered happened to carry a hint. That is luck, not a fix.

The three hint-carrying promote-tier orgs that did *not* apply: `figure` (over-matching-alias block, carried from 08-01), `dbs-bank` and `swiss-re` (oscillation guard, below).

**Backlog composition — the ordering constraint still holds.** Of the 119 blocked slugs, **74 are already on the JUNK list** (concepts, people, places, media, benchmarks) and of the remaining 45 only **~23 are plausible vendor entries**. Fixing the hint blocker without first pruning the tally would auto-promote ~96 non-companies into `enterprise_vendors` on the next run. **That ordering constraint has now held for five consecutive sweeps and remains the single highest-value pipeline repair outstanding.**

Plausible companies stuck at 14+ consecutive days at `promote` (the `classification_history` cap is 14, so "14" means *at least* 14):

| slug | consec. days | mentions / 30d |
|---|---|---|
| `novartis` | 14+ | 95 |
| `databricks` | 14+ | 77 |
| `workday` | 14+ | 60 |
| `alibaba` | 14+ | 55 |
| `box` | 14+ | 32 |
| `huawei` | 14+ | — |
| `bytedance` | 14+ | — |
| `roche`, `siemens`, `bcg`, `adobe`, `oecd`, `stanford-hai`, `vercel`, `vllm`, `ibm-research`, `exeon-analytics`, `giotto`, `artefact`, `future-of-life-institute`, `pro-informatik`, `ostp`, `chase` | — | — |

`oracle` remains excluded by the **duplicate-coverage guard** added 08-13 (already covered by the manual `workday_oracle` entry), not by the missing hint. `the runtime` remains rejected on invalid slug format — a tally leak with a space in it that has now survived several runs.

**No hint was backfilled this run.** The corpus-grounded diagnostic searched every URL in the six scanned files for a first-party host matching a hint-blocked slug and returned **nothing usable** — only substring artefacts (`ec`, `hu`, `li`) and media domains. There was one near-miss worth noting: `jobs/` contains `https://jobs.apple.com/...`, a genuine first-party Apple host and the first employer careers page this pipeline has ever read — but `apple` is already an `enterprise_vendors` entry with a working hint, so it unblocks nothing. **No hint is ever invented from memory.**

**New this run:** `defender` reached `promote` at 1/2 consecutive days. It is a **Microsoft product**, not a company, and has been added to `BRAND_TO_PARENT` (→ `microsoft`) and JUNK so it cannot become a phantom pending candidate. It was hint-blocked regardless.

### 2. Promote/demote oscillation — 3 flagged, 2 actively held

Demotion tests `last_seen` on a 14-day window while promotion tests a 30-day mention window, so a vendor demoted for silence re-qualifies almost immediately.

| slug | demoted on | days since | tier today | mentions / 30d | disposition |
|---|---|---|---|---|---|
| `swiss-re` | 2026-08-06 | 8 | `promote` | 48 across 10 days, 3 src types | **HELD** by the oscillation guard |
| `dbs-bank` | 2026-08-08 | 6 | `promote` | 11 across 5 days, **5 src types** | **HELD** by the oscillation guard |
| `accenture` | 2026-08-01 | 13 | `hot_event` | 9 across 4 days | held anyway — stale hot-event row, no fresh transaction |

**No org both demoted and re-promoted on the same day**, and no flip was applied. But the countdown is unchanged and now short: **on 2026-08-20 `swiss-re` ages out of the 14-day guard and will re-promote; `dbs-bank` follows on 2026-08-22.** Both are gaining, not losing, mentions — `swiss-re` is at 48/30d against 50 yesterday and `dbs-bank` now scores across **five** source types on a day when one source type was structurally dead. The guard is a patch in the sweep script, not a fix. **The real repair is aligning `deep_watch_demote.overflow_silence_days` (14) with `promotion_thresholds.window_days` (28/30) in `vendor_sweep_config`.**

`netflix`, the trap created by yesterday's run, did **not** re-qualify today (still short on distinct days) — but it remains inside its guard window until 2026-08-27.

### 3. Dead-source blindspot — the cap machinery ran and found nothing

The silent-vendor check reads **mention counts, not feed health.** Today the consequence is unusually crisp:

- `covered_silent` = **0** for the fourth consecutive run.
- The deep-watch sweep entered **OVERFLOW** regime (67 > 45), dropped its silence bar to **14 days**, had a budget of **5** — and found **zero candidates**. Every one of the 59 auto-added entries has a `last_seen` inside 14 days, purely on third-party mentions. The only entry that *was* silent long enough (`norm-ai`, 20 days) is TTL-shielded until 08-25.
- **All three expiries this run came from TTL, none from the cap.** `enterprise_vendors` fell 70 → 67 entirely because three clocks ran out. The soft cap has contributed **one** demotion in the last six runs.

**Vendors whose own feed produced nothing this run, separated from their mention counts:**

| vendor | list | own feed | mentions today | editorial | assessment |
|---|---|---|---|---|---|
| `meta` | priority (sacred) | **0 items** | 5 | 3 | **Fifth consecutive run at zero.** "Search surfaced only historical LlamaCon/Llama-3 posts." 2 of its 5 mentions are the collector's own telemetry line. Reads `covered_healthy` on a dark feed. |
| `n8n` | enterprise (MANUAL) | **0 items** | 1 | **0** | **Eighth consecutive run at zero, and TELEMETRY-ONLY** — its entire mention count is the collector's own `n8n: 0 items` line. Zero editorial presence anywhere. Strongest candidate in the file for a URL repair or a considered removal. |
| `snowflake` | enterprise (MANUAL) | 0 items | 1 | **0** | **Not a feed failure** — "Cortex AI Gateway already captured in the 2026-08-13 file, deliberately not re-run." A de-duplication decision. **But it is indistinguishable from `n8n` in the metrics**: both read as 1 mention / 0 editorial / telemetry-only. That is the blindspot stated as precisely as it can be. |
| `workday_oracle` | enterprise (MANUAL) | 1 item | 1 | **0** | Oracle appears **only** in the $1.5T commitments analysis. **No Workday item for a fourth consecutive run.** Telemetry-only. |
| `nvidia` | enterprise (MANUAL) | 1 item | 19 | 18 | The "item" is a *mention*, not an announcement — "no fresh newsroom item". **Fifth consecutive run without a first-party post** while scoring 19 mentions (22 open Swiss reqs, the OSS router `NVIDIA-NeMo/Switchyard`, the Mistral alliance line). |
| `sap` | enterprise (MANUAL) | 1 item | 9 | 7 | The item — SAP's API Policy v4/2026 banning external AI agents from calling SAP APIs — is materially the most consequential SAP news in weeks, and **the vendor's own feed did not carry it**; recovered via Techzine. |
| `deepseek` | priority (sacred) | 1 item | 19 | 16 | **Yesterday's diagnosis is partly reversed.** 08-13 concluded DeepSeek had "no first-party feed to poll". Today `daily/` cites `api-docs.deepseek.com/news/news260813/` as a live permalink for the pricing change. The domain **does** publish — the collector cannot reach the *index* under the provenance rule. This is a fetch-path failure, not an absent feed. |
| `mistral` | priority (sacred) | **2 items, fetched directly** | 34 | 29 | **RECOVERED — and yesterday's diagnosis is falsified.** 08-13 called this "a dying index". Two first-party canonicals fetched successfully today and Mistral is the third-most-mentioned org in the corpus. **It was a quiet vendor, not a dead feed.** Recording the correction. |

**Non-vendor sources that produced nothing:** all four LinkedIn layers (full stub, permission denied); **17** configured `long_form_blogs` — the collector calls it "a thin day" and backfilled two archive items to fill the section, and again reports `cognition.ai/blog` (an `enterprise_vendors` entry's own configured URL) as carrying nothing since 28 Jul; all **17** `medium.com/tag/*` feeds; arXiv RSS for all six categories; `papers.cool`; HF Papers trending (new failure mode — "response exceeded the fetch size limit"); `wellfound` (sixth dry run); `ai-jobs-switzerland.ch` (200 response with an empty body).

**Two stale-cache findings on the jobs side, both worth carrying forward:** `datacareer.ch` **fetched successfully** and is still dead — 43 listings advertised as live whose newest posting is dated **2026-03-07**, which the collector correctly calls "the jobs-side twin of the dead-vendor-feed problem." And the national `jobs.ch` URL returned a page **byte-identical to yesterday's**, while the location-scoped URL rendered live. **A 200-OK fetch is not feed health** — the same lesson as the GitHub trending fetch-proxy.

### 4. Velocity → hot_event path — confirmed dead code, sixth consecutive run

Measured across all 1,787 orgs: **max `velocity_ratio` = 4.00**, threshold **5.0**, **0 orgs surging**. 56 orgs sit at `accelerating` and most are pinned at exactly 4.00 — the arithmetic ceiling of `v7 / max(v28_avg, 1.0)` when the baseline is below 1.0/wk, not a distribution. No hot event has ever been raised by the velocity path and none can be.

### 5. Alias under-counting — two confirmed misses on today's biggest stories

The detect-only regression diagnostic found **13 orgs** whose slug-with-spaces appears in the corpus but whose alias list does not match it. Two are load-bearing today:

- **`writer`** — the subject of one of the day's three largest items (Palmyra X6 + the harness result that cut agent cost 41% across every model tested). Its alias list is `["Writer.com"]` only, so the **9 mentions in `news/` and 6 in `daily/` scored ZERO.** `writer` remains `dormant` at 12 lifetime mentions and `last_seen: 2026-05-28`. It is `uncovered` and **has a valid hint** (`https://writer.com/blog/`) — so this alias gap alone is keeping a genuinely promotable vendor invisible. Not auto-fixed: adding a bare `"Writer"` alias would over-match the English word, which is exactly the `figure` failure mode. **Needs a deliberate alias like `"Writer's"` + `"Palmyra"`, decided by hand.**
- **`goldman-sachs`** — "Goldman Sachs" appears in today's `news/` and `daily/` ($1.5T lease-commitment analysis) and scored **zero**; alias list is `["goldman-sachs"]`.

Others flagged: `european-commission`, `google-deepmind`, `the-information`, `the-verge`, `deepseek-v4-pro`, `claude-cowork`, plus concept slugs. **No alias was auto-added from this diagnostic** — it is detect-only by design, because a bad alias is far more expensive than a missed mention.

## ⏳ Pending — needs one more day at promote tier (1)

Of 125 promote-tier orgs, four carry a `blog_url_hint`; one applied and three are held (`figure` alias block, `dbs-bank` + `swiss-re` oscillation guard). Nothing is one day away from applying on hint-carrying merit.

- **`defender`** — days at promote **1/2**, but it is a **Microsoft product slug**, hint-blocked, and has been routed to `BRAND_TO_PARENT` this run. It will not apply and should not.

**Nothing will auto-apply tomorrow on the sustained-promote path unless a hint is backfilled by hand.**

## ⚡ Velocity signals (56 accelerating, 1,400 cooling)

Informational only; see defect 4 for why nothing here can escalate.

**Accelerating, ranked by absolute 7-day volume:**

- **`macquarie`** — 4.00× (36/7d vs 9.0/wk) · Theseus data-centre JV still compounding
- **`swisscom`** — 3.33× (30/7d vs 9.0/wk) · the only large-volume org *not* at the ceiling
- **`peax`** — 4.00× (22/7d) · Swiss vertical app
- **`geberit`** — 4.00× (19/7d) · the sanitary-products manufacturer with a named "AI Competence Center" hiring a forward-deployed engineer
- **`omnilex`** — 4.00× (19/7d) · Swiss legal-tech ETH spinout; "Private AI Runtime & Infrastructure Engineer (Local Data Residency)" — the sovereignty thesis as headcount
- **`lenovo`** — 3.30× (19/7d)
- **`kaiko-ai`** — 4.00× (17/7d) · #3 engineering employer on the Swiss board
- **`decart`** — 4.00× (15/7d) · applied to hot_event on 08-13
- **`gic`** 3.50× (14/7d) · **`sunrise`** 4.00× (14/7d, currently deep-watch) · **`theseus-infrastructure`** 4.00× (14/7d) · **`harmattan-ai`** 4.00× (14/7d, **admitted today**)
- **`taalas`** 4.00× · **`uber`** 3.69× (deep-watch) · **`gravis-robotics`** 4.00× · **`brookfield`** 4.00×

**Cooling with residual activity (not a demote trigger):** `kimi` 0.06×, `moonshot` 0.10×, `germany` 0.11×, `lambert` 0.14×, `mythos` 0.14×, `genesis` 0.21×, `ethz` 0.23×, `palantir` 0.25×, `swiss-ai-initiative` 0.26× — *note the promotion applied today is on a cooling org; the 30-day window and the 7-day window disagree, which is the same two-window disagreement that drives defect 2*.

## 👀 Watch list (185, showing 24)

- **`a2a-protocol`** — 7 mentions / 5 src types · 1.60× (protocol slug; pruning candidate)
- **`agent-runtime`**, **`agent-governance-toolkit`** — 7 each · concept slugs; pruning candidates
- **`black-forest-labs`** — 7 / 2 · frontier-lab
- **`bundeskartellamt`**, **`bundesnetzagentur`** — 7 / 2 each · German regulators
- **`kong`** — 7 / 5 · API/agent gateway
- **`plaid`** — 7 / 4 · fintech infrastructure
- **`sailpoint`** — 7 / 2 · 2.29× · identity governance
- **`lm-studio`** — 7 / 3 · local inference
- **`jetbrains`** — 7 / 2 · **`gemini-enterprise`** 7 / 3 · **`superblocks`** 7 / 2 · **`bjak`** 7 / 1
- **`neural-concept`** — 7 / 2 · 4.00× · Swiss EPFL simulation-ML spinout
- **`efg`** 7 / 1 · 4.00× · **`albedis`** 6 / 2 · 4.00× · **`alpian`** 6 / 1 · 4.00× — the Swiss cohort admitted over the last four runs is holding
- Media/person slugs still sitting in the watch tier and due for pruning: `gergely-orosz`, `matt-pocock`, `pragmatic-engineer`, `securityweek`, `mind`, `toronto`, `work-louder`

## ⚠️ Silent covered vendors (0)

Empty for the fourth consecutive run. **Treat this as a broken metric, not a clean bill of health** — see defect 3. The check reads mention counts, and third-party mentions keep `last_seen` fresh for every vendor on the list including the three whose own feeds produced literally nothing.

## 🆕 New orgs admitted to the tally (2)

Bar (precedent 2026-08-02, re-applied every run): named legal entity, **≥ 2 genuine in-prose mentions in a single file**; URL slugs, roll-up enumerations, telemetry lines and markdown link-text repeats of the same headline do not count; plus the journalist test.

- **`harmattan-ai`** — 14 mentions in `jobs/` + `daily/`. Lausanne + Zürich dual-site AI company running **four concurrent Swiss engineering reqs**, the second-largest independent footprint on the Swiss board after Mistral, with foundation-model **distillation as a product line**. The jobs collector's own framing: "the largest employer this file has been missing." No `blog_url_hint` — not invented.
- **`ergon`** — 4 mentions in `jobs/`. Ergon Informatik AG, the **single largest employer on swissdevjobs' ML board at 7 openings**, and the collector's point is that the largest ML-adjacent hiring block on that source is a Zürich security/platform house, not an AI company.

**Held under bar** (recorded, not admitted): `pony-ai`, `c3-ai`, `usda` (one editorial item each — the second apparent hit is headline link-text or the collector's own telemetry line); `genius-sports`, `orderfox`, `layered`, `dialectic`, `jk-advisory`, `infoconsulting` (clear the mention bar but arrive **only** via `datacareer.ch`, which this run's own collector certifies as a five-month-stale index recorded at *archive confidence* — admitting them would date-stamp a March listing as an August mention); `amadeus`, `caisse-des-depots` (Mistral ECU coalition enumeration — `asml`, `capgemini`, `cma-cgm` are already tracked and simply score their list mention); `smic`, `applied-materials`, `arc-institute`, `matraix`, `meituan`, `tsinghua`, `langfuse`, `milvus` (one mention each); `ipt` (3-char abbreviation, would over-match); `interdiscount`, `axa`, `digitec`, `sunrise`, `profidata`, `academic-work` (named **only** inside the jobs.ch signal-to-noise caveat as examples of *false positives* — counting them would invert the finding).

**Aliases added (corpus-grounded only):** `spacex` += "Anysphere"; `zhipu` += "GLM-5.3". Twenty of the ~22 "new employers" the jobs collector reports were already in the tally and simply scored today's mentions.

**Radar breadth:** 941 `breadth_orgs_7d` slugs across 30 radar JSONs; **527 rejected** (already tracked, brand→parent, JUNK, suffix-rejected, or no corroboration in this run's source files). Seven new brand→parent entries added: `defender`→microsoft, `anysphere`→cursor, `glm-5-3`→zhipu, `palmyra`→writer, `rambler`→google_deepmind, `deepsec`→vercel, `nexus`→pinecone.

## Tally summary

- Orgs tracked: **1,787** (+2)
- Covered (priority): **6**
- Covered (enterprise): **67** — incl. **59** auto-added, **8** manual (sacred)
- Covered (deep-watch): **14**
- Covered (informal URL list): 0
- Uncovered: **1,699**
- Orgs whose counters moved today: **182**
- New orgs added to tally today: **2**
- Hot events recorded today: **0**

## Sources scanned this run

- `discovered_orgs.json` (last_updated 2026-08-13 → 2026-08-14)
- Today's source files (6): `blogs/2026/08/2026-08-14.md`, `daily/2026/08/2026-08-14.md`, `jobs/2026/08/2026-08-14.md`, `linkedin/2026/08/2026-08-14.md` **(stub)**, `news/2026/08/2026-08-14.md`, `papers/2026/08/2026-08-14.md`
- Radar JSONs in window: **30**
- `sources.json` snapshot before edit → `sources.json.vendor.bak` (**4** changes applied)

_Provenance: `WebFetch` remained unavailable to every collector; all fetching went through `mcp__workspace__web_fetch` under per-URL search-first discipline. The news collector again could not fetch a single configured index page — all vendor blogs, all 13 `tech_news_sites`, both `swiss_sources` and all 9 `governance_sources` fell back to search — yet still recovered canonical first-party permalinks for openai, anthropic, google, mistral (×2), ibm and servicenow. The jobs collector read an **employer careers page directly for the first time** (`jobs.apple.com`), which is what surfaced nine Apple Zürich AI reqs invisible to all four configured Swiss boards._

## Out-of-scope list caps (reported, not acted on)

- `news_collector.web_search_queries`: **60 / 60** — **FROZEN.** Demotion fires only on `len > cap`, so no entry can be evicted and therefore no new query can be admitted. Owned by `ai-keyword-sweep`.
- `github_collector.watched_repos`: **163 / 80** — over cap, so `len > cap` does fire; at `max_demotions_per_run=5` the walk-down needs ~17 more runs. Owned by `ai-github-sweep`.
