# Vendor sweep — 2026-08-13

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

> **TWO-DAY WINDOW — this run catches up 2026-08-12.** The pipeline produced no `daily/` output for 08-12 and `discovered_orgs.json.last_updated` was **2026-08-11**, so the 08-12 collector dumps that *do* exist (`news/`, `papers/`, `blogs/`, `jobs/`) were never tallied. They are counted here, **attributed to their own file date** so `mentions_by_date` stays honest. `linkedin/` and `daily/` have no 08-12 file and contribute nothing for that day. **Ten source files were scanned, covering two days.** Every window described below ("last 7 days", "silent N days") is therefore measured across a two-day head, not one.
>
> **No same-day re-run.** No org carried an 08-12 or 08-13 counter before this run — the script asserts this and aborts otherwise. Counters were **accumulated once**, not reconciled, because there was nothing to reconcile against.

## 📋 What changed in sources.json today

**3 mutations applied.**

- **Added 0 promotions.** (See the two defect sections below — this is not because nothing qualified.)
- **Added 2 hot-event vendors:**
  - **`decart`** — `blog_urls=["https://www.decart.ai"]`, expires **2026-09-12**. Trigger: `acquisition` / `to acquire` / `raised`. Anthropic is reported in talks to buy the Israeli startup **Decart AI for about $6 billion** — real-time generative video, world models for simulated environments, GPU-optimisation. Talks are early-stage and could collapse; if completed the team folds into Anthropic's inference organisation. Decart had previously raised $300M with NVIDIA among its investors. 15 mentions in-window across `tech_news` + `daily_synthesis`. **Note the shape of this signal: the acquirer is a `priority_vendors` entry (sacred, untouched), so the coverage gain lands entirely on the target.**
  - **`lovable`** — `blog_urls=["https://lovable.dev/blog"]`, expires **2026-09-12**. Trigger: `Series C` / `valuation`. **$400M Series C at a confirmed $13.3B post-money**, led by Menlo Ventures and the Scaleup Europe Fund with 12+ additional investors, announced 12 Aug. A *closed, confirmed* round rather than a talks-stage report. 10 mentions in-window.
- **Removed 0 expired hot-event entries.** Three TTLs (`manus`, `tencent`, `helsing`) read **`_expires_on: 2026-08-13` — exactly today**. Step B tests `_expires_on < TODAY`, so they survive this run and expire tomorrow. Worth knowing: had the pipeline not skipped 08-12, nothing would change; but a *second* skipped day would leave three dead TTL entries occupying cap headroom.
- **Demoted 1 to deep-watch (70/30 cap, OVERFLOW regime):**
  - **`netflix`** — `last_seen 2026-07-30`, silent **14** days, exactly at the overflow bar. Moved to `deep_watch_vendors` keeping `blog_urls`, `fallback_search` and `_added_reason`. **This is the first deep-watch demotion to fire since 2026-08-08**, and only one candidate existed — see defect 3.
- **Re-promoted 0 from deep-watch.**
- **Skipped 0 silent vendors** — `covered_silent` came back **empty** for the third consecutive run. That number is not trustworthy; see defect 3.

`enterprise_vendors`: **68 → 70 (adds) → 69 (post-demotion)**, against a cap of **30**. `deep_watch_vendors`: 13 → 14.

## 🐞 Known defects exercised this run

### 1. `blog_url_hint` blocker — 116 orgs blocked, 0 promotions possible

**127 orgs classified `promote` today. Exactly 4 of them have a `blog_url_hint` at all.** The other **116** (the remaining 7 are blocked for other reasons, below) are rejected **solely** by Step C.5 because `blog_url_hint` is empty, and nothing in the pipeline ever populates that field. Every one of the 116 already clears the 2-consecutive-day sustained gate. **This single missing field is the entire reason today's promotion count is zero.**

Top blocked orgs that are **plausible vendor entries**, ranked by consecutive days at `promote` (the `classification_history` cap is 14, so "14" means *at least* 14 — these have been stuck for weeks):

| slug | consec. days at promote | mentions / 30d |
|---|---|---|
| `novartis` | 14+ | 97 |
| `databricks` | 14+ | 77 |
| `workday` | 14+ | 59 |
| `box` | 14+ | 32 |
| `cscs` | 14+ | 23 |
| `bytedance` | 14+ | 16 |
| `crewai` | 14+ | 13 |
| `alibaba` | 13 | 50 |
| `vllm` | 12 | 51 |
| `gartner` | 12 | 10 |
| `modal` | 11 | 25 |
| `huawei` | 8 | 28 |
| `ey` | 8 | 16 |
| `openrouter` | 8 | 11 |
| `kimi` | 7 | 299 |
| `amazon` | 7 | 124 |

**`oracle` has left this list — for a worse reason.** It is now blocked by an explicit **duplicate-coverage guard** added this run rather than by the missing hint. Oracle is *already* covered by the **manual** `enterprise_vendors` entry `workday_oracle`; promoting the bare `oracle` slug would create a second, competing Oracle entry. It had been reported as "blocked at 14+ consecutive runs" on every prior sweep, which overstated the coverage gap by one — the coverage exists, the *slug* is the duplicate. **This matters for the fix: draining the hint backlog must not resurrect it.**

**The backlog is still roughly half noise.** Of the 116 blocked slugs, only **53** are plausible companies. The other **63** are concept, brand, person, place and media slugs the tally should never have admitted — `linkedin` (715 mentions), `eu` (436), `switzerland` (369), `eth` (244), `time` (220), `gemini` (202), `cern` (201), `gpt-5` (174), `eu-ai-act` (159), `willison` and `simonwillison` as two separate slugs, `commission`, `agent-memory`, `prompt-injection`. **Fixing the hint blocker without first pruning the tally would auto-promote ~63 non-companies into `enterprise_vendors` on the next run.** That ordering constraint has now held for four consecutive sweeps and is the single highest-value pipeline repair outstanding.

**No hint was backfilled this run.** A corpus-grounded diagnostic searched every URL in the ten scanned files for a first-party host matching a blocked slug and returned **nothing usable** — the only matches were media domains (`axios.com`, `techcrunch.com`) and substring artefacts (`li`, `hu`, `ec`, `ode`). No hint is ever invented from memory.

Blocked for reasons *other* than the missing hint: `figure` (over-matching alias, carried from 08-01), `oracle` (duplicate-coverage guard, new), `the runtime` (invalid slug format — a pre-existing tally leak with a space in it), `dbs-bank` and `swiss-re` (oscillation guard, below).

### 2. Promote/demote oscillation — 3 orgs flagged, 2 actively held, 1 newly created

Demotion tests `last_seen` on a 14-day window while promotion tests a 30-day mention window, so a vendor demoted for silence can re-qualify almost immediately. Three deep-watch entries demoted inside the last 14 days re-classified into an applying tier today:

| slug | demoted on | days since | tier today | mentions / 30d | disposition |
|---|---|---|---|---|---|
| `swiss-re` | 2026-08-06 | 7 | `promote` | 50 across 11 days, 3 src types | **HELD** by the oscillation guard |
| `dbs-bank` | 2026-08-08 | 5 | `promote` | 11 across 5 days, 5 src types | **HELD** by the oscillation guard |
| `accenture` | 2026-08-01 | 12 | `hot_event` | 8 | held anyway — stale hot-event record, no fresh transaction this run |

**No org both demoted and re-promoted on the same day.** But the guard is a 14-day patch in the sweep script, not a fix — on **2026-08-20** `swiss-re` ages out and will re-promote, and on 2026-08-22 `dbs-bank` follows. The real fix is aligning the two windows in `vendor_sweep_config`.

**And this run created the next instance of the same trap.** `netflix` was deep-watch-demoted for **14 days of silence** while carrying **14 mentions in the 30-day window** across 4 source types. It misses `promote` today only on distinct-days (3, needs 4). One more mention on a fresh day and it re-qualifies — from inside the guard window. Same disagreement between two windows reading the same tally that produced `swiss-re`.

### 3. Dead-source blindspot — and a new instance where the two lists directly contradict each other

The silent-vendor check reads **mention counts**, not **feed health**. `covered_silent` is **0** again, and the deep-watch sweep found **exactly one** candidate against a list at **70/30** — every other auto-added entry has a `last_seen` inside 14 days purely on third-party mentions. **The soft cap still cannot converge on its own.**

**The sharpest instance this run is an internal contradiction between two collectors:**

> `cognition` is an `enterprise_vendors` entry whose `blog_urls` point at **`cognition.ai/blog`**. Today's blogs collector lists that exact URL under **"Silent / no fresh indexed content (failed)"** — *"no indexed posts returned"*. `cognition` nevertheless classifies **`covered_healthy`** on 223 lifetime mentions, and is simultaneously the subject of the run's third-largest transaction (**$40B+ valuation talks, ~$1B ARR**). The pipeline is reporting a vendor as healthy, on a feed it has itself certified as returning nothing, in the same 24 hours that vendor made news.

The blogs collector also certified **`blog.langchain.dev`** (nothing newer than early 2026) and **`llamaindex.ai/blog`** (latest indexed post **2026-03-31**) as dead. All three are five months stale.

**Feed-health suspects — vendors that read healthy but whose own feeds produced nothing:**

| vendor | list | 08-12 | 08-13 | assessment |
|---|---|---|---|---|
| `meta` | priority (sacred) | 0 | 0 | **Fourth consecutive run at zero.** Configured `ai.meta.com/blog/` still does not carry what `research.meta.ai/blog/` does. |
| `mistral` | priority (sacred) | 0 | 0 | **Degrading.** On 08-11 the search reached back to early August ("genuinely quiet"); today it reaches back to **March–May**. That is a dying index, not a quiet vendor. |
| `nvidia` | enterprise (MANUAL) | 0 | 0 | Third/fourth consecutive zero, while scoring **30 mentions** this run (L&T B300 order, 22 Swiss reqs, the Decart investor line). Reads `covered_healthy` on 1,561 lifetime mentions with a dark feed. |
| `redhat_ibm` | enterprise (MANUAL) | 0 | 0 | Both days zero; the 08-11 partial recovery did not hold. OR-joined `fallback_search` still unsplit. |
| `sap` | enterprise (MANUAL) | 1 | 0 | The one item arrived **through Snowflake's** press release — "no standalone news.sap.com post in window". No first-party item in three runs. |
| `servicenow` | enterprise (MANUAL) | 1 | 0 | Path is healthy (full newsroom permalink on 08-12). **Not** a dead feed. |
| `n8n` | enterprise (MANUAL) | 0 | 0 | **Seventh consecutive run at zero**, both first-party domains unreachable, and absent from the editorial corpus too. Both dead-feed *and* genuinely quiet — the strongest candidate in the file for a manual URL repair or a considered removal. |
| `workday_oracle` | enterprise (MANUAL) | 2 | 0 | Both 08-12 items were Oracle, trade-press or dated 30 Jul. The file states outright: **"No Workday item."** Half this paired entry has been silent for its entire observable history. |
| `deepseek` | priority (sacred) | 0 | 1 (aggregator) | **Corroborated as a genuine dead source, not a collector failure.** Simon Willison reports DeepSeek shipped **V4 Pro 0813 API-only with no announcement page** — benchmarks reached the public via a WeChat group → a deleted Reddit post → an ASCII table on HN. There is no first-party feed to poll for this release. |
| `anthropic` | priority (sacred) | 1 | 2 | Every item recovered from search snippets or aggregators. The 08-12 **global watermarking** announcement produced **no vendor permalink on either configured domain** after two explicit retry searches; the ~$6bn Decart approach was learned from a roundup citing Bloomberg. |

**Telemetry-only vendors** (their entire mention count this run is the news collector's own "0 items" self-report, with zero editorial hits): **`n8n` 2/0, `redhat_ibm` 2/0, `workday_oracle` 2/0.** Every one of these has `last_seen` refreshed by the pipeline reporting on itself.

Also silent this run: **12** configured `long_form_blogs`, all **17** `medium.com/tag/*` feeds, arXiv RSS for all six categories, `papers.cool`, LinkedIn hashtag + home feed + saved jobs (**fifth** consecutive run at zero by construction), and on the jobs side `wellfound` (fifth dry run) and `ai-jobs-switzerland.ch` (**ten** consecutive failures, with the collector explicitly recommending it be marked dead in `sources.json`).

### 4. Velocity → hot_event path — confirmed dead code again

Measured across all 1,785 orgs: **max `velocity_ratio` = 4.00**, threshold **5.0**, **0 orgs surging**. 58 orgs sit at `accelerating` and the great majority are pinned at exactly 4.00 — the arithmetic ceiling of `v7 / max(v28_avg, 1.0)`, not a distribution. Both of today's hot events came through the keyword path. No hot event has ever been raised by the velocity path and none can be.

## ⏳ Pending — needs one more day at promote tier (1)

Of 127 promote-tier orgs, only four carry a `blog_url_hint` at all, and three of those are held (`figure` alias block, `dbs-bank` + `swiss-re` oscillation guard). That leaves exactly one org that will auto-apply tomorrow simply by holding tier:

- **`swiss-ai-initiative`** — hint `https://www.swiss-ai.org`, days at promote **1/2**. The Apertus / Swiss AI Initiative entity (CHF 20m funding through 2028, surfaced via the swissinfo Swiss-AI item on 08-12). If it holds `promote` tomorrow it becomes the first sustained-promote application since `isomorphic-labs` on 08-11.

## ⚡ Velocity signals (58 accelerating, 1,379 cooling)

Informational only; see defect 4 for why nothing here can escalate.

**Accelerating — all at the 4.00 ceiling, ranked by absolute 7-day volume:**

- **`macquarie`** — 4.00× (36/7d vs 9.0/wk) · the Theseus data-centre JV continuing to compound
- **`geberit`** — 4.00× (17/7d vs 4.25/wk) · Swiss industrial "enablement office" pattern
- **`decart`** — 4.00× (15/7d vs 3.75/wk) · **applied to hot_event this run**
- **`lovable`** — 4.00× (10/7d vs 2.50/wk) · **applied to hot_event this run**
- **`imtf`** — 4.00× (9/7d) · Swiss regtech/AML
- **`infomaniak`** — 4.00× (8/7d) · Swiss sovereign cloud
- **`legora`** — 4.00× (8/7d) · legal AI, >$10B raise sought — **blocked, no hint**
- **`efg`**, **`alpian`**, **`albedis`**, **`kanton-luzern`**, **`meteoschweiz`** — 6–7/7d each · the Swiss cohort admitted over the last three runs is holding

**Cooling with residual activity (not a demote trigger):** `lambert` 0.11×, `kimi` 0.12×, `tsmc` 0.15×, `interconnects` 0.16×, `moonshot` 0.18×, `zhipu` 0.18×, `axios` 0.24×, `amazon` 0.29×, `slack` 0.29×, `thinking-machines` 0.30×, `claude-code` 0.32×, `google-deepmind` 0.33×.

## 👀 Watch list (180, showing 20)

- **`a2a-protocol`** — 7 mentions / 5 src types · 2.29× (protocol slug; pruning candidate)
- **`black-forest-labs`** — 7 / 2 · frontier-lab
- **`bundeskartellamt`**, **`bundesnetzagentur`** — 7 / 2 each · German regulators
- **`kong`** — 7 / 5 · API/agent gateway
- **`plaid`** — 7 / 4 · fintech infrastructure
- **`sailpoint`** — 7 / 2 · 4.00× · identity governance (named in the Snowflake Cortex AI Gateway partner list)
- **`reprisk`** — 7 / 2 · 4.00× · Swiss ESG data
- **`lm-studio`** — 7 / 3 · local inference
- **`jetbrains`** — 7 / 2
- **`superblocks`** — 7 / 2 · app-generation
- **`work-louder`** — 7 / 2 · hardware
- **`securityweek`**, **`bjak`** — 7 each
- **`efg`**, **`alpian`** — 6–7 · Swiss private banking, both accelerating
- **`albedis`** — 6 / 2 · 4.00× · **recruiter** — see the held-under-bar note below
- **`gergely-orosz`**, **`pragmatic-engineer`**, **`matt-pocock`** — 7 / 1–2 each (individual authors; tally pollution)
- **`agent-governance-toolkit`**, **`agent-runtime`**, **`mind`**, **`toronto`** — 7 each (concept/place slugs; pruning candidates)

Roughly a third of the watch list is still not a company. Same root cause as the blocked-promotions inflation.

## ⚠️ Silent covered vendors (0)

None flagged. **See defect 3 — that number is not trustworthy.** Nine vendor feeds produced zero or telemetry-only items across the two days and every one of them reads `covered_healthy` on third-party mentions.

## 🆕 New orgs admitted to the tally (8)

Bar (carried unchanged from 08-02): named legal entity, ≥2 **genuine in-prose** mentions in a **single** file (roll-up enumerations, URL slugs and markdown link-text repeats do not count), plus the journalist test.

**From `news/2026-08-13` — corporate-transaction subjects:**
- **`vantage-data-centers`** — 3 in `news/`, 1 in `daily/`. The Silver Lake / DigitalBridge-backed hyperscale operator weighing an **IPO at roughly $100B** (~$10B raise), with a sale also on the table; ~$11B raised since late 2023, involved in the Wisconsin campus tied to the OpenAI/Oracle Stargate buildout. Alias restricted to the full `Vantage Data Centers` — a bare `Vantage` alias would match the English noun (the `figure` lesson). **A $100B pure-play data-centre listing is the biggest single number in the corpus**, but as a same-day arrival it has no `blog_url_hint` and cannot reach hot_event on its first day.
- **`larsen-toubro`** — 4 (`L&T`) + 1 (`Larsen & Toubro`) in `news/`. Won a **~$1.57B (₹10,000–15,000 crore)** contract to deploy ~10,000 NVIDIA B300 GPUs at a Chennai campus for Together AI — India's largest single-cluster AI installation.
- **`blacksmith`** — 2 in `news/`, 2 in `daily/`. CI/test infrastructure; valuation up **10× to $550M** as agent-written code volume reprices software validation.

**From `jobs/2026-08-13` — the Zürich robotics/embodied-AI cluster the collector calls the largest single vertical on the board:**
- **`gravis-robotics`** — 4 in `jobs/`. ETH spin-out, autonomous construction machinery; two same-day reqs including a lead seat.
- **`mimic-robotics`** — 4 in `jobs/`. ETH robotic-hands spin-out. Alias restricted to `Mimic Robotics` — a bare `Mimic` alias would match the English verb.
- **`neural-concept`** — 2 in `jobs/`. EPFL spin-out, deep-learning-accelerated engineering simulation; a *foundation-models*-tagged req scoped to EV powertrain design, the strongest Lausanne find outside Mistral.
- **`voliro`** — 4 in `jobs/`. ETH spin-out, inspection drones; an LLM-tagged cloud engineering seat.

**From `jobs/2026-08-12` (the caught-up day):**
- **`kaiko-ai`** — 4 in `jobs/08-12`. Swiss medical foundation models; **9 open Swiss roles**, the board's #3 employer behind Nvidia and Google.

`nexthink`, `vector8` and `logicstar` also cleared the bar and were found **already present** in the tally — counters incremented, no new entries.

**Held under bar** (recorded so the next run can see the trend):
- **`thrive-holdings`** — **the run's largest un-admitted funding event**: $2B at a $12B valuation (SoftBank, D1 Capital, Altimeter) for the OpenAI-backed roll-up that buys accounting/IT/compliance firms and runs AI as the operating layer inside them. Exactly one in-prose mention in `news/` and one in `daily/` — never twice in a single file — and it reaches the corpus only through the techstartups.com roundup. Same disposition as `corma` (08-11) and `taalas` (08-06).
- **`albedis`** — clears the mention bar (2 + 2) but **held on role**: the collector states plainly it is a recruiter and "the end employer is unknown". Admitting staffing intermediaries is the `rockstar-recruiting` failure mode flagged on 08-11 — they accrue volume they did not earn as a subject of coverage.
- **`pareto-labs`** (1 genuine; the second apparent hit is a URL slug), **`kioxia`**, **`panthalassa`** (1 each).

**Not admitted (carried rules):** LinkedIn job-search *sample* employers — `specto-medical`, `etops`, `techtask-consulting`, `coresystems`, `careera`, `all-here`, `anzen-engineering` (1 each, a seven-row slice of a weekly search). Roll-up enumeration only — `lonza`, `bayer`, `cistec`, `mentiora`, `cradle`, `akina`, `flink-robotics`, `isolutions`, `unit8`, `synchrotech`, `rexult`, `rapidata`. Paper-submitter / author affiliations — `pathway`, `ai2`, `dolphindb`, `socfortress`. The **Gemini connector enumeration** (`granola`, `otter-ai`, `wix`, `fever`, `getyourguide`, `localiza`, `opentable`, `ticketmaster`, `iheartradio`, `pandora`, `angi`, `thumbtack`, `zocdoc`) — one comma-separated list, restated once. Customer-name enumerations (`linklaters`, `heineken`, `mercedes-benz`, `nasa`) and investor enumerations (`silver-lake`, `digitalbridge`, `menlo-ventures`, `softbank`, `d1-capital`, `altimeter`) — investors named in a round are not the subject of the round.

**Not orgs:** `grok-4-6`, `spacexai`, `gpt-5-6-cyber`, `daybreak`, `astra`, `gemini-spark`, `gemini-nano`, `tensor-g6`, `pixel-11`, `titan-m3`, `magic-capture`, `v4-pro`, `mai-thinking-1`, `rubin-cpx`, `b300`, `datastream`, `coco`, `cowork`, `ai-control-tower`, `now-assist`, `netsuite`, `fusion-applications` (all SKUs/features — added to `BRAND_TO_PARENT` this run so they resolve to their parent instead of accruing counters); `bdh-cq`, `sciret`, `masdrift`, `map-graph`, `evograph-mem`, `ascon`, `spieval`, `vibelifebench`, `mechanist` (paper/system names); `ccc`, `netzpolitik`, `cilip`, `flexera`, `diginomica`, `neomanex`, `benchlm`, `tomshardware`, `yonhap`, `euronews` (media/NGO).

**Alias maintenance:** `decart` ← `Decart`, `Decart AI`; `legora` ← `Legora`; `lovable` ← `Lovable`. **19 alias regressions remain detected-but-unrepaired** (`google-deepmind`, `european-commission`, `goldman-sachs`, `amazon-bedrock`, `azure-ai-foundry`, `claude-cowork`, `vera-rubin`, `gemini-3`, `gemma-4`, `agent-memory`, `agent-identity`, `eugene-yan`, `michael-kratsios`, `sapphire-2026`, `washington-post`, `the-register`, `white-house`, `ai-office`, `annex-iii`) — hyphenated slugs whose spaced form appears in today's corpus but whose alias list has no spaced variant, so they score zero on text that is plainly about them. Most are SKUs or bylines that `BRAND_TO_PARENT` absorbs at build time; `google-deepmind`, `european-commission` and `goldman-sachs` are real and worth repairing.

## 📏 Out-of-scope list caps (reported, not mutated — other sweeps own these)

- **`news_collector.web_search_queries` — 60 / 60. Sitting at EXACTLY its cap.** Demotion fires only on `len > cap`, so the list is **frozen**: nothing can be evicted, therefore nothing new can be admitted. Owner: `ai-keyword-sweep`. Unchanged for eleven runs.
- `github_collector.watched_repos` — **132 / 80**. Over cap, so the walk-down does fire; at 5 demotions per run it needs ~11 more runs. Owner: `ai-github-sweep`.
- `news_collector.enterprise_vendors` — **69 / 30** (this sweep's own list). Over cap and in the OVERFLOW regime, but only **one** eligible candidate existed this run (see defect 3), so the walk-down moved by 1 against a budget of 5.

## Tally summary

- Orgs tracked: **1,785**
- Covered (priority): 6
- Covered (enterprise — 61 auto-added of 69 total, against a cap of 30): **69**
- Deep-watch: 14
- Covered (informal URL list): 0
- Uncovered: 1,695
- New orgs added to tally today: **8**
- Orgs touched by the two days of files: **247**
- Hot events recorded today: **2** (both applied)

## Sources scanned this run

- `discovered_orgs.json` (last_updated **2026-08-11** — two days stale, hence the catch-up)
- Source files (**10**, spanning two days): `news/`, `papers/`, `blogs/`, `jobs/` for **both** 2026-08-12 and 2026-08-13; `linkedin/` and `daily/` for 2026-08-13 only (no 08-12 file exists)
- Radar JSONs in window: **30** (916 breadth-derived candidates, 504 rejected under the "what's an org" rules; the remainder were already in the tally)
- `sources.json` snapshot before edit → `sources.json.vendor.bak` (**3** changes applied)

### Provenance note

`WebFetch` remained unavailable to the collectors; all fetching went through `mcp__workspace__web_fetch` under per-URL search-first discipline. The jobs collector reports its **first clean sweep ever** (4 attempted, 4 succeeded) after adopting `swissaijob.ch` as primary index. The news collector, by contrast, could not fetch a single `vendor_blogs` / `tech_news_sites` / `swiss_sources` / `governance_sources` index page on either day — **all 39 configured index URLs fell back to search.**
