# Vendor sweep — 2026-08-06

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

**Added 0 promotion(s):** none — but see § *Pending*, where **`cloudflare`** is one day away.

**Added 0 hot-event vendor(s):** none.

Today's corpus contains exactly **one** new transaction — **AMD acquiring Taalas** (AI chip startup, weights etched into silicon) — and neither side yields an applicable hot event:

- `amd` is **already** in `enterprise_vendors` (hot-event TTL `2026-08-21`), so classification short-circuits at `covered_healthy` before the hot-event test runs. Structurally identical to yesterday's Anaconda/Enkrypt case.
- `taalas` is the **acquiree**, and it is **not in the tally**: the daily file carries exactly one genuine in-prose mention (the headline); the apparent second hit is the Register URL slug. Even if admitted it has no `blog_url_hint`, so Step C.5 would reject it — and promoting a firm being absorbed repeats the `enkrypt` (08-05) and `natoma` (08-04) mistakes both prior sweeps explicitly declined.

The other 5 strong-keyword paragraphs were correctly *not* attributed: Snowflake's "May acquisition of Natoma Labs" is **actioned** and now shipping as Cortex AI Gateway; SAP's "$5.2B n8n stake" is **historical** (May round, GA still Q3); and "over a quadrillion tokens", "1 billion cumulative downloads", "2.8T parameters", "$15B India data-centre" and the Act's "€15M or 3% of global turnover" ceiling are **magnitudes**, not deals.

**Removed 2 expired hot-event entry(s):**

- **baseten** — `_expires_on: 2026-08-05` < today. 30-day hot-event TTL elapsed; the entry never converted to a sustained promote.
- **supabase** — `_expires_on: 2026-08-05` < today. Same.

Both were flagged as due on the 08-05 run and left on exactly the schedule predicted. Next out: `prime-intellect` and `sambanova` (08-09), then `manus` / `tencent` / `helsing` (08-13).

**Demoted 1 to deep-watch (69/30 cap) — and this is the finding of the run: the soft cap fired for the first time in three days.**

- **swiss-re** — last mention `2026-07-23`, **silent 14 days** (overflow regime bar is 14d), moved to `deep_watch_vendors` keeping its `blog_urls` (`https://www.swissre.com/about-us/media/news-releases.html`), `fallback_search` and original `_added_reason`. It is a holding tier, not a graveyard — one qualifying mention re-promotes it exactly.

`swiss-re` crossed the 14-day overflow silence bar **today** (last seen 2026-07-23), and it is the only `_auto_added` entry that is both silent enough and **not** TTL-shielded. The demotion budget was 5 and the walk-down target 45, so the run took the single candidate available.

**Re-promoted 0 from deep-watch:** none.

**Skipped 0 silent vendor(s):** no entry on either vendor list reached `covered_silent` — every one has a mention inside its silence window (45d priority / 60d enterprise). **Read the next section before treating that as good news.**

## 🩺 Dead and half-blind feeds — reported, NOT applied

Today's daily file closes with an explicit collector health note: **`mistral`, `n8n` and `workday_oracle` returned zero items, and no August-dated Red Hat AI release could be confirmed.** All four still classify `covered_healthy`. That is not a bug in the data — it is the structural blind spot in this sweep's silent-vendor test, which counts **mentions**, not whether a feed still works. A vendor named anywhere in the corpus — including in the collector's own failure line — refreshes its `last_seen`.

**Nothing below was applied or logged.** Step D's verb table has exactly six verbs and none covers a URL repair or a feed-health demotion; SKILL.md says *"Do NOT invent other verbs."* Independently, **every affected entry is sacred**: `mistral` and `deepseek` are in `priority_vendors`, and each named `enterprise_vendors` entry lacks `_auto_added`, i.e. it is **manual**. These need a human edit to `sources.json`.

| slug | list / protection | broken as configured | suggested replacement | evidence |
|---|---|---|---|---|
| `mistral` | priority_vendors (SACRED) | https://mistral.ai/news | no replacement identified - the fallback search itself returns nothing August-dated | 0 items today, called out by name in the daily file's collector health note. The newest items the fallback surfaces are Vibe remote agents / Workflows / Mistral Small 4, all PRE-AUGUST. Still classifies covered_healthy: `mistral` appears 2x in the news telemetry block and 1x in the jobs file (Mistral AI x5 Zurich/Lausanne job reqs), so its last_seen refreshes on non-editorial text. |
| `n8n` | enterprise_vendors (MANUAL - sacred) | https://n8n.io/blog/ + https://blog.n8n.io | no verified replacement - both first-party domains unreachable across three runs | 0 items for a THIRD consecutive day. Today's news file records '0 items (fallback used - no new August news; $5.2B SAP round is May, GA pending Q3)'. The slug still scores 4 mentions today, ALL of them from the SAP/Joule Studio storyline plus the collector's own coverage line - none from an n8n feed. |
| `workday_oracle` | enterprise_vendors (MANUAL - sacred) | https://blog.workday.com/en-us/topics/artificial-intelligence.html + https://blogs.oracle.com/ai-and-datascience/ | SPLIT the OR-joined fallback into two queries: "site:workday.com 2026 (...)" and "site:oracle.com 2026 (...)" | 0 items for a THIRD consecutive day; today's fallback `(site:workday.com OR site:oracle.com)` returned ONLY 2025-dated Illuminate results. The search backend does not honour site: OR-operators, so the query collapses to Workday and ORACLE IS NEVER SEARCHED. |
| `redhat_ibm` | enterprise_vendors (MANUAL - sacred) | fallback_search = "(site:redhat.com OR site:ibm.com) 2026 (AI OR agent OR OpenShift OR Granite OR Watsonx)" | SPLIT into two queries: "site:redhat.com 2026 (...)" and "site:ibm.com 2026 (...)" | the 1 item logged today is IBM watsonx Orchestrate; the news file states plainly 'no August-dated Red Hat AI release surfaced', and its dedicated Red Hat query returned 'only third-party Portuguese-language coverage; not retained'. Half of a two-company entry has now been dark for the whole month while the entry reads covered_healthy on IBM's mentions alone. |
| `deepseek` | priority_vendors (SACRED) | https://api-docs.deepseek.com/news | https://huggingface.co/deepseek-ai (already in research_urls - promote it to blog_urls) | the changelog has been stale since 2026-04-24 - now 104 days - yet DeepSeek shipped V4-Flash on 31 July and signalled a significant API price rise TODAY. Both facts reached this pipeline via Caixin and llm-stats.com, NOT via the configured vendor URL. Fourth consecutive day reported. |
| `servicenow` | enterprise_vendors (MANUAL - sacred) | https://newsroom.servicenow.com | newsroom.servicenow.com/press-releases (the dated press-release index, which DID resolve today) | improved: 1 item with a real newsroom permalink (Build Agent in coding tools). Downgraded from 'not being updated' to 'reachable via the press-release path'. Kept on the list for one more run to confirm. |
| `sap` | enterprise_vendors (MANUAL - sacred) | https://news.sap.com/topics/artificial-intelligence/ | https://news.sap.com/ (the ungated index) | 1 item, but it is the same JULY-dated Q2 Business AI release-highlights post carried on 08-05. No August-dated SAP item has surfaced this month. |
| `(long_form_blogs)` | long_form_blogs URL list (not a vendor entry) | ai.engineer, stratechery.com, semianalysis.com, every.to/p/chain-of-thought, eugeneyan.com, lilianweng.github.io, pragmaticengineer.com, blog.langchain.dev, karpathy.github.io, youtube.com/@AndrejKarpathy, huyenchip.com, hamel.dev, cognition.ai/blog, lastweekin.ai, jack-clark.net, aisnakeoil.com | search-first discovery only - none of these are fetchable without a prior WebSearch hit | SIXTEEN configured long-form sources produced no fresh signal: the blogs collector had NO WebFetch path at all for a SECOND consecutive run and was search-only end to end. jack-clark.net is now three months stale (latest indexed issue is May 2026). |
| `(medium_tags)` | blogs_collector.medium_tags URL list (not a vendor entry) | medium.com/tag/llm, /generative-ai, /large-language-models, /retrieval-augmented-generation, /agent-protocols, /tool-use | no replacement - all `medium.com/tag/*` feeds are known-dead for this collector | six configured tags yielded no qualifying new item; everything surfaced was a listicle, vendor content, or already carried. |
| `(arxiv cs.IR)` | papers_collector listing (not a vendor entry) | https://arxiv.org/list/cs.IR/recent | no replacement - upstream maintenance, expected to self-heal | STALE: still serving the Tue 4 Aug page with the arXiv 'System maintenance August 4th and 5th' banner. Retrieval coverage came from cs.CL cross-lists instead. Upstream, not ours. |
| `(linkedin hashtag + home feed)` | linkedin_collector layers (not a vendor entry) | #MCP / #AIGovernance / #RAG content search, and the home feed | no replacement - LinkedIn removed the permalinks; Pulse and jobs still work | 0 items BY CONSTRUCTION for a second day: LinkedIn no longer exposes `urn:li:activity` permalinks in either DOM, so the canonical-URL-or-drop rule discards everything. Pulse (21 articles) and jobs (7 URLs) are unaffected. |

### Quiet vs. unreachable — today's split

- **Unreachable or dark (fetch/query failure):** `mistral` (0 items; every August-dated query empty), `n8n` (0 items, **third** consecutive day), `workday_oracle` (0 items, third consecutive day, and the OR-joined fallback returned only 2025-dated Illuminate results), `redhat_ibm` (**Red Hat half dark all month** — the news file states "no August-dated Red Hat AI release surfaced" and its dedicated Red Hat query returned only untranslated third-party coverage), `deepseek` (changelog frozen 104 days while V4-Flash shipped and a price rise was signalled **today**).
- **Genuinely quiet (fetch worked, nothing new in window):** `sap` (1 item, but the same July-dated Q2 post carried on 08-05), `salesforce`, `nvidia`, `snowflake` (this one had a real vendor permalink today), `servicenow` (**improved** — a dated newsroom press-release permalink resolved).

All of them read `covered_healthy` to the classifier. The five in the first group are the blind spot.

### Telemetry vs. editorial — the measurable version of the blind spot

Diagnostic re-run this sweep (**counted separately, not applied to the tally**): the news collector's own `Priority/Enterprise vendor coverage` blocks and the daily file's `Collector health notes` line are stripped, and each vendor's mentions are re-counted against editorial prose only.

**Telemetry-only — every mention today came from a collector self-report, zero from editorial prose:**

- `google_deepmind` — 1 tally mention(s), **0 editorial mentions**
- `redhat_ibm` — 1 tally mention(s), **0 editorial mentions**
- `workday_oracle` — 1 tally mention(s), **0 editorial mentions**

`workday_oracle` and `redhat_ibm` are the clean instances: both returned 0 (Red Hat) or only 2025-dated (Workday/Oracle) results, and the **only** reason their `last_seen` moved to today is that the collector printed its own coverage line. `google_deepmind` is a false positive of the diagnostic rather than of the pipeline — it is covered editorially all over today's corpus under `Google DeepMind` and `Gemini`, which map to different slugs.

**Telemetry-inflated — real editorial coverage, but the count is padded:**

| slug | tally mentions | editorial | from telemetry |
|---|---|---|---|
| `anthropic` | 14 | 13 | 1 |
| `deepseek` | 11 | 9 | 2 |
| `meta` | 21 | 17 | 4 |
| `mistral` | 3 | 1 | 2 |
| `n8n` | 4 | 2 | 2 |
| `nvidia` | 7 | 6 | 1 |
| `openai` | 28 | 26 | 2 |
| `salesforce` | 8 | 7 | 1 |
| `sap` | 11 | 9 | 2 |
| `servicenow` | 9 | 8 | 1 |
| `snowflake` | 17 | 15 | 2 |

`mistral` is the one that matters: **3 tally mentions, 1 editorial** — and that single editorial hit is the *jobs* file recording five Mistral AI job reqs in Zürich/Lausanne, not a Mistral publication. Its news coverage today was literally `0 items`. A priority vendor whose feed produced nothing is being kept `covered_healthy` by a job advert.

### Structural cause

The fetch layer enforces a per-URL provenance rule - it refuses any URL that has not already appeared in a WebSearch result. Tonight it took out arXiv RSS, papers.cool, arxiv.org/list/cs.AI/recent, the three HN endpoints (recovered via the Firebase API), and EVERY configured priority-vendor index page: openai.com, anthropic.com, claude.com, blog.google, deepmind.google, ai.meta.com, mistral.ai and api-docs.deepseek.com were all covered by fallback search only. Search-first-then-fetch is the only working pattern.

The blogs collector had **no fetch path at all** for a **second consecutive run** and went search-only end to end, leaving **sixteen** configured long-form sources with no fresh signal and all **six** `medium_tags` empty. On the LinkedIn side, hashtag search and the home feed returned **0 items by construction** for a second day — LinkedIn has removed `urn:li:activity` permalinks from both DOMs, so the canonical-URL-or-drop rule discards everything; Pulse (21 articles) and jobs (7 URLs) are unaffected.

## 🧮 Soft cap: the first demotion in three runs, and why it was only one

`enterprise_vendors` went **71 → 68** today: −2 TTL expiries (`baseten`, `supabase`) and −1 deep-watch demotion (`swiss-re`), against **0** additions. The cap is **30** and the overflow threshold **45**, so the sweep is still in the **overflow** regime (silence bar 14d, walk-down target 45, budget 5/run).

Of 61 `_auto_added` entries evaluated (61 before the demotion), the eligibility funnel was:

- **Silent ≥ 14 days:** 4 — `prime-intellect` (26d), `manus` (23d), `helsing` (16d), `swiss-re` (14d).
- **TTL-shielded** (C.6 rule 4 exempts hot-event entries still inside `_expires_on`): 3 — `prime-intellect` (expires 08-09), `manus` (08-13), `helsing` (08-13). These leave by `expire-remove` instead, which is the specified behaviour.
- **Eligible:** 1 — `swiss-re`, a *promote*-path entry with no TTL to hide behind. Demoted.

So the run is a **partial correction of the 08-04/08-05 finding**. Those runs reported `dw_cands` empty and the cap decorative. It is not decorative — it fires the moment an entry without a TTL goes quiet. What is true is narrower and worth keeping: **while the list is dominated by hot-event entries, the only drain that can act on them is TTL expiry**, and 21 of the remaining 68 entries still carry a live TTL. The trend line is now going the right way for the first time this month: 62 (08-03) → 68 (08-04) → 71 (08-05) → **68** (08-06).

The 8 **manual** entries (`n8n`, `nvidia`, `redhat_ibm`, `salesforce`, `sap`, `servicenow`, `snowflake`, `workday_oracle`) are permanently exempt and always will be — they are sacred.

## 🔒 List caps outside this sweep's authority

Checked as instructed; **not touched** — this SKILL's cap rules govern `enterprise_vendors` only.

| list | now | cap | owner | finding |
|---|---|---|---|---|
| `news_collector.web_search_queries` | 60 | 60 | `ai-keyword-sweep` | sits at EXACTLY the cap, so a `len > cap` demotion test never fires - the list is frozen and no entry can be evicted or added |
| `github_collector.watched_repos` | 137 | 80 | `ai-github-sweep` | over cap, so `len > cap` DOES fire; at max_demotions_per_run=5 the walk-down needs ~12 more runs |

`web_search_queries` remains the genuine off-by-one: at **60/60** a `len > cap` test is false, so nothing is evicted **and nothing new can be admitted** — the list is frozen, not merely full. `watched_repos` at 137/80 is *not* blocked by that comparison; it is mid-walk-down at 5 demotions per run (138 → 137 since 08-05).

## ⏳ Pending — needs one more day at promote tier (1 applicable)

**`cloudflare`** — 21 mentions / 4 src types / 4 distinct days over 30d · days at promote so far: **1/2** · `blog_urls: ["https://blog.cloudflare.com/"]`

This is the one to watch. Cloudflare is arguably the biggest structural story in today's corpus — Cloudflare OS launched as an agent/app platform on Workers, and the companion repo `cloudflare/computer` was the **#1 daily star gainer on all of GitHub** (+2,802 on a 5,010 base). A hyperscaler entering the agent-runtime layer is exactly the kind of vendor this sweep exists to catch early. It has a usable `blog_url_hint` and clears every threshold; it needs one more day at promote tier and will auto-apply on the 08-07 run if it holds. Its history is `promote → dormant → promote → dormant → promote`, so the alternation is the only thing that has kept it out so far.

**Still blocked: `figure`** (7th consecutive promote day, has a blog hint, deliberately not applied) — over-matching alias corrected 2026-08-01; the pre-correction counters (16 of 18 hits were the English word 'figure') remain inside the 30-day window.

**108 further slugs sit at promote tier but were rejected at Step C.5** for having no `blog_url_hint` — they can never be applied and are not logged. The safety net is doing its job, but the size of that list is itself the signal: it is dominated by tally pollution that predates this sweep (`commission`, `eu`, `germany`, `switzerland`, `time`, `linear`, `wang`, `li`, `chen`, `iso`, `hn`, `workspace`, `artifacts`, plus brand slugs like `gemini`, `gpt-5`, `azure`, `cortex`, `agentforce`, `qwen`, `claude-code` that `BRAND_TO_PARENT` already maps to parents). One entry — **`the runtime`** — is additionally rejected as an **invalid slug** (contains a space), a second independent signature of the same pollution. Worth a cleanup pass; not auto-removable under any rule in this SKILL.

Genuine orgs in that rejected set that would be applicable if someone added a URL: `artificial-analysis` (14 consecutive promote days), `apertus`, `giotto`, `eth-zurich`, `stanford-hai`, `huawei`, `roche`, `novartis`, `exeon-analytics`, `future-of-life-institute`, `gartner`.

## ⚡ Velocity signals (118 accelerating, 14 cooling with activity)

**Surging (ratio ≥ 5.0 → auto hot_event): 0** — none today. No vendor's posting cadence 5×'d against its 4-week baseline, which is consistent with a day whose one deal involved an already-covered acquirer.

**Accelerating (ratio ≥ 3.0, informational — watch tomorrow):**

- **flexion-robotics** — 4.0× (30 mentions/7d vs. 7.5/wk baseline)
- **supabase** — 4.0× (25 mentions/7d vs. 6.25/wk baseline)
- **sandisk** — 4.0× (24 mentions/7d vs. 6.0/wk baseline)
- **qualcomm** — 4.0× (20 mentions/7d vs. 5.0/wk baseline)
- **stripe** — 4.0× (20 mentions/7d vs. 5.0/wk baseline)
- **enkrypt** — 4.0× (18 mentions/7d vs. 4.5/wk baseline)
- **latam-airlines** — 4.0× (18 mentions/7d vs. 4.5/wk baseline)
- **gema** — 4.0× (17 mentions/7d vs. 4.25/wk baseline)
- **lyft** — 4.0× (17 mentions/7d vs. 4.25/wk baseline)
- **jua** — 4.0× (16 mentions/7d vs. 4.0/wk baseline)
- **suno** — 4.0× (16 mentions/7d vs. 4.0/wk baseline)
- **anyscale** — 4.0× (14 mentions/7d vs. 3.5/wk baseline)

`flexion-robotics` at 30 mentions/7d is the largest absolute mover in the accelerating band, and `enkrypt` (18/7d) is still riding the Anaconda acquisition recorded yesterday.

**Cooling (ratio ≤ 0.33, informational — never a demote trigger):**

- **intel** — 0.17×
- **cerebras** — 0.22×
- **sambanova** — 0.24×
- **google-deepmind** — 0.25×
- **lakera** — 0.25×
- **prior-labs** — 0.26×
- **broadcom** — 0.27×
- **claude-code** — 0.27×
- **gemini-3** — 0.28×
- **samsung** — 0.3×

`lakera` holds at 0.25× for a fourth day — the post-acquisition blog is dead and `check-point` now carries the coverage; the two still need reconciling by hand. `sambanova` at 0.24× is cooling *and* its hot-event TTL expires 08-09, so it will most likely leave by `expire-remove` on that run.

## 👀 Watch list (164 total, top 20 shown)

- **a2a-protocol** — 7 mentions / 5 src types · velocity 1.14× · —
- **agent-runtime** — 7 mentions / 5 src types · velocity 1.14× · —
- **anybotics** — 7 mentions / 2 src types · velocity 4.0× · robotics
- **bjak** — 7 mentions / 1 src types · velocity 2.29× · enterprise-adopter
- **black-forest-labs** — 7 mentions / 2 src types · velocity 1.14× · frontier-lab
- **bundeskartellamt** — 7 mentions / 2 src types · velocity 4.0× · regulator
- **gergely-orosz** — 7 mentions / 2 src types · velocity 0.0× · —
- **gf-accord-ai** — 7 mentions / 1 src types · velocity 4.0× · agent-startup
- **hackernews** — 7 mentions / 1 src types · velocity 0.0× · —
- **illuminate** — 7 mentions / 1 src types · velocity 0.57× · —
- **kyndryl** — 7 mentions / 2 src types · velocity 4.0× · it-services
- **lm-studio** — 7 mentions / 3 src types · velocity 0.0× · —
- **matt-pocock** — 7 mentions / 1 src types · velocity 0.0× · —
- **mind** — 7 mentions / 5 src types · velocity 2.29× · data-security
- **naver** — 7 mentions / 2 src types · velocity 0.0× · —
- **plaid** — 7 mentions / 4 src types · velocity 1.14× · —
- **schunk** — 7 mentions / 2 src types · velocity 0.0× · enterprise-adopter
- **securityweek** — 7 mentions / 2 src types · velocity 1.71× · —
- **superblocks** — 7 mentions / 2 src types · velocity 4.0× · app-generation
- **trains** — 7 mentions / 5 src types · velocity 1.71× · —

## ⚠️ Silent covered vendors (0)

_None._ No vendor on either list exceeded its silence window. See § *Dead and half-blind feeds* for why that number is not the reassurance it looks like.

## 🆕 Tally maintenance

**New orgs inserted (3):**

- **`discovery-loop`** — `frontier-lab`, US. The independent PBC Jeff Dean, Sanjay Ghemawat, Oriol Vinyals and Quoc Le left Google DeepMind to found, targeting automated ML and scientific discovery. 2 genuine in-prose mentions in `daily/` + 1 in `blogs/`. The most material *new legal entity* in today's corpus — four of the most senior systems people in the field leaving a frontier lab at once. `blog_url_hint` deliberately **empty**: no domain appears anywhere on disk and none is inferable.
- **`servisbot`** — `agent-vendor`, IE. Conor O'Neill's EU AI Act Article 14 human-oversight piece is one of the day's five headline LinkedIn items. 2 genuine in-prose mentions in `daily/` + 1 in `linkedin/`. Hint empty — the only URL on disk is the LinkedIn Pulse permalink.
- **`jet-aviation`** — `employer-non-vendor`, CH (Basel). 2 genuine in-prose mentions in `jobs/`. Excluded from the jobs slice as an internship, but the collector flags it as the first MCP-and-RAG job content it has ever seen from a business-aviation employer. Follows the `ergon-informatik` precedent (08-05): out-of-slice exclusions are still tracked as entities. Hint left empty **on purpose** — this is an aviation firm, not an AI vendor, and it must never become promotable.

**Alias repairs (5):** `eth-zurich` += ["ETH Zürich", "ETH Zurich"]; `sierra` += ["Sierra"]; `weaviate` += ["Weaviate"]; `sonarsource` += ["SonarSource"]; `orange-business` += ["Orange Business Digital Services"]

The one that matters is **`eth-zurich`**, which had *no aliases at all*, so matching fell back to the bare hyphenated slug — a form prose never uses. Today's jobs file is dominated by ETH (its own board read in full: 136 open positions, 7 in-slice, 5 new to the pipeline) and the org was reading none of it. Both the umlaut and ASCII spellings were added because the corpus mixes them. Same regression class as `applied-intuition` (08-05), `julius-baer` / `red-hat` (08-04) and `swiss-re` / `together-ai` (08-03) — this keeps recurring and is worth a one-off sweep over every hyphen-only-alias org.

The automatic regression diagnostic flagged 14 further hyphen-only slugs whose space-form appears in today's corpus. **None were repaired**: they are people (`adnan-masood`, `rick-hightower`), media (`the-register`, `help-net-security`, `latent-space`), products (`azure-ai-foundry`, `claude-code`, `mistral-small`, `vibe-remote-agents`, `google-cloud-next`), or JUNK-listed bare-token governance slugs (`ai-office`, `eu-ai-office`, `uk-aisi`) and the `google-deepmind` brand alias that `BRAND_TO_PARENT` already maps to the priority vendor.

**Deliberately NOT inserted (18 classes):**

- `taalas` — ACQUIREE + UNDER BAR. AMD acquiring Taalas is today's only new transaction, but the daily file carries exactly ONE genuine in-prose mention (the headline); the second hit is the Register URL slug. It is also the company being absorbed - the `enkrypt`/Anaconda (08-05) and `natoma`/Snowflake (08-04) precedent declines to promote a firm that stops publishing independently. Note the acquirer `amd` is ALREADY an enterprise_vendors entry (TTL 2026-08-21), so the deal short-circuits at covered_healthy before the hot-event test runs - structurally identical to yesterday's Anaconda/Enkrypt case
- `neon` — UNDER BAR - 1 genuine in-prose mention plus one markdown link-text repeat of the same item (the Castform retrieval-cost post). Real company, will re-surface if the thread continues
- `1password, aembit, saviynt, linx-security` — PARTNER-LIST ENUMERATION - each appears only inside Snowflake's seven-identity-partner list, restated once in news and once in daily; no independent editorial treatment. `1password` additionally fails the C.5 slug rule `[a-z][a-z0-9-]*` (leading digit). The three partners already tracked (`okta`, `sailpoint`, `cyera`) were admitted on their own coverage, not on a partner list
- `rivian, asana, edmunds` — CUSTOMER-LIST ENUMERATION - named twice as Databricks Unity AI Gateway reference customers and nowhere else. Buyers of a product, not entities in the AI supply side
- `artifactory` — PRODUCT of JFrog - and `jfrog` was promoted to enterprise_vendors yesterday, so the parent is already covered. Added to BRAND_TO_PARENT
- `grokipedia` — PRODUCT of xAI (`xai` is tracked). Added to BRAND_TO_PARENT
- `qnx` — PRODUCT/OS of BlackBerry, and both hits are inside one sentence
- `hornet, mixedbread, lighton` — 1 mention each, inside a single HN taxonomy enumeration of late-interaction retrieval vendors. Real firms; skipped under when-in-doubt, will re-surface
- `kaggle, castform, sandstorm, kenton-varda, caixin, public-ai, wilson-sonsini, lc-waikiki, axera` — 1 genuine mention each (and `castform` is a model name, `sandstorm` a defunct product, `kenton-varda` a person)
- `khare-automations, zerolook, wasteflow, etops, 10x-partners, a1` — LinkedIn job-search sample employers - 1 mention each, and the sample is a first page of 218 results, not editorial signal
- `dextra-labs, navikenz, converteo, arvato-systems, bdf-experts, techment, spillwave-solutions, agentic-ai-foundation` — LinkedIn Pulse author affiliations - 1 mention each
- `valantic, tecan, adaptyv, dontouch, harmattan-ai, swiss-geolab, inspire-ag, university-of-basel, bank-j-safra-sarasin` — carried Swiss job-board names at 1 mention each this run - ordinary counter behaviour, several already in the tally
- `mimic, fairwai, verity, six, ost, arm, cogent-ai, june, spring, figure` — ALIAS HAZARDS - bare tokens that are common English words or over-matching acronyms. Carried block, unchanged
- `wellfound, glassdoor, indeed, aijobs-ai, startup-jobs, swissaijob, fwddeploy, academicpositions` — JOB BOARDS - distribution surfaces this pipeline reads, not entities in the AI market
- `infoq, the-register, venturebeat, marktechpost, techcrunch, quartz, caixin, techstartups, heise, t3n, golem, lawfare, latent-space, smol-ai` — MEDIA outlets - JUNK-listed by class
- `aisi / uk-aisi, european-commission, eu-ai-office, nist, us-congress` — governance bodies already tracked or already JUNK-listed as bare-token slugs
- `muse-code, muse-spark, cortex-ai-gateway, unity-ai-gateway, cloudflare-os, context-engine, agent-studio, glm-5.2, qwen3.8-max, kimi-k3, lfm2.5, gpt-5.6, discovery-loop-the-thesis` — PRODUCT / MODEL / SKU names - track the vendor (meta, snowflake, databricks, cloudflare, sierra, alibaba, moonshot, liquid-ai, openai), not the SKU
- `breadth long-tail with no corroboration in any of today's six source files` — carried from 08-03..08-05; when in doubt, skip

Radar breadth: **802 distinct slugs** across 30 radar JSONs; **398 rejected** by the §3 org filter (suffix / brand→parent / JUNK / too-short / no corroboration in any of today's six source files), **0 admitted** — every breadth candidate that survived the filter this run was already in the tally.

## Tally summary

- Orgs tracked: **1764**
- Covered (priority): 6
- Covered (enterprise — incl. 60 auto-added of 68): 68
- Deep-watch: 12
- Covered (informal URL list): 0
- Uncovered: 1677
- New orgs added to tally today: 3
- Orgs touched by today's files: 231
- Hot events recorded today: 0

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-08-05 → 2026-08-06)
- Today's source files: blogs/2026/08/2026-08-06.md, daily/2026/08/2026-08-06.md, jobs/2026/08/2026-08-06.md, linkedin/2026/08/2026-08-06.md, news/2026/08/2026-08-06.md, papers/2026/08/2026-08-06.md
  - `github/` and `hackernews/` exist for today but are **not** among the six source directories SKILL §3 defines, so they are not tallied. (Worth noting anyway: the GitHub collector had a clean run this time — 137/137 watched repos read with exact counts, and the trending stale-cache problem was confirmed a fetch-proxy artifact that did not recur.)
- Radar JSONs in window: 30
- sources.json snapshot before edit → sources.json.vendor.bak (3 change(s) applied)
