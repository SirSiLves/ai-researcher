# Vendor sweep — 2026-08-11

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

> **EVENING MERGE RUN — this file REPLACES the 07:46 morning version.** A partial sweep ran this morning against partial collector output. All six tallied source files have since grown (papers 25→44 items, jobs 27→61, linkedin 19→26, news 18→22, blogs 16→21; `hackernews/` and `github/` grew too but are not tallied source types under the SKILL). Today's org counters were **reconciled — stripped and recomputed from the current files — not accumulated**. 203 orgs carried morning counters; all 203 were reset before the recount. There is no `-v2` file and no double-counting.
>
> **Backup note.** `sources.json.vendor.bak` was written by the morning pass and holds the **pre-day** snapshot. It was deliberately **not** overwritten tonight, so the documented rollback command still reverts the whole of 2026-08-11 (morning + evening), which is the correct unit for a one-canonical-sweep-per-day pipeline.

## 📋 What changed in sources.json today

**This evening pass applied nothing. `sources.json` was not rewritten** (byte-identical to the 08:44 state; mtime unchanged).

The **morning pass** made four mutations, and they stand — the evening recount re-derives all four and finds none of them wrong:

- **Added 1 promotion:** `isomorphic-labs` — `blog_urls=["https://www.isomorphiclabs.com/articles"]`, promote for 2 consecutive days (9 mentions, 3 src types, 7 distinct days / 30d).
- **Added 1 hot-event vendor:** `intel` — trigger `billion`, expires **2026-09-10**. $15bn underwritten common-stock offering announced 10 Aug (+$2.25bn underwriter option), Intel's first public share sale since 1971, earmarked for AI-silicon capex. Tonight `intel` scores **16** mentions (up from 15) and classifies `covered_healthy` — the entry is doing its job and is correctly **not** re-added.
- **Removed 2 expired hot-event entries:** `prime-intellect`, `sambanova` (TTL elapsed).
- **Demoted 0 to deep-watch, re-promoted 0.**

Evening change-set, for the record: **0 promotions, 0 hot events, 0 expirations, 0 deep-watch demotions, 0 re-promotions.** No audit-log lines were appended, per Step D ("never log a decision-not-to-apply").

### Why the evening added nothing

| Gate | Outcome |
|---|---|
| Sustained-promote candidates with a usable `blog_url_hint` | 3 — `figure` (blocked, over-matching alias), `dbs-bank` + `swiss-re` (blocked, oscillation guard — see below) |
| Fresh hot-event transactions in tonight's corpus | 1 (Intel), already applied this morning |
| `expired_to_remove` | 0 — the three TTL entries left (`manus` 2026-08-13, `helsing` 2026-08-13, `norm-ai` 2026-08-25) have not elapsed |
| Deep-watch demotion candidates | **0**, despite `enterprise_vendors` sitting at **68 / cap 30** — see the dead-source note below |

## 🐞 Known defects exercised this run

### 1. `blog_url_hint` blocker — 120 orgs, and it is the whole reason nothing moved

**120** orgs classified `promote` tonight are rejected **solely** because `blog_url_hint` is empty (Step C.5), and **116** of those have already cleared the 2-consecutive-day sustained gate — i.e. they are fully qualified promotions that the pipeline cannot express. Nothing in the pipeline ever populates that field. Only **3** of the 124 promote-tier orgs have a hint at all.

Top blocked orgs by consecutive days at `promote` (the history cap is 14, so "14" means *at least* 14 runs — `oracle` is now on its 15th consecutive blocked run):

| slug | consec. days at promote | mentions / 30d |
|---|---|---|
| `oracle` | 14+ | 109 |
| `linkedin` | 14+ | 687 |
| `agentforce` | 14+ | 39 |
| `modelcontextprotocol` | 14+ | 41 |
| `bytedance` | 14+ | 18 |
| `box` | 14+ | 32 |
| `axios` | 14+ | 33 |
| `artificial-analysis` | 14+ | 21 |
| `future-of-life-institute` | 14+ | 23 |
| `hai` (Stanford HAI) | 14+ | 34 |
| `giotto` | 14+ | 23 |
| `slack` | 14+ | 14 |
| `postgres` | 13 | 21 |
| `hf` (Hugging Face) | 12 | 70 |
| `deepseek-v4-pro` | 12 | 11 |

A second, compounding problem is visible in that same list: only **44 of the 120** blocked slugs are plausible vendor entries at all. The other 76 are concept/brand/person slugs the tally should never have admitted (`switzerland` 378 mentions, `time` 207, `workspace`, `wang`, `stanford`, `qwen`, `prompt-injection`, `willison`/`simonwillison` as two separate slugs). They are blocked by the same missing-hint check, so the backlog number overstates the real coverage gap by roughly 2.5×. **Fixing the hint blocker without first pruning the tally would auto-promote ~76 non-companies into `enterprise_vendors`.**

Tonight's run backfilled exactly **one** hint, and only because the URL appears verbatim in the corpus (no hint is ever invented from memory):

- `macquarie` → `https://www.macquarie.com/us/en/about/news.html` — Macquarie's newsroom is the **only** first-party permalink for the Anthropic/Macquarie/GIC data-centre JV; `anthropic.com/news` did not carry it.

### 2. Promote/demote oscillation — 3 orgs flagged, 2 actively held

Demotion tests `last_seen` on a 14-day window while promotion tests a 30-day mention window, so a vendor demoted for silence can re-qualify almost immediately. Three deep-watch entries demoted within the last 14 days re-classified into an applying tier tonight:

| slug | demoted on | days since | tier today | mentions / 30d | disposition |
|---|---|---|---|---|---|
| `swiss-re` | 2026-08-06 | 5 | `promote` | 49 across 10 days, 3 src types | **HELD** by the oscillation guard |
| `dbs-bank` | 2026-08-08 | 3 | `promote` | 12 across 6 days, 5 src types | **HELD** by the oscillation guard |
| `accenture` | 2026-08-01 | 10 | `hot_event` | 3 | held anyway — stale hot-event record, no fresh transaction this run |

`swiss-re` is the clearest illustration: it was demoted for 14 days of *silence* while sitting on **49 mentions in the 30-day window**. Both windows are reading the same tally and disagreeing. `dbs-bank` was demoted three days ago and already clears every promotion threshold.

**No org both demoted and re-promoted on the same day** — the guard held all three. But the guard is a 14-day patch in this sweep script, not a fix: on 2026-08-21 `swiss-re` ages out of the guard window and will re-promote, and the cycle restarts. The real fix is to align the two windows (both 30d, or both 14d) in `vendor_sweep_config`.

### 3. Dead-source blindspot — and tonight it produced a hard, visible consequence

The silent-vendor check reads **mention counts**, not **feed health**, so a vendor mentioned by third parties reads healthy no matter how dead its own feed is. `covered_silent` came back **empty (0)** tonight and **deep-watch demotion found 0 candidates** even though `enterprise_vendors` is at 68 against a cap of 30 — every auto-added entry has a `last_seen` inside 14 days, purely on third-party mentions. **The soft cap therefore cannot converge.** That is not the cap working; that is the cap being unable to fire.

Feeds the collectors reported dead or degraded today, all of which nevertheless read healthy:

- `meta` (priority, sacred) — 0 first-party items for a second run, while shipping the window's single biggest story (Muse Glimmer, 1,083 HN points). `research.meta.ai/blog/` carries the post; the configured `ai.meta.com/blog/` does not.
- `google_deepmind` (priority) — 0 items, while `developers.googleblog.com` (not in `blog_urls`) carried the only first-party Google item of the day.
- `anthropic` (priority) — 0 items from `anthropic.com/news`, while the `claude.com/blog` half of the same entry shipped a 280-point headline the read missed; and the Theseus JV was learned from Macquarie's newsroom.
- `nvidia` (manual) — 0 items for a second run; scores 15 mentions tonight and reads `covered_healthy`.
- `n8n` (manual) — 0 items for a **fifth** consecutive run; both first-party domains unreachable.
- `workday_oracle` (manual) — the only vendor whose entire mention count tonight is the news collector's **own telemetry line** (1 total, 0 editorial). Instrumented below.
- 20 configured `long_form_blogs`, all 17 `medium.com/tag/*` feeds, and 5 arXiv/papers.cool listings produced nothing.

**Telemetry-inflated vendors tonight** (total hits vs. genuinely editorial hits, after stripping the collector's own vendor-coverage blocks) — every one of these has its `last_seen` partly refreshed by the pipeline reporting on itself: `workday_oracle` 1/0, `anthropic` 28/26, `deepseek` 21/20, `meta` 25/24, `nvidia` 15/14, `openai` 32/31, `sap` 11/10, `servicenow` 11/10, `snowflake` 22/21, `mistral` 4/3, `n8n` 3/2, `salesforce` 2/1.

### 4. Velocity → hot_event path — confirmed dead code, not exercised

Measured across all 1,777 orgs tonight: **max `velocity_ratio` = 4.00**, threshold = **5.0**, **0 orgs surging**. The ratio is `v7 / max(v28_avg, 1.0)`, and with `v28_avg` floored at 1.0 a single-day burst caps at `v7 / (v7/4) = 4.0`. 78 orgs sit at `accelerating`, and **69 of them are pinned at exactly 4.00** — the ceiling, not a distribution. No hot event has ever been raised by this path and none can be.

## ⏳ Pending — needs one more day at promote tier (0)

None. Every one of the 124 promote-tier orgs either already holds ≥2 consecutive days (and is blocked on `blog_url_hint`), is oscillation-held, or is the `figure` alias block. There is no org that will auto-apply tomorrow simply by holding tier.

## ⚡ Velocity signals (78 accelerating, 1,358 cooling)

Informational only; see defect 4 for why nothing here can escalate.

**Accelerating — all pinned at the 4.00 ceiling, ranked by absolute 7-day volume:**

- **`enkrypt`** — 4.00× (18/7d vs 4.5/wk) · security-eval vendor, second week of momentum
- **`latam-airlines`** — 4.00× (18/7d vs 4.5/wk) · enterprise adopter
- **`lyft`** — 4.00× (17/7d vs 4.25/wk) · enterprise adopter
- **`julius-baer`** — 4.00× (17/7d vs 4.25/wk) · Swiss private bank, hiring-led signal
- **`geberit`** — 4.00× (14/7d vs 3.5/wk) · admitted to the tally this morning; the "enablement office" pattern in Swiss industry
- **`caplena`** — 4.00× (13/7d vs 3.25/wk) · Swiss NLP vendor
- **`archlet`** — 4.00× (12/7d vs 3.0/wk) · Swiss procurement AI
- **`gf-accord-ai`** — 4.00× (11/7d vs 2.75/wk)
- **`brookfield`** — 4.00× (10/7d vs 2.5/wk) · data-centre capital
- **`ergon-informatik`** — 4.00× (9/7d vs 2.25/wk) · Swiss

**Cooling with residual activity (not a demote trigger):** `kimi` 0.07×, `lambert` 0.11×, `moonshot` 0.12×, `interconnects` 0.17×, `minimax` 0.19×, `google-deepmind` 0.24×, `slack` 0.29×, `thinking-machines` 0.30×, `claude-code` 0.32×, `samsung` 0.31×.

## 👀 Watch list (178, showing 20)

Orgs trending upward but below promotion thresholds.

- **`infomaniak`** — 7 mentions / 2 src types · velocity 4.00× · swiss_sovereign_cloud (admitted this morning; strongest new Swiss entry)
- **`black-forest-labs`** — 7 / 2 · frontier-lab
- **`bundesnetzagentur`** — 7 / 2 · governance-regulator
- **`bundeskartellamt`** — 7 / 2 · regulator
- **`destinus`** — 7 / 1 · 1.71× · aerospace-defence
- **`superblocks`** — 7 / 2 · app-generation
- **`plaid`** — 7 / 4 · fintech infrastructure
- **`jetbrains`** — 7 / 2
- **`lm-studio`** — 7 / 3 · local inference
- **`kong`** — 7 / 5 · API/agent gateway
- **`work-louder`** — 7 / 2 · hardware
- **`bjak`** — 7 / 1 · enterprise-adopter
- **`securityweek`** — 7 / 2 · 1.71×
- **`gergely-orosz`**, **`pragmatic-engineer`**, **`matt-pocock`** — 7 / 1–2 each (individual authors; tally pollution, candidates for pruning)
- **`agent-governance-toolkit`**, **`agent-runtime`**, **`mind`**, **`hackernews`** — 5–7 each (concept/infrastructure slugs; pruning candidates, not vendors)
- **`9to5google`** — 6 / 2 · 2.67× (media outlet; pruning candidate)

Roughly a third of the watch list is not a company. Same root cause as the blocked-promotions inflation above.

## ⚠️ Silent covered vendors (0)

None flagged — see defect 3 for why that number is not trustworthy. Six vendor feeds produced zero first-party items today and every one of them reads `covered_healthy` on third-party mentions.

## 🆕 New orgs admitted to the tally tonight (4)

Bar (carried unchanged from 08-02): named legal entity, ≥2 **genuine in-prose** mentions in a **single** file (roll-up enumerations, URL slugs and markdown link-text repeats do not count), plus the journalist test.

- **`theseus-infrastructure`** — 13 mentions (news + daily). Newly formed JV of **Anthropic + Macquarie Asset Management + GIC** to develop, own, operate and **lease** purpose-built data-centre capacity, announced 10 Aug. The evening's one materially new corporate entity. **No size was disclosed**, so it carries no funding keyword and correctly does *not* reach `hot_event` — it is a JV formation, not a raise. Classifies `dormant` tonight (13 mentions is *above* the watch band's 7-mention ceiling but it has only 1 distinct day and 2 source types, so it clears neither watch nor promote — a genuine gap in the tier ladder for same-day arrivals).
- **`meteoschweiz`** — 4 (jobs + daily). Bundesamt für Meteorologie und Klimatologie, hiring an ML engineer for climate/weather forecasting.
- **`kanton-luzern`** — 3 (jobs + daily). Cantonal government hiring a *KI-Engineer / KI-Plattform-Spezialist*; described by the collector as the single most interesting find of the merge run.
- **`spitalzentrum-biel`** — 6 (jobs + daily). Public hospital hiring an ICT enterprise architect for AI.

The Swiss public sector entered the corpus at all three tiers of government simultaneously tonight (federal / cantonal / municipal) — a first for this pipeline.

**Held under bar** (recorded so the next run can see the trend): `swiss-medical-network` (1), `lgt-capital-partners` (1, and parent `lgt` is already tracked), `neho` (1 genuine + 1 roll-up), `frontiers-media` (1 once the alias is made safe — a bare `frontiers` alias would match the English noun), `genius-sports`/`orderfox`/`infoconsulting`/`jk-advisory`/`optiml`/`visium` (1 genuine each; their extra hits are all inside the run's two enumeration lines), `stadt-bern`/`kanton-zuerich` (1 each, inside the three-tier enumeration).

**Not orgs:** `muse-glimmer`, `qwen3-8-max`, `pinecone-nexus`, `oracle-ai`, `private-ai-runtime` (products/SKUs); `dcas`, `superlocalmemory`, `skillsentry`, `masdrift`, `matraix`, `muscle-memory` (paper/system names); `adao`, `aicc`, `agentic-enablement-office` (internal units); `zhaw`, `zhdk`, `waterloo`, `zhejiang` (university affiliations, consistently declined); `michael-page` (recruiter infrastructure); `testingcatalog`, `technode`, `dataconomy` (media).

**One count worth distrusting:** `rockstar-recruiting` (already tracked, `uncovered`) scored **9** hits in `jobs/` — the densest name in the file — but **seven** of those are `via Rockstar Recruiting AG` attributions on *other* companies' requisitions. It is a staffing intermediary standing between the pipeline and unnamed employers, and it will cross watch thresholds on volume it did not earn as a subject of coverage.

**Alias maintenance:** `macquarie` ← `Macquarie`, `Macquarie Asset Management`; `lgt` ← `LGT Capital Partners`; `luware` ← `Luware AG`. Ten alias regressions remain detected-but-unrepaired (`adnan-masood`, `gemini-3`, `gemma-4`, `muse-spark`, `cortex-agents`, `amazon-bedrock`, `azure-ai-foundry`, `knowledge-2026`, `agent-runtime`, `matt-mochalkin`) — all are SKUs, releases or bylines that `BRAND_TO_PARENT` absorbs at build time and that should never accumulate their own counters.

## 📏 Out-of-scope list caps (reported, not mutated — other sweeps own these)

- `news_collector.web_search_queries` — **60 / 60**. Demotion fires only on `len > cap`, so the list is **frozen**: nothing can be evicted, therefore nothing new can be admitted. Owner: `ai-keyword-sweep`.
- `github_collector.watched_repos` — **132 / 80**. Over cap, so the walk-down does fire; at 5 demotions per run it needs ~11 more runs. Owner: `ai-github-sweep`.

## Tally summary

- Orgs tracked: **1,777**
- Covered (priority): 6
- Covered (enterprise — 60 auto-added of 68 total, against a cap of 30): 68
- Deep-watch: 13
- Uncovered: 1,689
- Informal URL list: 0
- New orgs added to tally today: **4** (evening) + 7 (morning) = 11 for the day
- Orgs touched by today's files: 241
- Hot events recorded today: 1 (Intel, applied this morning)
- Orgs whose today-counters were reconciled rather than accumulated: **203**

## Sources scanned this run

- `discovered_orgs.json` (last_updated 2026-08-11, morning pass)
- Today's source files (6): `news/`, `papers/`, `blogs/`, `jobs/`, `linkedin/`, `daily/` — all `2026/08/2026-08-11.md`
- Radar JSONs in window: 30 (890 breadth-derived candidates, 486 rejected under the "what's an org" rules; the remainder were already in the tally)
- `sources.json` snapshot: **not re-taken** — the morning `.bak` is the pre-day rollback point (**0 evening changes applied**)

### Reconcile method (for audit)

Today's morning contribution was removed exactly for `mentions_by_date[2026-08-11]`, `total_mentions`, `classification_history` and `velocity_history`, then recomputed from the current files. `mentions_by_source_type` and `alias_hits` store no per-date-per-type split and the morning file bodies are not recoverable (last git commit is 2026-07-12), so today's contribution to those two fields was re-allocated over tonight's split with largest-remainder rounding: **per-org totals are exact; only the split across types may differ by ±1 from a true recount.** Neither field feeds classification — SKILL §4 reads `recent_mentions` from `mentions_by_date` and source-type breadth from `distinct_source_types` (a monotone set). Verified post-write: 0 orgs carry a duplicate `2026-08-11` classification row.
