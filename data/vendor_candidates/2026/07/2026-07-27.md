# Vendor sweep — 2026-07-27

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

**4 mutations applied.** Net list sizes are unchanged (two in, two out on each list), but all four movements are real:

- **Added 0 promotion(s).** No uncovered org cleared the sustained-promote gate (≥2 consecutive days at `promote`) _with a usable `blog_url_hint`_. The one slug that did clear it — **ubs** — is in `deep_watch_vendors`, so it routes to the re-promotion path below rather than creating a duplicate entry.
- **Added 1 hot-event vendor:**
  - **safe-superintelligence** — `blog_urls=["https://ssi.inc"]`, expires **2026-08-26**. Ilya Sutskever's lab took a **multi-billion-dollar investment from NVIDIA** plus access to the Vera Rubin platform (~10× its compute) after two years in stealth. **New to the tally today**, 10 mentions across `tech_news` + `daily_synthesis`. This is the first capital event SSI has ever disclosed, and it puts NVIDIA on both sides of the frontier on the same day it launched a security alliance without the labs that ship.
    - _Gate note:_ the phrasing is "investment", not "Series X" — the matched keywords are `funding round` and `billion`. Bare `billion` normally fails the strong-funding gate (it is how commercial supply MOUs and capex numbers leak in), so this was admitted on an explicit read of the source text, not on the keyword alone. A disclosed multi-billion equity investment into a private lab is a funding round in substance.
- **Removed 1 expired hot-event entry:** **spacex** (`_expires_on` 2026-07-26 < today). TTL elapsed without crossing sustained-promote thresholds; the entry lapses cleanly. This is the first of the TTL unblocks yesterday's run predicted.
- **Re-promoted 1 from deep-watch:** **ubs** — second consecutive day at `promote` (07-26, 07-27), so the sustained gate fires and the original entry moves back to `enterprise_vendors` with its `blog_urls` and `fallback_search` intact. **Fourth oscillation in five days** (promoted 07-23, demoted 07-24, promoted today) — see the note below.
- **Demoted 1 to deep-watch (55/30 cap, OVERFLOW regime):** **minimax** — last mention 2026-07-13, silent **14 days**, `_auto_added`, no live TTL. The only eligible candidate in the whole list.
- **Skipped 0 silent vendor(s).** No covered vendor is past its silence threshold (priority 45d / enterprise 60d). All 6 priority vendors surfaced today; the most-silent enterprise entry is `qualcomm` at 26 days, well inside the 60-day window.

_Net: sources.json changed — **4 mutations**. `enterprise_vendors` **54 → 54** (46 auto, 8 manual: +safe-superintelligence, +ubs, −spacex, −minimax); `deep_watch_vendors` **15 → 15** (+minimax, −ubs)._

### The soft cap: one demotion, and the TTL shield is still the binding constraint

Post-adds the list stood at **55** against `max_enterprise_vendors: 30` — past the overflow line (55 > 45 = cap×1.5), so the **overflow** regime engaged with the relaxed 14-day silence bar, target 45 and a budget of 5. It found exactly **one** eligible entry. Every other `_auto_added` entry silent ≥14 days is still shielded by a live hot-event `_expires_on` (Step C.6 condition 4):

| slug | silent | TTL shield |
|---|---:|---|
| qualcomm | 26d | 2026-07-27 _(lapses tomorrow)_ |
| baseten | 21d | 2026-08-05 |
| supabase | 21d | 2026-08-05 |
| prime-intellect | 16d | 2026-08-09 |
| together-ai | 15d | 2026-08-02 |
| sambanova | 14d | 2026-08-09 |

So the diagnosis from 07-26 holds and is now demonstrated rather than asserted: **the TTL shield, not the silence window, throttles cap convergence.** `minimax` was demotable precisely because it is the one silent auto-entry with no TTL. `qualcomm` (26d silent) becomes eligible tomorrow when its 07-27 TTL lapses, then the queue opens further on 08-02 / 08-05 / 08-09. Budget was 5, spend was 1 — no rule was bent to force convergence.

### ⚠️ `ubs` has now oscillated four times in five days

07-21 demote → 07-23 promote → 07-24 demote → 07-27 promote. Every promote leg is driven by **trailing-window volume**: 23 mentions / 6 source types / 4 distinct days, all of it from 06-27→07-07, with **nothing fresh since 2026-07-07** (20 days silent). The rules are being followed exactly — sustained-promote outranks the demotion queue, and a re-promoted slug is exempt from same-day demotion — but the pair of rules is producing churn rather than signal. It will fall back into the demotion queue the moment its 30-day window rolls past 07-07 (i.e. from **2026-08-07**), unless UBS actually surfaces again. Flagging as a rules-interaction observation for the user; **not** something this skill is authorised to fix unilaterally.

### Rejected — classified hot/promote but not applicable (Step C.5)

- **cxmt** (hot_event) — Changxin Memory's Shanghai debut **surged ~470% to a ~$487B valuation on a 7% free float** (`valuation` keyword, 7 mentions today). A US-blacklisted DRAM/HBM maker capitalising at that scale matters because memory, not logic, is the tight link in the AI supply chain. Still **no fetchable vendor blog**, so the entry would be useless to the news collector — rejected for the second consecutive day, exactly as on 07-26. Recorded on the tally; not applied.
- Standing hint-less `promote` set, unchanged in character: **bytedance**, **alibaba**, **nous-research**, **giotto**, **cern**, **cscs**, **apertus**, **artificial-analysis**, **future-of-life-institute**, **workday**, **box**, **lm-studio**, **sakana**, plus the historical tally pollution (`agentforce`, `linkedin`, `slack`, `postgres`, `modelcontextprotocol`, `prompt-injection`, `hai`, `axios`, `latent-space`, `singh`, `claude-cowork`, `deepseek-v4-pro`, `beijing`) that classifies on stray substring matches and is never applied. 82 candidates rejected in total.

### Held by the fresh-activity guard (hot_event on stale in-window signal, no fresh funding this run)

**moonshot** (v7=126, ratio 2.39× — the K3 weights drop is the day's biggest story but a weights release is not a funding or M&A event), **cognition**, **cursor**, **groq**, **tenstorrent**, **reflection-ai**, **general-intuition**, **aisi-uk**, **goldman-sachs**, **blackstone** _(deep-watch)_, **uber** _(deep-watch)_, **ibm**.

### Explicitly **not** treated as hot events today

- **DeepSeek freezing its second round at a reported ¥500B (~$74B) target** — the largest funding number in the cycle, but it is a round being *suspended*, not closed. DeepSeek is a `priority_vendor` and sacred either way.
- **Gartner's $6.37T 2026 IT-spend forecast** (+14.2% YoY) — a market forecast, not corporate funding.
- **Cognizant becoming a Claude Partner Network Global Premier Partner** — a partnership. `cognizant` is already in `deep_watch_vendors`; it accelerated to 4.0× (10 mentions/7d) today but sits at `dormant` on window volume, so no re-promotion path fires.
- **SAP completing the Prior Labs acquisition** — already applied 07-19, TTL runs to 08-18.
- **Enigma's $71M raise** (robot teleoperation interfaces) — a genuine closed round, but it appears exactly **once** in `news/` (headline plus its own URL slug), below the ≥2-mentions-in-one-file bar to even enter the tally. Deliberately not inserted; it will re-surface if it is real.
- **NVIDIA's Open Secure AI Alliance** — a 40+ member consortium launch, not a transaction. Added to the tally as an org (below).

## ⏳ Pending — needs one more day at promote tier (1 applicable)

**deloitte** — currently in `deep_watch_vendors`, `blog_url_hint` present (`www2.deloitte.com/global/en/insights.html`), days at promote so far: **1/2**. A second consecutive promote day tomorrow triggers `deep-watch-promote`, not a fresh add.

Also at promote-day-1 but **not applicable** (no `blog_url_hint`): `gartner`, `ostp`, `block`, `vllm`. `gartner` is the interesting one of those — it has crossed the thresholds on the back of the IT-spend forecast cycle and is a real analyst house, but it publishes behind a paywalled newsroom and no URL has been verified, so nothing is invented for it.

## ⚡ Velocity signals (0 surging, 93 accelerating, 13 cooling)

No vendor is **surging** (ratio ≥ 5.0), so nothing was velocity-promoted to `hot_event`. As on previous runs the `accelerating` bucket is dominated by orgs pinned at exactly 4.0× — one active day against a near-empty 4-week baseline, which is arithmetic rather than signal. The movers with real mass behind them:

- **aws** — 3.36× (79/7d vs 23.5/wk) · the largest absolute 7-day volume on the board.
- **amd** — 3.88× (63/7d vs 16.25/wk) · enterprise, live TTL; the Anthropic investment still compounding, and the 2nm server silicon lands in the same week.
- **white-house** — 3.67× (45/7d vs 12.25/wk) · policy body; OSTP's Moonshot accusations plus the frontier-model review framework expected before 1 Aug.
- **samsung** — 3.22× (33/7d vs 10.25/wk) · the HBM4 supply thread under every compute story.
- **fireworks-ai** — 4.0× (16/7d) · **poolside** — 3.56× (16/7d) · **cisco** — 3.73× (14/7d) · **cognizant** — 4.0× (10/7d vs 2.5/wk, on the Anthropic partnership).
- New or newly-active entrants land at 4.0× by construction (empty baseline): `safe-superintelligence`, `cxmt`, `black-forest-labs`, `debian`, `exeon-analytics`, `norm-ai`, `knowledge-lab`.

**Cooling (ratio ≤ 0.33, informational — not a demote trigger):** `thinking-machines` 0.10×, `semianalysis` 0.11×, `hamel` 0.15×, `xai` 0.17×, `pragmaticengineer` 0.22×, `zhipu` 0.24×, `sebastianraschka` 0.25×, `buildfastwithai` 0.29×, `gemma` 0.31×, `a2a-protocol` 0.31×, `eugeneyan` 0.33×, `european-commission` 0.33×, `tether` 0.33×. Note `thinking-machines` was promoted only a week ago (07-20) and is now the coldest entry on the board — worth a look if it stays there.

## 👀 Watch list (138 uncovered orgs in the 3–7 mentions/30d band; top 15)

- **kaiko** — 7 / 1 src · vel 0.57× · health-ai (Swiss)
- **modal** — 7 / 6 src · vel 1.14× · serverless GPU infra — widest source spread on the watch list
- **mozilla** — 7 / 4 · vel 0.0× · open-source foundation
- **openrouter** — 7 / 3 · vel 0.0× · multi-provider gateway
- **schunk** — 7 / 2 · vel 0.0× · enterprise-adopter
- **v-zug** — 7 / 2 · vel 0.0× · Swiss enterprise-adopter
- **debian** — 6 / 2 · vel 4.0× · LLM-contribution General Resolution
- **japan-meti** — 6 / 2 · vel 0.0× · government agency
- **roche** — 6 / 4 · vel 2.0× · pharma adopter
- **airbus** — 5 / 2 · vel 3.2× · enterprise-adopter, blog URL present
- **black-forest-labs** — 5 / 2 · vel 4.0× · German frontier lab, verified `bfl.ai/blog`
- **general-catalyst** — 5 / 2 · vel 0.0× · VC
- **swarovski** — 5 / 2 · **new** — enterprise-adopter hiring an "AI Agent Factory Lead" that owns Responsible AI for the agent portfolio
- **swisslinx** — 4 / 2 · **new** — Swiss staffing firm placing bank-side AI-platform infrastructure roles
- **cnil** — 3 / 2 · **new** — French regulator; gains AI Act supervision and sanction powers on 2 August

`black-forest-labs` remains the one to watch — a real frontier lab with a verified blog URL. It needs two more active days to clear promotion thresholds with an applicable URL.

**Classification gap worth noting:** **open-secure-ai-alliance** landed at **9 mentions / 2 source types** on day one — *above* the watch ceiling (7) but below the promote bar (needs 3 source types and 4 distinct days), so it falls straight through to `dormant`. That is the rule working as written, not a bug, but a 40+ member alliance founded by NVIDIA and Microsoft being invisible to every tier on its launch day is the sort of thing the tiering was meant to catch. It will classify normally from tomorrow as its day count builds.

## ⚠️ Silent covered vendors (0)

No covered vendor is past its silence threshold this run. All 6 priority vendors surfaced in today's files; the most-silent enterprise entry is `qualcomm` at 26 days against a 60-day threshold.

_Collector-health note, **not** a silence signal:_ today's news collector again reported client-rendered or failing indexes for `openai.com` (recovered via fallback), `blog.google` / `deepmind.google`, `salesforce.com`, `servicenow.com`, `snowflake.com` and `workday`/`oracle`. None of those vendors is silent — their mentions arrive via third-party coverage, so `covered_silent` correctly does not fire. Repairing `blog_urls` on an existing entry is not one of this skill's six authorised change-set operations.

## Tally summary

- Orgs tracked: **1685**
- Covered (priority): **6** (openai, anthropic, google_deepmind, meta, mistral, deepseek)
- Covered (enterprise — incl. **46** auto-added): **54**
- Deep-watch (holding tier): **15** (unchanged in size; minimax in, ubs out)
- Uncovered / other: **1610**
- New orgs added to tally today: **8** — `safe-superintelligence` (SSI, ×10), `open-secure-ai-alliance` (×9, incl. the OSAIA abbreviation), `swarovski` (×5), `swisslinx` (×4), `cnil` (×3), `pro-informatik` (×3), `autoform` (×2), `vz-vermogenszentrum` (×2). Each clears ≥2 mentions in a single file and passes the journalist test.
- Deliberately **not** inserted: `enigma` (1 real mention — the second hit is its own URL slug), `epoch-ai` (real research institute, but today's only surface form is the bare word "Epoch", which would over-match ML text — waiting for a full-name mention rather than minting a polluting alias), `kaust` / `catl` / `institute-for-law-and-ai` (1× per file, below the bar), `swiss-ai-initiative` / `eth-ai-center` (sub-units of the already-tracked `eth-zurich`, per the "track the parent" rule), `graphify` / `harbor` / `mirrorcode` / `veracium` (products and benchmarks, not entities), and all GitHub-handle noise (`pbakaus`, `usestrix`, `Wei-Shaw`, `moeru-ai`, `mattpocock`).
- Hot events recorded today: **6 records across 2 orgs** (safe-superintelligence ×4, cxmt ×2).

### Out of scope for this skill — reported, not acted on

- **`github_collector.watched_repos` hygiene — 11 entries now 301-redirect, 2 of them outright duplicates double-counting stars** (`affaan-m/everything-claude-code` → `affaan-m/ECC`, both at 234,018; `openinterpreter/open-interpreter` → `openinterpreter/openinterpreter`, both at 67,343), plus nine rename-only redirects (`anthropics/anthropic-cookbook` → `claude-cookbooks`, `modelcontextprotocol/specification` → `modelcontextprotocol/modelcontextprotocol`, `ggerganov/llama.cpp` → `ggml-org/llama.cpp`, `hiyouga/LLaMA-Factory` → `hiyouga/LlamaFactory`, `joaomdmoura/CrewAI` → `crewAIInc/crewAI`, `All-Hands-AI/OpenHands` → `OpenHands/OpenHands`, `QwenLM/Qwen2.5` → `QwenLM/Qwen3`, `lobehub/lobe-chat` → `lobehub/lobehub`, `block/goose` → `aaif-goose/goose`). **Real, and the two duplicates are actively corrupting star totals — but `watched_repos` belongs to `ai-github-sweep`** (`sources.json.github.bak`, `github_changes.log`). This skill's authorised change set is six list operations over `enterprise_vendors` / `deep_watch_vendors` only; writing into `github_collector` from here would clobber the other sweep's rollback target and put its audit trail out of sync with its own state. Escalated unchanged for the second consecutive day — the redirect count has now gone 4 → 11.
- **Broken vendor feed URLs** (openai HTML indexes, blog.google/deepmind, salesforce, servicenow, snowflake, workday/oracle). `openai`/`google_deepmind` are `priority_vendors` (sacred); `servicenow`/`workday_oracle`/`salesforce` are manual `enterprise_vendors` entries (sacred). Editing `blog_urls` on an existing entry is not one of this skill's six change-set operations.
- Pre-existing tally duplicates observed but not merged (no authorised merge operation beyond aliasing): `sakana` / `sakana-ai`, `fireworks` / `fireworks-ai`, `red-hat` / `redhat` / `redhat_ibm`, `contextual` / `contextual-ai`, `monday` (weekday over-match).

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-07-26 → 2026-07-27)
- Today's source files (6): `news/`, `papers/`, `blogs/`, `jobs/`, `linkedin/`, `daily/` for 2026-07-27 — `## Sources scanned` methodology sections stripped before counting so WebSearch query dumps don't inflate mention counts or manufacture hot events. **138 orgs matched.**
- Hot-event keyword matching is word-bounded and gated on both a strong funding/M&A term **and** an allowlist of orgs the event is genuinely about; `valuation` is guarded against matching inside "evaluation".
- Radar JSONs in window: **30** (2026-06-26 → 2026-07-27); `breadth_orgs_7d` consumed — 451 distinct slugs seen, **159** new candidates all rejected as GitHub handles (`1jehuang`, `blaizzy`, `astrbotdevs`, `arc53`…), geography/blocs (`asean`, `african-union`, `arab-league`), or brand/slug variants (`arcprize` / `arc-prize`, `aaif-goose`) per the "when in doubt, skip" rule.
- sources.json snapshot before edit → sources.json.vendor.bak (**4 changes applied**: hot-event-add `safe-superintelligence`, expire-remove `spacex`, deep-watch-promote `ubs`, deep-watch-demote `minimax`).
