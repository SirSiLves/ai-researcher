# Vendor sweep — 2026-07-18

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back this run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

Enterprise vendor list: **64 → 51** (43 auto-added, 8 manual). Net of **−13 expired hot-event entries**, 0 adds, 0 deep-watch demotions. Deep-watch: **15 (unchanged)**. This is the large expiry batch the 07-17 run predicted — the list finally walks well below the overflow line.

- **Added 0 promotions.** No uncovered/deep-watch org cleared the sustained-promote gate (≥2 consecutive days at `promote`) *with a usable blog URL and a fresh mention today*. The strongest live candidate, **thinking-machines** (Inkling), is one distinct-day short of the promote threshold — see Pending.

- **Added 0 hot-event vendors.** **There is no genuine funding / M&A / IPO event in today's files** — the day's headlines are model releases (Inkling, Kimi K3), a TSMC earnings beat, and EU AI Act enforcement, none of which are vendor financing events. All 19 raw hot-keyword hits were false positives and were suppressed (see data-quality note). `microsoft` still classifies `hot_event` on its *stale* 07-03/07-07 entries but has no event dated today → held.

- **Removed 13 expired hot-event entries** (all `_auto_added: true`, `_expires_on 2026-07-17 < TODAY`):
  `aisi-uk`, `aleph-alpha`, `anysphere`, `cerebras`, `goldman-sachs`, `groq`, `moonshot`, `novo-nordisk`, `perplexity`, `sierra`, `tomoro`, `ubs`, `windsurf`.
  - Each reverts to `coverage: uncovered` in the tally. The genuinely-quiet ones (`sierra` last seen 05-27, `tomoro` 05-25, `novo-nordisk` 06-10, `aleph-alpha` 06-29) correctly drop off.
  - **`moonshot` is a notable case:** its 06-17 hot-event TTL lapsed *today*, so it is removed even though Kimi K3 is today's headline. This is by design — hot events are temporary. moonshot is highly active (9 distinct mention-days in the window, 3+ source types) so it will **re-promote automatically via the sustained-promote gate within ~2 days**, this time as a durable `promote` entry with no expiry. `cerebras`, `groq`, `perplexity`, `ubs`, `anysphere`, `aisi-uk`, `goldman-sachs` were all seen within the last ~5–11 days and will re-surface as `promote`/`watch` if activity continues.

- **Demoted 0 to deep-watch (51/30 cap — OVERFLOW regime, effective silence 14d).** The list is still over the 45 overflow threshold, but there were **no eligible demotion candidates**: every `_auto_added` entry silent ≥14 days is either being removed by expiry today, or shielded by a **live hot-event TTL** (`general-intuition` → 07-20, `isomorphic-labs` → 07-20, `tenstorrent` → 07-21, `qualcomm` → 07-27). The skill exempts within-TTL entries from overflow demotion. The cap keeps converging as those four TTLs lapse over 07-20 → 07-27.

- **Re-promoted 0 from deep-watch.** No deep-watch vendor hit a clean, fresh `hot_event` or sustained-`promote` today (see Held/rejected).

- **Skipped 0 silent vendors** — no priority/enterprise vendor is past its silence threshold (45d priority / 60d enterprise).

_Net: sources.json changed — **13 mutations** (13 expire-remove). enterprise_vendors 64 → 51; deep_watch_vendors unchanged at 15. `vendor_changes.json` consistency check: `log_only_not_in_sources` and `sources_only_not_in_log` both empty (43 auto in sources == 43 active per log; 15 deep-watch == 15)._

### Held / rejected (reported, NOT applied)

- **microsoft** (uncovered, hint present) — classifies `hot_event`, but only on **stale** entries (`billion` 07-03, `$2.5B` 07-07); its 07-09 TTL from those very events already expired, and today's single mention is an incidental "Microsoft Azure" blog byline. No event dated today → **held** (stale-hot-event guard). Same call as 07-17.
- **blackstone** (deep-watch, hint) — `hot_event` on the stale 07-16/07-17 Ode JV (as *investor*, not subject); **0 mentions today** → held on the investor-mention / fresh-activity guard, consistent with 07-15/16/17.
- **cognizant** (deep-watch, hint) — still classifies sustained-`promote` on a stale June cluster (last real mention **2026-06-27**), **0 mentions today**; demoted 07-11 for silence. **Held** on the fresh-activity guard — reviving on stale-window mentions would thrash. Self-clears when the 30-day window rolls past 06-27 (~2026-07-27).
- **figure**, **metr**, **uber** — classify `hot_event`/`promote` on stale within-window entries with **0 mentions today** → held.
- **~108 promote/hot-tier slugs rejected for a missing `blog_url_hint`** (Step C.5) — mostly tally pollution (protocols/products/media/author-name slugs: `a2a`, `mcp`, `claude`, `codex`, `arxiv`, `commission`, `chen`…), but including **real, promotable orgs worth a manual `blog_url_hint`**: **databricks** (10 mentions today, Databricks+NVIDIA agentic partnership), **apertus**, **bytedance**, **cern**, **crewai**, **oracle**, **intel**, **novartis**, **z-ai**, **ollama**, **gartner**, **eth-zurich**, **european-commission**. Add a `blog_url_hint` in `discovered_orgs.json` to make any of these auto-applicable.

### 🔧 Data-quality note — hot-event false positives suppressed (19 → 0)

Today's raw keyword scan produced **19** hot-event hits, **all false**. Word-boundary matching (fixed 07-17) is not enough when the money words appear in non-funding contexts. Added a **funding-context gate** this run:

1. **`billion`** fired on **model-parameter counts** — "41 **billion** active parameters", "975-**billion**-parameter" (Inkling/Kimi K3), and on the heise **URL slug** "234-**Billion**-US-Dollars…" (a Gartner revenue-at-risk analysis). Suppressed when a size context (`parameter`/`token`/`active`) is present or no funding cue accompanies the amount.
2. **`raised`** fired on **TSMC "raised 2026 revenue growth guidance … hiked capex"** — an earnings beat, not a financing round. Now requires `raised` to be followed by an actual money object (`raised $X` / `raised N million|billion`), rejecting "raised guidance/outlook/capex".
3. **`valuation`** fired on the **stale** "n8n: SAP strategic investment at $5.2B valuation **was May**" line and on "e**valuation**, permissions" (harness prose). Now rejected on stale-date cues and non-funding prose.

Effect: 0 hot events recorded to the tally today, so no stale funding noise is injected. `microsoft`/`blackstone` still carry historical hot_events from earlier runs; those are inert here (held by the fresh-event-today apply guard).

## ⏳ Pending — needs one more day at promote tier (0 applicable)

No org is one clean day away from an applicable auto-promotion.

- **thinking-machines** (Inkling) is the standout near-miss: **frontier-lab, `blog_url_hint` present, velocity 4.0×** (33 mentions/7d vs 8.25/wk), and it clears the mention (≥8) and source-type (≥3) bars easily — but it has only **3 distinct mention-days** (07-16, 07-17, 07-18) vs the **4** required, so it classifies `dormant`, not `promote`. One more distinct day of Inkling coverage and it enters the sustained-promote gate. Worth a manual promote if you want it covered now.

## ⚡ Velocity signals (0 surging, ~12 accelerating, 1337 cooling)

No vendor is `surging` (≥5.0×) → **no velocity-path hot events** this run. Notable `accelerating` (≥3.0×), restricted to orgs actually mentioned today:

- **thinking-machines** — 4.0× (33/7d vs 8.25/wk) · **frontier-lab, closest to promotion** (Inkling)
- **tsmc** — 4.0× (26/7d) · record Q2 print carried across news + daily
- **moonshot** — 3.62× (87/7d vs 24.0/wk) · Kimi K3 (just expired from enterprise; re-promotes via the sustained gate)
- **infosys** — 4.0× · Zürich agentic-AI consulting hiring (jobs slice)
- **rockstar-recruiting** — 4.0× · Swiss agentic-AI recruiter (jobs slice)
- **deloitte** — 3.2× · deep-watch, Swiss consulting hiring — not enough for re-promotion
- **white-house** — 4.0× · the rumored open-model executive order (Lambert essay)

`cooling` (≤0.33×) is informational only and never triggers a demote.

## 👀 Watch list (102 — top by recent mentions)

- **redis** — 7 mentions / 2 src types · velocity 2.86× · infra — **Iris** context-and-memory platform (auto-generates MCP tools; agent memory on Redis Flex); the day's RAG→runtime-context story. Has a `blog_url_hint` — a promote candidate if coverage sustains.
- **future-of-life-institute** — 7 / 2 · 4.0× · governance-body
- **hitachi-energy** — 7 / 1 · 4.0× · industrial (Baden-Dättwil GenAI hiring)
- **work-louder** — 7 / 2 · 4.0× · hardware (OpenAI Codex Micro partner)
- **vera-rubin** — 7 / 2 · 4.0× — NVIDIA platform slug, not a firm
- **japan-meti** — 6 / 2 · 4.0× · government-agency
- **kaiko** — 6 / 1 · 4.0× · health-ai
- **enisa** — 6 / 3 · governance body
- **artificialanalysis** — 6 / 2 · independent benchmark house
- Swiss/DACH cluster: **emil-frey**, **v-zug**, **schunk**, **bank-j-safra-sarasin**, **inspire-ag**, **knowit**
- Other: **crypto-finance**, **anomalyco**

## ⚠️ Silent covered vendors (0)

None — every priority/enterprise vendor has a mention within its silence window (45d priority / 60d enterprise). The soft-cap deep-watch path handles over-cap silence.

## Tally summary

- Orgs tracked: **1637** (+0 today)
- Covered (priority): 6
- Covered (enterprise — incl. 43 auto-added): 51
- Deep-watch (holding tier): 15
- Uncovered: 1565
- New orgs added to tally today: **0** — every named entity appearing ≥2× today was already tracked (`redis`, `calvin-risk`, `tsmc`, `databricks`, `apertus`, `deloitte`, `infosys`, `cern` all pre-existing). No genuinely new legal entity surfaced.
- Hot events recorded today: **0** (19 raw keyword hits, all false positives — suppressed by the funding-context gate)

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-07-17 → 2026-07-18)
- Today's source files: `news/`, `papers/`, `blogs/`, `jobs/`, `daily/` (2026-07-18) — 123 orgs matched. _`linkedin/` is a **stub** again (2 Chrome browsers connected in an unattended cron run — the recurring disambiguation case), so the `linkedin_network_post` source type contributed nothing; a standing drag on the `min_source_types` promotion bar._
- Radar JSONs in window: 30 (2026-06-19 → 2026-07-18). `breadth_orgs_7d` consumed: 113 distinct slugs, 33 unknown — **0 accepted** (all GitHub handles `nutlope`/`safishamsi`/`juliusbrussee`/`hkuds`…, products/OSS `lmstudio`/`lobehub`/`openinterpreter`/`boundaryml`, countries `china`/`south-korea`, generic bodies `african-union`/`arab-league`/`asean`, benchmarks `arc-prize`/`girai`, or the `thinkingmachines` duplicate). Borderline-real firms (`mindgard`, `bnetza`) skipped per the conservative "when in doubt, skip — the next radar re-surfaces it" rule.
- sources.json snapshot before edit → sources.json.vendor.bak (**13** changes applied)
