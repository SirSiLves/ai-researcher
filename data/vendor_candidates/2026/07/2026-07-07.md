# Vendor sweep — 2026-07-07

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed, and silent auto-added vendors over the soft cap are moved to deep-watch. This page describes what happened. To roll back this single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 0 promotion(s).** No uncovered org crossed the sustained-promote gate with a usable blog URL today (see rejected list below).
- **Added 0 hot-event vendor(s).** Every funding/M&A keyword hit today mapped to an already-covered vendor or a false trigger (details under _Hot-event scan_). No velocity surge (ratio ≥ 5.0) anywhere in the tally.
- **Removed 0 expired hot-event entry(s).** No `_auto_added` entry has `_expires_on < 2026-07-07` (the nearest, `microsoft`, expires 2026-07-08 — kept).
- **Re-promoted 1 from deep-watch:**
  - **llamaindex** — trigger: **sustained promote** (promote tier held 2 consecutive sweep rows: 2026-07-06 + 2026-07-07). Moved `deep_watch_vendors → enterprise_vendors`, carrying forward `blog_urls=["https://www.llamaindex.ai/blog"]`, `fallback_search`, and original reason; dropped `_demoted_on/_demoted_reason/_demoted_from`; refreshed `_added_on=2026-07-07`. Window: 31 mentions / 5 src types / 6 distinct days over 30d. This closes the loop from its 2026-06-27 overflow demotion.
- **Demoted 1 to deep-watch (67/30 cap, OVERFLOW regime → target 45, budget 5):**
  - **contextual-ai** — last mention 2026-06-23, silent **14 days**, `_auto_added: true`, no active TTL, not classified promote/hot today. Moved `enterprise_vendors → deep_watch_vendors` with `_demoted_reason="deep-watch (overflow): silent 14 days, 67/cap 30"`. Keeps `blog_urls=["https://contextual.ai/blog/"]` + `fallback_search` for a clean future re-promotion.
- **Skipped 0 silent vendor(s):** no silent vendor below the cap needed flagging (auto-demote stays disabled; the over-cap silent path is handled by deep-watch demotion above).

**Net effect:** `enterprise_vendors` 66 → 66 (+llamaindex, −contextual-ai); `deep_watch_vendors` 6 → 6 (−llamaindex, +contextual-ai). JSON validated on write.

### Why the soft-cap only demoted one entry
Post-adds count was 67 (> overflow threshold 45), so the OVERFLOW regime applied with a 14-day effective silence and a 5-entry budget. Only **contextual-ai** qualified: nearly every other silent `_auto_added` entry is still inside its hot-event `_expires_on` TTL (sierra 41d, tomoro 43d, novo-nordisk 27d, perplexity 27d, windsurf 22d, bmw 16d, ona 15d, isomorphic-labs 32d — all TTL-exempt per the rule that lets the TTL expire them instead), and the remaining non-TTL silences were < 14 days (tcs 13d, kpmg 11d, cognizant 10d, mckinsey 8d, …). **accenture** looked eligible against its stored last_seen (2026-06-23) but was mentioned in today's news (Microsoft Frontier Company partner), so its refreshed last_seen = 2026-07-07 removed it from the candidate set.

### Rejected — promote tier today but not applicable (no usable blog URL / covered parent)
These hit promote thresholds in the 30-day window but were **not** written to `sources.json`:
- **ollama** — 12 mentions / 3 src types / 5 days, but `blog_url_hint` is empty → `rejected_no_blog_url`.
- **z-ai** — 36 mentions / 3 src types / 4 days (today's ZCode/GLM-5.2 headline), but no `blog_url_hint` → `rejected_no_blog_url`. Also only its first promote-streak day, so the 2-day gate is unmet regardless.
- **databricks** — 19 mentions / 5 src types / 7 days, no `blog_url_hint` → `rejected_no_blog_url`.
- **ibm** — 69 mentions / 3 src types, but maps to the manually-covered `redhat_ibm` entry (sacred) and has no `blog_url_hint` → rejected.
- **oracle** — 40 mentions / 5 src types, but maps to the manually-covered `workday_oracle` entry (sacred) and has no `blog_url_hint` → rejected.
- **intel / ey / pwc** — real orgs at promote in the window but each has an empty `blog_url_hint` → `rejected_no_blog_url`.

### GitHub-collector suggestions (evaluated, not adopted)
Today's GitHub collector flagged `addyosmani/agent-skills` and `ComposioHQ/awesome-claude-skills` as watch-list adds and `anthropics/skills` as a re-promotion candidate (all top-5 trending). Applying this skill's own "what is an org" rules: these are **GitHub repositories**, not legal entities, so they are not vendor-list candidates here — the journalist test fails (`"addyosmani/agent-skills said today…"`). They belong to the GitHub-sweep state (`github_stars.json` / `github_changes.log`), and `anthropics/skills` "in deep_watch" refers to the GitHub collector's watch tiers, **not** `deep_watch_vendors`; the underlying vendor, **Anthropic**, is already a `priority_vendor`. The underlying real company **Composio** does appear in today's radar `agent-skills-wave` breadth, but it has no on-disk mentions today and stays uncovered/dormant in the tally (no action). The individual handles `addyosmani` and `obra` are people, not entities — left as-is.

## ⏳ Pending — needs one more day at promote tier (0)
No org is currently at promote tier with a usable blog URL and only 1 of the required 2 consecutive days. (The promote-tier orgs above are blocked on a missing blog URL, not on the sustained-day gate, so they are listed under Rejected rather than Pending.)

## ⚡ Velocity signals (0 accelerating, informational)
No org reached the `accelerating` (ratio ≥ 3.0) or `surging` (ratio ≥ 5.0) threshold on genuine, de-noised mentions this run. `llamaindex` sits at a mild upward drift (its re-promotion is driven by sustained promote-tier volume, not a velocity spike). No `cooling` flags of note among covered vendors.

## 👀 Watch list (11 shown of 141 raw; curated to recognizable entities)
Orgs trending upward but below promotion thresholds (uncovered, 3–7 mentions/30d). Raw list is dominated by substring/noise slugs; only real, nameable AI/enterprise entities shown.

- **atlassian** — 7 mentions / 3 src types · velocity 2.9× · MCP-EMA adopter (Atlassian MCP servers cited today)
- **citi** — 7 mentions / 3 src types · velocity 2.9× · enterprise-adopter (financial services)
- **crowdstrike** — 7 mentions / 2 src types · cybersecurity-edr
- **intuit** — 7 mentions / 3 src types · enterprise-adopter
- **suno** — 7 mentions / 2 src types · frontier-lab (audio; recently expired from enterprise list)
- **swiss-re** — 7 mentions / 2 src types · velocity 4.0× · Swiss enterprise-adopter
- **tesla** — 7 mentions / 2 src types · velocity 4.0×
- **a24** — 7 mentions / 2 src types · velocity 4.0× · media-entertainment
- **alibaba** — 6 mentions / 3 src types · velocity 2.7× (tracked separately from covered `alibaba-qwen`)
- **comparis** — 6 mentions / 3 src types · Swiss enterprise-adopter (hiring signal)
- **dxc** — 6 mentions / 2 src types · IT-services

## ⚠️ Silent covered vendors (0)
No covered vendor tripped the standalone silence flag below the soft cap this run. (Silent auto-added vendors over the cap are handled by deep-watch demotion — see above. Manual entries are never demoted and none are silence-flagged.)

## Hot-event scan (funding / M&A keyword co-occurrence)
Keyword hits found in today's files, and why none produced a new hot-event vendor:
- **Microsoft — "$2.5B" Frontier Company** → Microsoft is already covered (auto `enterprise_vendors`, TTL to 2026-07-08). Recorded on the tally's `hot_events` for history; not promotable (already covered).
- **Salesforce — "$1B" Switzerland** (and "acquisition" for Cimulate/Agentforce Commerce) → Salesforce is a **manual** `enterprise_vendors` entry (sacred). Recorded on tally `hot_events`; no list change.
- **Z.ai — "753-billion-parameter"** → false trigger; "billion" refers to model parameter count, not a funding/valuation event. No hot-event.
- Remaining "$1B / valuation / billion" hits were in boilerplate ("Sources scanned", "Theme balance") lines — ignored.

## Tally summary
- Orgs tracked: **1585**
- Covered (priority): 6
- Covered (enterprise — 58 auto-added of 66 list entries): 66 in list (72 tagged `coverage: enterprise` in tally, incl. historical drift)
- Covered (deep-watch holding tier): 6
- Covered (informal URL list — vendor_blogs 15, governance_sources 9): ~24
- Uncovered: 1503
- New orgs added to tally today: **0** (all of today's org mentions were already in the tally)
- Hot events recorded on tally today: **2** (microsoft, salesforce — both already covered)

## Sources scanned this run
- discovered_orgs.json (last_updated 2026-07-06 → 2026-07-07)
- Today's source files: news/2026/07/2026-07-07.md, papers/2026/07/2026-07-07.md, blogs/2026/07/2026-07-07.md, jobs/2026/07/2026-07-07.md, linkedin/2026/07/2026-07-07.md, daily/2026/07/2026-07-07.md
- Radar JSONs in window: 29 (2026-06-07 … 2026-07-07); today's `breadth_orgs_7d` consumed — no new real org passed the "what is an org" filter (composio already tracked; addyosmani/obra are individuals)
- sources.json snapshot before edit → sources.json.vendor.bak (**2** changes applied: 1 deep-watch-promote, 1 deep-watch-demote)
