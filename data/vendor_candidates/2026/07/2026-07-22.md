# Vendor sweep — 2026-07-22

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed, and the enterprise list is soft-capped via deep-watch demotion. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 1 promotion:**
  - **metr** — `blog_urls=["https://metr.org/blog"]`. 18 mentions / 6 source types / 8 distinct days over the 30-day window. Sustained-day proof: `promote` on **2026-07-21** and **2026-07-22** = 2/2 consecutive days, clearing `min_consecutive_days_at_promote`. Predicted by yesterday's log ("days at promote so far: 1/2"). METR (Model Evaluation & Threat Research) has been a standing presence in the eval-integrity thread and today's UK AISI eval-cheating item keeps it there. _Caveat for the collector: today's news run recorded `metr.org` RSS as 404 — the entry ships with `fallback_search` so the WebSearch path covers it._
- **Added 2 hot-event vendors** (TTL 30 days, `_expires_on: 2026-08-21`):
  - **amd** — trigger keyword `$5B`, from today's news file: _"AMD and Anthropic sign 2 GW Instinct MI450 partnership; **AMD to invest up to $5B**"_. `blog_urls=["https://ir.amd.com/news-events/press-releases"]`. This is the day's headline event and the first frontier-lab compute commitment at this scale that isn't NVIDIA; AMD jumped from 15 lifetime mentions to 56 (+41 today, velocity 3.81×). Flagged by the collector hand-off as a new actor — it clears the hot-event gate on its own merits.
  - **microsoft** — trigger keyword `billion`, from today's news file: _"**Microsoft deepens Mistral partnership**; Samsung weighs a billion-dollar stake … a billion-scale deal in which Microsoft strengthens its position with Mistral AI"_. `blog_urls=["https://microsoft.ai/news/"]`. Microsoft was expire-removed on 2026-07-09; this is a **fresh, today-dated** event, not a re-litigation of the old one, so the re-add is legitimate.
- **Removed 2 expired hot-event entries:**
  - **bmw** — `_expires_on: 2026-07-21` elapsed. Original trigger was the BMW i Ventures $300M agentic/physical-AI fund (added 06-21); no fresh event, last mention 2026-07-17 → back to `uncovered`.
  - **tenstorrent** — `_expires_on: 2026-07-21` elapsed. Original trigger was the Qualcomm $8–10B acquisition talks (added 06-21); silent 23 days → back to `uncovered`.
- **Demoted 0 to deep-watch (53/30 cap — OVERFLOW regime).** The list is over the 45 overflow threshold, so the silence bar drops to 14 days and the run had a budget of 5. Only four `_auto_added` entries are silent ≥14 days — **tenstorrent** (removed above by TTL), **qualcomm** (`_expires_on` 2026-07-27), **supabase** and **baseten** (both 2026-08-05). The last three sit inside live hot-event TTLs, which the skill exempts from overflow demotion ("let the TTL expire those instead"). **0 eligible candidates.** The cap converges as those TTLs lapse over the next fortnight.
- **Re-promoted 0 from deep-watch.** `blackstone` and `uber` both classify `hot_event` on the rolling window, but on **stale** keyword hits (2026-07-17 and 2026-07-03) with no mention today — see the freshness guard below. Held back rather than thrashing against demotions made 5 and 19 days ago.
- **Skipped 0 silent vendors.** No priority or enterprise vendor is past its silence threshold (45d priority / 60d enterprise) this run.

_Net: sources.json changed — **5 mutations**. enterprise_vendors **52 → 53** (45 auto-added, 8 manual); deep_watch_vendors unchanged at **15**._

### 🐛 Two correctness fixes applied to this run's gating

1. **Hot-keyword matching is now word-bounded.** The keyword scan was plain-substring, so `"valuation"` matched inside **`"evaluation"`** — 21 times in today's files alone. That manufactured a hot-event classification for **`aisi-uk`**, the UK *AI Safety Institute*, purely because today is an AI-**evaluation** news day. Word-bounded matching drops today's genuine `valuation` hits to 0 and removed 7 false hot-event candidates (`aisi-uk`, `anysphere`, `cognition`, `cursor`, `general-intuition`, `goldman-sachs`, `groq`) that would otherwise have been re-added on phantom triggers.
2. **Freshness guard on hot-event application.** §4's hot-event tier is window-based (any `hot_events` entry within 30 days), which means a slug whose TTL just expired instantly re-qualifies on the *same old event* — an add/remove ping-pong. The guard sits in the Step C.5 applicability filter (classification itself is unchanged): a hot event is only **applied** when its trigger was observed **today**, or when velocity is `surging` today. **41 slugs** were blocked this way, including the ones expire-removed on 07-18 (`anysphere`, `cognition`, `cursor`, `groq`, `moonshot`, `goldman-sachs`, `general-intuition`) and both deep-watch revival candidates. This matches the precedent set on 07-09 and 07-21.

## ⏳ Pending — needs one more day at promote tier (1 appliable)

Hit promote thresholds today but haven't yet held the tier for the required 2 consecutive days. Will auto-apply tomorrow if they stay at promote and keep a usable blog URL.

**ubs** — 23 mentions, 6 src types, days at promote so far: 1/2 · has `blog_url_hint`. Note the tension: UBS was deep-watch-demoted yesterday for 14-day silence, and is now climbing back on trailing-window volume. If it reaches 2/2 tomorrow it returns via the `deep-watch-promote` path (entry revived in place, not duplicated).

_Also at promote today but **unappliable** (no `blog_url_hint`): 8 more — `aisi`, `claude-cowork`, `gcp`, `hai`, `kumar`, `langgraph`, `lm-studio`, `prompt-injection`. Most are tally pollution (a product name, a protocol, a surname, a concept); `lm-studio` is the one real firm in the group and only needs a URL to become appliable._

_A further 73 slugs classify promote/hot on the rolling window but carry no blog URL and never reach `sources.json`. The genuine companies in that set are **databricks, bytedance, novartis, oracle, samsung, cern, eth-zurich, nous-research** — each would apply immediately if someone added a `blog_url_hint`. (`samsung` is today's near-miss: it clears the hot-event bar on the reported billion-dollar Mistral stake, but as the *investor* side of the deal with no vendor blog to fetch — same call as `aramco` on 07-09.)_

## ⚡ Velocity signals (0 surging, 67 accelerating, 6 cooling)

Posting-cadence changes, independent of tier. **Nothing reached the surging (≥5×) threshold**, so no velocity-path hot events this run.

**Accelerating (≥3×), top by 7-day volume:**
- **moonshot** — 3.74× (144 mentions/7d vs 38.5/wk baseline) · Kimi K3 plus the US distillation accusation
- **kimi** — 3.8× (132/7d vs 34.75/wk)
- **amd** — 3.81× (41/7d vs 10.75/wk) · auto-applied to hot_event above; the acceleration and the event agree
- **thinking-machines** — 4.0× (40/7d vs 10.0/wk)
- **prior-labs** — 4.0× (38/7d vs 9.5/wk) · SAP acquisition closed 17 Jul, still compounding
- **oracle** — 3.02× (37/7d vs 12.25/wk) · notable given `workday_oracle`'s blog fetch is dead (see below)
- **eth-zurich** — 3.1× (31/7d vs 10.0/wk) · two Swiss job postings this run
- **anaconda** — 4.0× (24/7d vs 6.0/wk)
- **lambert** — 3.17× (23/7d vs 7.25/wk)
- **tsmc** — 3.38× (22/7d vs 6.5/wk)
- **vera-rubin** — 4.0× (19/7d vs 4.75/wk) · NVIDIA product line, not an org (tally pollution)
- **bloomberg** — 3.09× (17/7d vs 5.5/wk) · publication, not a vendor

_Cooling (≤0.33×) with recent activity: 6 orgs — informational only, never a demote trigger._

## 👀 Watch list (118, showing top 20)

Uncovered orgs trending upward (3–7 mentions in 30d) but below promotion thresholds.

**work-louder** — 7 mentions / 2 src types · velocity 4.0× · hardware
**v-zug** — 7 mentions / 2 src types · velocity 0.0×
**unit8** — 7 mentions / 2 src types · velocity 4.0× · consultancy
**target** — 7 mentions / 5 src types · velocity 2.29×
**swissinfo** — 7 mentions / 2 src types · velocity 3.43×
**sonnet-4** — 7 mentions / 4 src types · velocity 1.14×
**schunk** — 7 mentions / 2 src types · velocity 0.0× · enterprise-adopter
**redis** — 7 mentions / 2 src types · velocity 2.86× · infra
**pentagon** — 7 mentions / 3 src types · velocity 0.0×
**openrouter** — 7 mentions / 3 src types · velocity 1.71×
**modal** — 7 mentions / 6 src types · velocity 1.14×
**jain** — 7 mentions / 4 src types · velocity 0.0×
**intel** — 7 mentions / 6 src types · velocity 0.0×
**hitachi-energy** — 7 mentions / 1 src type · velocity 4.0× · industrial
**emil-frey** — 7 mentions / 2 src types · velocity 0.57×
**ai-office** — 7 mentions / 4 src types · velocity 1.14×
**a24** — 7 mentions / 2 src types · velocity 0.0× · media-entertainment
**swyx** — 6 mentions / 2 src types · velocity 2.0×
**singh** — 6 mentions / 4 src types · velocity 3.33×
**simplyspeak** — 6 mentions / 1 src type · velocity 4.0× · vertical-agent

## ⚠️ Silent covered vendors (0)

_None this run — every priority and enterprise vendor has a mention inside its silence window._

### 🔌 Collector URL health — flagged, NOT auto-fixed (out of this skill's mandate)

Today's news collector reported five configured vendor URLs failing. **None of them triggered `covered_silent`**, because the silence gate measures *mentions in our corpus*, not *fetch success* — `meta`, `servicenow` and `workday_oracle` are all still being written about daily even though their blogs return nothing. That is the correct behaviour, but it means a permanently dead URL can hide indefinitely behind third-party coverage.

The auto-applier's sanctioned mutations are add/remove/deep-watch on `enterprise_vendors`. **Rewriting `blog_urls` on a manual or priority entry is explicitly forbidden** ("manual entries are sacred", "`priority_vendors` is sacred too"), so these are reported for manual action only:

| slug | list | symptom | working fallback found by the collector |
|---|---|---|---|
| `meta` | **priority** (sacred) | `ai.meta.com` HTTP 400 to server-side fetches, HTML **and** RSS | none — WebSearch fallback only; latest vendor posts stale since 9 Jul |
| `servicenow` | enterprise (manual) | press room + blog index both time out | none — fallback search returns only May 2026 items |
| `workday_oracle` | enterprise (manual) | `blog.workday.com` AI topic 404, `blogs.oracle.com` 403 | none |
| `salesforce` | enterprise (manual) | configured `/news/category/products/ai-features/` 404s | `https://www.salesforce.com/news/feed/` ✅ |
| `n8n` | enterprise (manual) | `n8n.io/blog/rss.xml` 404s | `https://blog.n8n.io/rss/` ✅ |

The last two are one-line config fixes with a verified working feed; the first three need a human decision (find a new URL or demote). `oracle` accelerating at 3.02× while `workday_oracle`'s fetch is dead is the sharpest illustration of the blind spot.

**Also out of scope:** the GitHub collector's report that `affaan-m/everything-claude-code` and `affaan-m/ECC` are the same repo double-counting ~232k stars. That's `github_collector.watched_repos`, owned by `ai-github-sweep` — this skill only mutates `news_collector.{enterprise,deep_watch}_vendors` and does not touch it.

## 🆕 Tally maintenance

- **New orgs added: 7**, all radar-breadth-derived and all filtered through the §3 "what is an org" rules: `mindgard` (AI security, UK), `browser-use` (agent tooling), `jina` (retrieval infra, Berlin), `bnetza` (Bundesnetzagentur — German federal regulator), `arcee` (open-model lab; also in today's news), `docdigitizer` (document AI), `opaque-systems` (confidential AI). All start `coverage: uncovered` with no `blog_url_hint` — none is appliable until a URL is recorded, which is the intended conservative default.
- **0 new orgs from today's source files.** Every named entity clearing the ≥2-mentions-in-one-file bar was already tracked (AMD, Samsung, Knowledge Lab AG, isolutions AG, CERN, ETH Zurich, Swiss Re, Novartis, Hugging Face, Future of Life Institute, HSG, Luware). Knowledge Lab AG and isolutions AG — the two new Swiss actors flagged in the collector hand-off — were already in the tally from earlier runs and simply took mention increments (+8 and +7).
- **~100 radar breadth slugs rejected** as non-orgs per §3: GitHub personal handles (`dietrichgebert`, `tirth8205`, `juliusbrussee`, `safishamsi`, `ryancodrai`, `1jehuang`, `ayghri`, …), projects rather than legal entities (`openinterpreter`, `agentwrapper`, `agentskills`), benchmarks (`arc-prize`), geography and blocs (`china`, `asean`, `african-union`, `arab-league`, `south-korea`), and 3-char over-matchers (`bis` — the BIS is real, but the token collides with the German word "bis" throughout our German-language sources).
- **6 alias merges** (prevents duplicate slugs and recovers lost counts): `huggingface` += "Hugging Face" (recovered **31** mentions that the space-less slug was missing — a covered enterprise vendor was being systematically undercounted), `redhat_ibm` += "Red Hat", `lm-studio` += "lmstudio", `together-ai` += "togetherai", `thinking-machines` += "thinkingmachines", `future-of-life-institute` += "FLI". The last four fold radar breadth variants into the canonical slug instead of creating duplicates.
- **1 `blog_url_hint` backfilled:** `amd` → `https://ir.amd.com/news-events/press-releases`, taken verbatim from the citation in today's own news file rather than invented.

## Tally summary

- Orgs tracked: **1,653** (+7)
- Covered (priority): **6**
- Covered (enterprise — incl. 45 auto-added): **53**
- Deep-watch (holding tier): **15**
- Uncovered: **1,579**
- New orgs added to tally today: **7**
- Hot events recorded today: **21** (2 applied, 19 on already-covered or unappliable slugs)

## Sources scanned this run

- `discovered_orgs.json` (last_updated 2026-07-21 → 2026-07-22)
- Today's source files: `news/`, `papers/`, `blogs/`, `jobs/`, `linkedin/`, `daily/` — all `2026/07/2026-07-22.md`
- Radar JSONs in window: **30** (2026-06-24 → 2026-07-22)
- `sources.json` snapshot before edit → `sources.json.vendor.bak` (**5** changes applied)
- Audit trail: 5 lines appended to `vendor_changes.log` (288 → 293)
