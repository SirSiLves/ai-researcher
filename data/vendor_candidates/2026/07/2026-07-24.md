# Vendor sweep — 2026-07-24

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

## 📋 What changed in sources.json today

- **Added 0 promotion(s).** No uncovered/deep-watch org cleared the sustained-promote gate (≥2 consecutive days at `promote`) _with a usable `blog_url_hint`_. The rolling-window promote list is real but hint-less — see the rejected list below.
- **Added 0 hot-event vendor(s).** Today's `IPO` / `files to go public` / `valuation` / `billion` language is all attached to already-covered or sacred names: **Anthropic**'s confidential draft S-1 (priority — never auto-added; recorded as a hot_event on the tally only), the _"can the stockmarket swallow Anthropic, SpaceX and OpenAI"_ mega-IPO HN thread (Anthropic/OpenAI priority; **spacex** already enterprise on a live TTL → 07-26), and **SAP**'s M&A / **ServiceNow**'s _"$1B AI ACV"_ + _"guidance raised"_ (both manual, covered). **China's PsiBot** (~$100M at a $1.48B valuation, "unicorn") is a genuine fresh round but appears only once per file (below the ≥2×-in-a-single-file bar), is flagged market-color by the collector, and has no `blog_url_hint` → not added to the tally.
- **Removed 0 expired hot-event entry(s).** The earliest live TTL is **reflection-ai** (`_expires_on` 2026-07-25); nothing has lapsed under the strict `_expires_on < TODAY` rule. (spacex→07-26, qualcomm→07-27, modular→07-29 lapse over the coming days.)
- **Demoted 1 to deep-watch (53/30 cap — OVERFLOW regime):**
  - **ubs** — `last_seen` 2026-07-07, **silent 17 days**. The list is over the overflow line (54 > 45 = cap×1.5), so the effective silence bar drops to 14 days; `ubs` is the only `_auto_added` entry silent ≥14d that is **not** shielded by a live hot-event TTL and **not** classified promote/hot today (it re-classifies `covered_healthy` now that it sits in `enterprise_vendors`). Moves to `deep_watch_vendors` keeping its `blog_urls`, `fallback_search`, and `_added_reason` for an exact re-promotion. **This is the tolerated oscillation the 07-23 run predicted:** `ubs` was re-promoted 07-23 on trailing-window volume (23 mentions all 06-27→07-07) with nothing fresh since 07-07, so a quiet 07-24 sends it straight back into the demotion queue.
- **Re-promoted 0 from deep-watch.** The two deep-watch entries that classify `hot_event` today — **blackstone** (stale 07-17 in-window event) and **uber** (stale 07-03) — have **0 fresh mentions today**, so the fresh-activity guard holds them rather than thrash a demotion on a stale signal.
- **Skipped 0 silent vendor(s).** No covered vendor is past its silence threshold (priority 45d / enterprise 60d): all 6 priority vendors were mentioned today; the most-silent enterprise entry is `qualcomm` at 23 days. Auto-demote stays disabled by default; the soft-cap deep-watch path (above) handles over-cap silence.

_Net: sources.json changed — **1 mutation** (deep-watch-demote `ubs`). `enterprise_vendors` **54 → 53** (45 auto, 8 manual); `deep_watch_vendors` **14 → 15**._

### Rejected — classified promote/hot but not applicable (no `blog_url_hint`) — Step C.5

These crossed a promote/hot threshold on the 30-day window but have no blog URL for the news collector to fetch, so they can't be applied. Not logged as mutations.

- **cern** (promote — 6 consecutive days, ~181 mentions/30d) — research org, hiring-driven signal (careers.cern), no vendor blog.
- **european-commission** (promote — 6 consecutive days) — regulatory body; no fetchable vendor blog. Track "European Commission" as one org, not `commission`/`consilium`/`europarl`.
- **bytedance** (promote) — real firm awaiting a `blog_url_hint`.
- **nous-research** (promote) — real lab awaiting a `blog_url_hint`.

_(A larger set of `promote`/`hot` slugs is historical tally pollution — media outlets, people, products, protocol/brand names like `mcp`, `codex`, `gemini`, `linkedin`, `slack`, `swe-bench`, `agentforce`, `z-ai` — that classify on stray substring matches. They have no blog hint and are never applied.)_

### Held by the fresh-activity guard (hot_event on stale in-window signal, 0 mentions today)

- **moonshot** — classifies `hot_event`, has a blog (`platform.moonshot.cn`), and Kimi K3's weight-freeing (27 Jul) is in today's news — but a model-weights release is **not** a funding/M&A/IPO event, its only in-window hot_events are stale, and velocity (2.99×) is not surging → held, consistent with every run since 07-18.
- **blackstone**, **uber** — deep-watch, stale hot_events, 0 fresh mentions → held.

## ⏳ Pending — needs one more day at promote tier (0)

No org is one sustained-day away from an _applicable_ auto-promotion. A valid candidate needs a real `blog_url_hint` **and** a fresh mention; the only blog-bearing org that recently sat at `promote` was `ubs`, which is now covered (and demoted). The rest of the rolling-window promote list is hint-less pollution or already-covered brands.

## ⚡ Velocity signals (accelerating: single-day bursts; cooling: 4)

No vendor is **surging** (ratio ≥ 5.0), so nothing was velocity-promoted to `hot_event`. The `accelerating` (ratio ≥ 3.0) bucket is dominated by orgs whose ratio pins at exactly 4.0 — one active day against a near-empty 4-week baseline — so it is mostly noise today. The more plausible real-org movers:

- **vercel** — 4.0× (6/7d vs 1.5/wk) · from the "Agent Platform Bake-Off" LinkedIn piece; informational.
- **unit8** — 4.0× (8/7d) · Swiss Data & AI consultancy; jobs-driven.
- **redis** — 2.86× · **openrouter** — 1.71× · **intel** / **roche** / **hitachi-energy** — infra/enterprise-adopter chatter.
- **stanford-hai** — 4.0× · governance/research mentions (in `governance_sources`, not a vendor).

**Cooling (ratio ≤ 0.33, informational — not a demote trigger):** `zhipu` 0.16×, `modular` 0.29×, `spacex` 0.31×, `tether` 0.33×.

## 👀 Watch list (104 uncovered orgs in the 3–7 mentions/30d band; top 15)

Trending upward but below promotion thresholds. Capped at 20.

- **redis** — 7 mentions / 2 src types · vel 2.86× · infra
- **openrouter** — 7 / 3 · vel 1.71× · —
- **intel** — 7 / 6 · vel 0.0× · —
- **schunk** — 7 / 2 · vel 0.0× · enterprise-adopter
- **hitachi-energy** — 7 / 1 · vel 1.71× · industrial
- **swissinfo** — 7 / 2 · vel 2.29× · —
- **v-zug** — 7 / 2 · vel 0.0× · —
- **sonnet-4** — 7 / 4 · vel 1.14× · (Anthropic product slug — brand→parent, not promotable)
- **roche** — 6 / 4 · vel 2.0× · pharma adopter
- **vercel** — 6 / 3 · vel 4.0× · —
- **openrouter / redis / intel / roche** anchor the genuine infra-adopter signal; the rest of the band is Swiss enterprise-adopter and media noise.

## ⚠️ Silent covered vendors (0)

No covered vendor is past its silence threshold this run (priority 45d / enterprise 60d). All 6 priority vendors surfaced today; the most-silent enterprise entry is `qualcomm` (23 days, well inside its 60-day window and shielded by a live hot-event TTL → 07-27).

## Tally summary

- Orgs tracked: **1658**
- Covered (priority): **6** (openai, anthropic, google_deepmind, meta, mistral, deepseek)
- Covered (enterprise — incl. **45** auto-added): **53**
- Deep-watch (holding tier): **15** (ubs added this run)
- Uncovered / other: **1584**
- New orgs added to tally today: **0** — no ≥2×-in-a-single-file entity was both new and org-like (PsiBot skipped: 1×/file + market-color + no blog), and all ~103 radar `breadth_orgs_7d` candidates were rejected as GitHub handles/repos (jcodesmore, composiohq, tabbyml…), geography (china, usa, asean…), or brand/slug variants (togetherai, thinkingmachines, lmstudio) per the "when in doubt, skip" rule.
- Hot events recorded today: **1** (anthropic — confidential draft S-1 / IPO; priority, informational only)

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-07-23 → 2026-07-24)
- Today's source files: news, papers, blogs, jobs, linkedin, daily (2026-07-24) — `## Sources scanned` methodology sections stripped before counting so WebSearch query dumps (e.g. news' _"…Thinking Machines Reflection xAI"_, _"SAP Salesforce ServiceNow"_) don't inflate mention counts or manufacture hot events.
- Radar JSONs in window: 29 (2026-06-26 → 2026-07-24; `breadth_orgs_7d` consumed and filtered).
- sources.json snapshot before edit → sources.json.vendor.bak (**1 change applied**: deep-watch-demote `ubs`).
