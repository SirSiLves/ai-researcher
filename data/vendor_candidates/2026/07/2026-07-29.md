# Vendor sweep — 2026-07-29

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

> **Two-day catch-up.** The 2026-07-28 sweep never ran (the synthesis stage did not complete), so `discovered_orgs.json` was last updated 2026-07-27. This run tallied **both** 2026-07-28 and 2026-07-29 source files. 07-28 has no `linkedin/` or `daily/` file, so its slice is news + papers + blogs + jobs only.

## 📋 What changed in sources.json today

**Added 1 hot-event vendor:**

- **`cyera`** — trigger `acquisition` / `to acquire` / `billion` · expires **2026-08-28** (TTL 30d) · `blog_urls=["https://www.cyera.com/blog"]`
  _"Cyera to acquire Oasis Security for $1B to police proliferating AI agents — Cyera's third acquisition this year, aimed squarely at non-human identity: the credentials, tokens and service accounts that agents accumulate and that no existing IAM stack was designed to govern."_ (news/2026/07/2026-07-29.md, daily/2026/07/2026-07-29.md)

**Removed 1 expired hot-event entry:**

- **`qualcomm`** — `_expires_on: 2026-07-27` elapsed (2 days overdue because no sweep ran on 07-28). Last mention 2026-07-01, 28 days silent. The Snapdragon/AI200 datacentre-inference story that drove the 06-27 insertion did not sustain.

**Re-promoted 1 from deep-watch:**

- **`deloitte`** — returned via **sustained promote** (promote tier on the 07-27 run and again today; the 07-28 run did not execute, so these are two consecutive *sweep runs*, which is what `auto_promote.min_consecutive_days_at_promote: 2` gates on). 8 mentions / 4 source types / 6 distinct days over 30d. Keeps its original `blog_urls`, `fallback_search` and `_added_reason` from before the 2026-06-20 demotion.

**Demoted 0 to deep-watch (55/30 cap — overflow regime active, no eligible candidates):**

The list sits at 55 against a soft cap of 30 and an overflow threshold of 45, so the **overflow** regime applied (effective silence 14 days, walk-down target 45, budget 5). Zero entries qualified:

- `ubs` (the only non-TTL entry that was 22 days silent going in) was **re-mentioned in today's jobs slice** — last_seen refreshed to 2026-07-29, silence reset to 0. That ends the 07-21 → 07-27 demote/re-promote oscillation for now.
- Every other auto-added entry at ≥14 days of silence is **TTL-shielded** (a hot-event entry inside its `_expires_on` window is exempt by rule — the TTL removes it, not the cap): `baseten` (23d, exp 08-05), `supabase` (23d, exp 08-05), `prime-intellect` (18d, exp 08-09), `together-ai` (17d, exp 08-02), `sambanova` (16d, exp 08-09), `manus` (15d, exp 08-13), `unitree` (14d, exp 08-14).

Structural note: seven of the nine over-cap slots are held by hot-event TTLs that unwind between 2026-08-02 and 2026-08-14. The cap will converge on its own as those expire; no rule change needed.

**Skipped 0 silent vendors:** no `enterprise_vendors` or `priority_vendors` entry is past its silence threshold (45d priority / 60d enterprise). `covered_silent` was empty this run.

**Held (hot-event tier, not applied):**

- **`oasis-security`** — the acquisition *target*. Recorded in the tally with its hot-event entry, but deliberately **not** added to `enterprise_vendors`: once absorbed it stops being an independent publishing source and the acquirer's feed carries the story. Judgment call, flagged here for visibility.
- **`recursive-superintelligence`** — Richard Socher's lab committed $410M of the ~$650M it has raised to an AWS multiyear compute deal (news 07-28). Genuine capital event, but the tally has **no `blog_url_hint`** for it, so Step C.5 rejected the application (`rejected_no_blog_url`). Add a URL to `discovered_orgs.json` if you want this one applied on a future surge.
- 25 further slugs sat at `hot_event` tier on a *stale* in-window hot event with no fresh funding signal this run (`cxmt`, `cognition`, `cursor`, `reflection-ai`, `spacex`, `tenstorrent`, `blackstone`, `goldman-sachs`, `uber`, `moonshot`, …). Held by design — the freshness gate stops a 3-week-old funding round from re-adding a vendor every day.

## ⏳ Pending — needs one more run at promote tier (2)

Orgs at promote thresholds today that have not yet held the tier for 2 consecutive sweep runs. Will auto-apply on the next run if they stay at promote.

- **`palantir`** — 8 mentions, 4 src types, days at promote so far: 1/2 · `https://blog.palantir.com` (currently deep-watch; would return via the re-promotion path)
- **`perplexity`** — 8 mentions, 4 src types, days at promote so far: 1/2 · `https://www.perplexity.ai/hub`

_(A further ~90 slugs technically hit promote thresholds but are legacy tally pollution — products, protocols, benchmarks, surnames, media outlets: `azure`, `gpt-5`, `a2a`, `eu-ai-act`, `karpathy`, `linkedin`, `commission`… They carry no `blog_url_hint` and are rejected before logging. A tally-cleanup pass on these is the standing backlog item.)_

## ⚡ Velocity signals (30+ accelerating, 5 cooling with activity)

**Accelerating (ratio ≥ 3.0 — informational, watch tomorrow):**

- **`vercel`** — 4.0× (13 mentions/7d vs 3.25/wk baseline)
- **`cognizant`** — 4.0× (12 vs 3.0) · currently deep-watch; one more good day puts it at promote
- **`odyssey`** — 4.0× (12 vs 3.0) · new org today, Zurich world-models lab hiring 7 roles
- **`deepseek-v4-pro`** / **`deepseek-v4-flash`** — 4.0× (11 / 8) · model slugs, parent already priority
- **`safe-superintelligence`** — 4.0× (11 vs 2.75) · applied as hot-event 07-27, still climbing
- **`vector8`** — 4.0× (11 vs 2.75) · new org, Swiss banking-AI consultancy, 6 AI roles incl. a pure governance seat
- **`exeon-analytics`**, **`georgia-power`**, **`open-secure-ai-alliance`**, **`swarovski`**, **`treasury`** — 4.0× (10 each)
- **`ostp`**, **`pictet`** — 4.0× (9) · **`owasp`**, **`typeform`**, **`jfrog`** — 4.0× (8) · **`cxmt`** — 4.0× (7)

No org reached `surging` (≥5.0) on the velocity path this run, so the only hot-event insertion came from the keyword path.

**Cooling (ratio ≤ 0.33 — informational, never a demote trigger):**
`google-deepmind` 0.18× · `xai` 0.25× · `thinking-machines` 0.28× · `buildfastwithai` 0.29× · `tether` 0.33×

## 👀 Watch list (157 total, top 20 shown)

Orgs trending upward but below promotion thresholds.

- **`chase`** — 7 mentions / 4 src types · velocity 2.86×
- **`mozilla`** — 7 / 4 · 0.0× · open-source-browser
- **`openrouter`** — 7 / 3 · 0.0×
- **`naver`** — 7 / 2 · 0.0×
- **`schunk`** — 7 / 2 · 0.0× · enterprise-adopter
- **`v-zug`** — 7 / 2 · 0.0×
- **`work-louder`** — 7 / 2 · 0.0× · hardware
- **`swissinfo`** — 7 / 2 · 0.57×
- **`matt-pocock`**, **`swyx`** — 7 mentions, single/dual source type (individual authors, not firms — tally-cleanup candidates)
- **`debian`** — 6 / 2 · 4.0× · open-source-foundation
- **`codeberg`** — 6 / 1 · 4.0× · **new today**, banned predominantly AI-generated projects at its 2026 general assembly
- **`pro-informatik`** — 6 / 2 · 4.0× · services-si (CH)
- **`roche`** — 6 / 4 · 0.8× · **`redis`** — 6 / 3 · 0.67× · **`enisa`** — 6 / 3 · 0.0×
- **`japan-meti`** — 6 / 2 · 0.0× · government-agency
- **`gemini-enterprise`** — 6 / 3 · 2.0× (product slug → Google DeepMind)
- **`figure`** — 6 / 6 · 3.33× · vertical-agent (six source types, one mention short of promote)
- **`theregister`**, **`tomshardware`** — 6 each (media outlets, tally-cleanup candidates)

## 🆕 New orgs added to the tally (14)

Named legal entities appearing ≥2× in a single file, passing the journalist test:

| slug | why | blog hint |
|---|---|---|
| `cyera` | $1B acquisition of Oasis Security (applied as hot-event) | ✅ |
| `oasis-security` | acquisition target, non-human identity | — |
| `jfrog` | Artifactory zero-day at the centre of the frontier-lab agent intrusion (8 CVEs) | ✅ |
| `kiteworks` | source of the 2026 agentic-containment survey (n=225) quoted in blogs | ✅ |
| `codeberg` | Berlin non-profit (e.V.) banning predominantly AI-generated projects | ✅ |
| `pjm-interconnection` | largest US grid operator, will curtail power to data centres | ✅ |
| `swiss-national-bank` | central bank hiring into ML/NLP | ✅ |
| `vector8` | Swiss banking-AI consultancy, 6 AI roles | — |
| `odyssey` | Zurich world-models lab, 7 roles | — |
| `fore-ai` | Zurich GenAI-evaluation startup, 4 roles | — |
| `logicstar` | Zurich autonomous-bug-fixing startup, 2 roles | — |
| `docpier` | Lucerne AI engineering employer | — |
| `alcon` | first "AI Compliance" in a Swiss QA job title | — |
| `elca` | Swiss SI, agentic-AI engineering roles | — |

Swiss employer-layer entries are deliberately left without a `blog_url_hint` — they cannot be auto-applied until a real URL is confirmed, which is the intended Step C.5 brake.

Alias maintenance: `recursive-superintelligence` gained the alias `"Recursive Superintelligence"` (the slug alone was not matching the spelled-out name).

Rejected as non-orgs this run: `OKF` (knowledge format), `Harbor`, `MAI-Cyber-1-Flash`, `Kimi-K3`, `GLM-5.2`, `Fireworks Nexus`, `Cortex AI Gateway`, `Deep Agents`, `Tool Search`, `Managed Agents`, `ChatGPT Work`, `SafeFlow`, `COVENANT`, `ACORN`, `CodeNib`, `SpecBox`, `MemTX`, `MTGuard`, `AIR-BENCH Live` (papers/systems/products), `Ars Technica`, `InfoQ`, `Golem`, `Black Hat` (media/events), `Lobby` (real Zurich startup, but the alias is too generic a common word to match safely — will re-surface). Plus **199 of 506** radar `breadth_orgs_7d` slugs rejected by the standing filter (suffix / brand→parent / junk-word / too-short).

## ⚠️ Silent covered vendors (0)

None. Every priority and enterprise vendor has a mention inside its silence threshold.

## Tally summary

- Orgs tracked: **1699** (+14 today)
- Covered (priority): 6
- Covered (enterprise — incl. 47 auto-added of 55): 55
- Deep-watch: 14
- Covered (informal URL list): 0
- Uncovered: 1623
- New orgs added to tally today: 14
- Hot events recorded today: 7 records across 3 orgs (`cyera`, `oasis-security`, `recursive-superintelligence`)
- Orgs whose counters moved this run: 238

## 🔧 Hygiene noted, not actioned (out of this skill's scope)

Today's GitHub collector reported two `github_collector.watched_repos` issues. This skill's auto-apply rules cover `news_collector` vendor lists only, so nothing was changed — routing to `ai-github-sweep`:

1. **11 of 133 watched repos are stale names resolving via 301**: `All-Hands-AI/OpenHands`→`OpenHands/OpenHands`, `QwenLM/Qwen2.5`→`QwenLM/Qwen3`, `anthropics/anthropic-cookbook`→`anthropics/claude-cookbooks`, `block/goose`→`aaif-goose/goose`, `ggerganov/llama.cpp`→`ggml-org/llama.cpp`, `hiyouga/LLaMA-Factory`→`hiyouga/LlamaFactory`, `joaomdmoura/CrewAI`→`crewAIInc/crewAI`, `lobehub/lobe-chat`→`lobehub/lobehub`, `modelcontextprotocol/specification`→`modelcontextprotocol/modelcontextprotocol`, `openinterpreter/open-interpreter`→`openinterpreter/openinterpreter`, `affaan-m/everything-claude-code`→`affaan-m/ECC`.
2. **Two duplicate pairs double-count stars**: `affaan-m/everything-claude-code` + `affaan-m/ECC`, and `openinterpreter/open-interpreter` + `openinterpreter/openinterpreter`. Effective distinct repo count is **131**, not 133.

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-07-27 → 2026-07-29)
- Source files (10, two days): news/papers/blogs/jobs 2026-07-28 + news/papers/blogs/jobs/linkedin/daily 2026-07-29
- Radar JSONs in window: 30 (506 distinct `breadth_orgs_7d` slugs seen)
- sources.json snapshot before edit → sources.json.vendor.bak (3 changes applied: +1 hot-event, −1 expired, +1 deep-watch re-promotion)
