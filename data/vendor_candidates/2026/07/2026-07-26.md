# Vendor sweep — 2026-07-26

_Daily change log. The pipeline auto-maintains `sources.json` — promotions and hot events are added directly, expired hot events are removed. This page describes what happened. To roll back a single run: `cp ../pipeline/state/sources.json.vendor.bak ../pipeline/state/sources.json`._

> **Catch-up run.** 2026-07-25 was never swept — there is no `vendor_candidates/2026/07/2026-07-25.md` and `discovered_orgs.json.last_updated` was still `2026-07-24` (a sweep script was written that day but never applied). This run therefore folds in **both** the 07-25 and 07-26 source files, so the rolling tally has no missing day. The two funding rounds applied below are 07-25 events surfacing one day late.

## 📋 What changed in sources.json today

- **Added 0 promotion(s).** No uncovered/deep-watch org cleared the sustained-promote gate (≥2 consecutive days at `promote`) _with a usable `blog_url_hint`_. The one applicable candidate, **ubs**, is at day 1 of 2 — see Pending.
- **Added 2 hot-event vendor(s)** — both genuine, correctly-attributable funding rounds from the missed 07-25 cycle:
  - **etched** — `blog_urls=["https://www.etched.com"]`, expires **2026-08-25**. Trigger `valuation` / `Series` — _"Etched raises $300M at a $10.3B valuation"_ (TechCrunch via Tech Startups, 24 Jul). The Harvard-dropout startup building transformer-only inference ASICs; the round is the clearest bet yet that specialized silicon undercuts NVIDIA on inference economics. 3 mentions / 2 source types.
  - **norm-ai** — `blog_urls=["https://norm.ai"]`, expires **2026-08-25**. Trigger `Series C` — _"Norm AI closed a $120M round led by Khosla Ventures (with Blackstone, Vanguard, Bain Capital Ventures, Coatue, Craft)"_. Turns regulatory rules directly into autonomous compliance agents — governance-as-agents becoming a fundable enterprise category is exactly the intersection this pipeline tracks. 5 mentions / 2 source types.
- **Removed 1 expired hot-event entry:** **reflection-ai** (`_expires_on` 2026-07-25 < today). TTL elapsed without crossing sustained-promote thresholds in the meantime, so the entry lapses cleanly.
- **Demoted 0 to deep-watch (54/30 cap — OVERFLOW regime fired, 0 eligible candidates).** See the note below — this is the interesting finding of the run.
- **Re-promoted 0 from deep-watch.** Eleven deep-watch/uncovered slugs classify `hot_event` on stale in-window signals; all are held by the fresh-activity guard (below).
- **Skipped 0 silent vendor(s).** No covered vendor is past its silence threshold (priority 45d / enterprise 60d). All 6 priority vendors were mentioned today; the most-silent enterprise entry is `qualcomm` at 25 days.

_Net: sources.json changed — **3 mutations** (+2 hot-event adds, −1 expiry). `enterprise_vendors` **53 → 54** (46 auto, 8 manual); `deep_watch_vendors` unchanged at **15**._

### ⚠️ The soft cap did not converge this run — every eligible entry is TTL-shielded

The list is at 54 against `max_enterprise_vendors: 30`, past the overflow line (54 > 45 = cap×1.5), so the **overflow** regime engaged with the relaxed 14-day silence bar and a budget of 5. It found **zero** candidates. Every `_auto_added` entry silent ≥14 days is currently shielded by a live hot-event `_expires_on` (Step C.6 condition 4 — let the TTL expire those rather than deep-watching them early):

| slug | silent | TTL shield |
|---|---:|---|
| qualcomm | 25d | 2026-07-27 |
| supabase | 20d | 2026-08-05 |
| baseten | 20d | 2026-08-05 |
| prime-intellect | 15d | 2026-08-09 |
| together-ai | 14d | 2026-08-02 |

The next non-shielded entry is `sambanova` at 13 days — one day short of the bar. **The TTL shield, not the silence window, is now the binding constraint on cap convergence.** The queue unblocks as TTLs lapse: `spacex` (07-27), `qualcomm` (07-28), `modular` (07-30), `together-ai` (08-03), `supabase`/`baseten` (08-06), `prime-intellect`/`sambanova` (08-10). Expect demotions to resume mid-week rather than today. No rule was bent to force convergence.

### Rejected — classified hot/promote but not applicable (no `blog_url_hint`) — Step C.5

- **cxmt** (hot_event, **new to the tally today**) — Changxin Memory Technologies is heading for a record IPO (`IPO` keyword, 3 mentions in `news/2026-07-26.md`). A US-blacklisted DRAM/HBM maker getting a large domestic war chest matters because memory, not logic, is the tight link in the AI supply chain — but there is no fetchable English vendor blog, so the entry would be useless to the news collector. Recorded on the tally; not applied.
- Standing hint-less `promote` set, unchanged in character from prior runs: **bytedance**, **nous-research**, **giotto**, **cern**, **cscs**, **apertus**, **artificial-analysis**, **future-of-life-institute**, **workday**, **box**, **lm-studio** — real entities awaiting a blog URL — plus the historical tally pollution (`agentforce`, `linkedin`, `slack`, `postgres`, `modelcontextprotocol`, `prompt-injection`, `hai`, `axios`, `latent-space`, `singh`, `claude-cowork`) that classifies on stray substring matches and is never applied.

### Held by the fresh-activity guard (hot_event on stale in-window signal, no fresh funding this run)

**moonshot** (v7=143, ratio 2.39× — Kimi K3's weight drop is a model release, not a funding/M&A event), **cognition**, **cursor**, **blackstone** _(deep-watch)_, **uber** _(deep-watch)_, **groq**, **tenstorrent**, **anysphere**, **goldman-sachs**, **general-intuition**, **aisi-uk**. Also **georgia-power** — classifies `hot_event` purely on a 4.0× velocity reading against a near-empty baseline; no funding event, held.

### Explicitly **not** treated as hot events today

- **Samsung ↔ Broadcom ~$200B HBM4 / 2nm turnkey pact** — the largest number in the cycle, but a commercial supply MOU, not funding or M&A. Bare `billion` does not clear the strong-funding gate. (`broadcom` is already enterprise; `samsung` remains uncovered.)
- **Salesforce $1.6B US Department of Veterans Affairs contract** — a contract win. Salesforce is a manual enterprise entry and is sacred either way.
- **Alphabet's first negative FCF / ~$200B capex, Intel's raised outlook** — capex and guidance, not corporate funding rounds.
- **White House >$5B Genesis Mission** — a government programme.
- **Prentis** (new lab from Reid Hoffman and Mark Pincus) — _"in talks to raise $100M"_. An unclosed round; added to the tally, no hot event.

## ⏳ Pending — needs one more day at promote tier (1)

**ubs** — 23 mentions, 6 src types, 4 distinct days over 30d · `blog_url_hint` present · days at promote so far: **1/2**. Currently in `deep_watch_vendors` (demoted 07-24 in the overflow regime), so a second consecutive promote day triggers `deep-watch-promote`, not a fresh add. Caveat carried forward from 07-23: ubs's window volume is trailing (nothing fresh since 2026-07-07), so this is the same oscillation the last three runs flagged — it will bounce back to the demotion queue if activity stays flat.

## ⚡ Velocity signals (0 surging, ~70 accelerating, 4 cooling)

No vendor is **surging** (ratio ≥ 5.0), so nothing was velocity-promoted to `hot_event`. The `accelerating` bucket is again dominated by orgs pinned at exactly 4.0× — one active day against a near-empty 4-week baseline — which is arithmetic, not signal. The movers with real mass behind them:

- **prior-labs** — 4.0× (39/7d vs 9.75/wk) · enterprise, live TTL; SAP's >€1B acquisition still compounding.
- **amd** — 3.87× (59/7d vs 15.25/wk) · enterprise; Cerebras mixed-silicon inference deal plus the Anthropic investment.
- **fireworks-ai** — 4.0× (16/7d vs 4.0/wk) · enterprise; post-Series-D coverage.
- **cisco** — 3.64× (10/7d vs 2.75/wk) · enterprise; the ARD spec coalition.
- **poolside** — 3.56× (16/7d) · **vercel** — 4.0× (13/7d) · **unit8** — 4.0× (8/7d, Swiss consultancy, jobs-driven) · **stanford-hai** — 4.0× (11/7d, governance source, not a vendor).
- New entrants land at 4.0× by construction (empty baseline): `black-forest-labs`, `kuaishou`, `debian`, `scaleway`, `datalab`, `exeon-analytics`, `norm-ai`, `cxmt`.

**Cooling (ratio ≤ 0.33, informational — not a demote trigger):** `zhipu` 0.19×, `buildfastwithai` 0.29×, `infosys` 0.33×, `tether` 0.33×.

## 👀 Watch list (136 uncovered orgs in the 3–7 mentions/30d band; top 15)

- **openrouter** — 7 / 3 src · vel 0.0× · multi-provider gateway
- **redis** — 7 / 2 · vel 0.0× · infra
- **mozilla** — 7 / 4 · vel 0.0× · open-source-browser
- **kaiko** — 7 / 1 · vel 0.57× · health-ai (Swiss)
- **schunk** — 7 / 2 · vel 0.0× · enterprise-adopter
- **v-zug** — 7 / 2 · vel 0.0× · Swiss enterprise-adopter
- **ostp** — 7 / 3 · vel 4.0× · US policy body (Genesis Mission)
- **debian** — 6 / 2 · vel 4.0× · **new** — LLM-contribution General Resolution
- **gemini-enterprise** — 6 / 3 · vel 2.0× · (Google brand → parent, not promotable)
- **roche** — 6 / 4 · vel 2.0× · pharma adopter
- **japan-meti** — 6 / 2 · vel 0.0× · government agency
- **exeon-analytics** — 6 / 1 · vel 4.0× · **new** — Swiss NDR/UEBA security
- **black-forest-labs** — 5 / 2 · vel 4.0× · **new** — German frontier lab (FLUX 3)
- **kuaishou** — 5 / 2 · vel 4.0× · **new** — KwaiKAT / KAT-Coder-V2.5
- **vectifyai** — 5 / 1 · vel 4.0× · **new** — PageIndex, vectorless RAG

`black-forest-labs` is the one to watch: a real frontier lab, a verified blog URL (`https://bfl.ai/blog`, cited in today's news), and a multimodal release with a robot-action head. Two more active days and it clears promotion thresholds with an applicable URL.

## ⚠️ Silent covered vendors (0)

No covered vendor is past its silence threshold this run (priority 45d / enterprise 60d). All 6 priority vendors surfaced in today's files; the most-silent enterprise entry is `qualcomm` at 25 days, well inside the 60-day window.

_Collector-health note, **not** a silence signal:_ today's news collector reported hard fetch failures for `ai.meta.com/blog` (HTTP 400), `openai.com` HTML indices (redirect-cancelled; the `openai.com/news/rss.xml` fallback worked), and the `servicenow` / `workday_oracle` / `langchain` / `llamaindex` feeds. None of those vendors is silent — their mentions arrive via third-party coverage, so `covered_silent` correctly does not fire. Repairing `blog_urls` on existing entries is outside this skill's authorised change set (see Out of scope).

## Tally summary

- Orgs tracked: **1677**
- Covered (priority): **6** (openai, anthropic, google_deepmind, meta, mistral, deepseek)
- Covered (enterprise — incl. **46** auto-added): **54**
- Deep-watch (holding tier): **15** (unchanged)
- Uncovered / other: **1602**
- New orgs added to tally today: **19** — `etched`, `norm-ai`, `itc-infotech` (07-25); `black-forest-labs`, `kuaishou`, `cxmt`, `datalab`, `debian`, `scaleway`, `verizon`, `prentis`, `vectifyai`, `databend`, `posit`, `cruciallogics`, `zuger-kantonalbank`, `exeon-analytics`, `bjak`, `rivia` (07-26). Each appears ≥2× in a single file and/or is confirmed in radar `breadth_orgs_7d`, and passes the journalist test.
- Alias merges: **1** — `Noestra` (Japan's 44-company sovereign-AI consortium, 07-25) folded into the existing **noetra** slug (`Noetra Corp.`, NVIDIA Vera Rubin AI factory, 07-17). Same Japanese entity, one spelling is a collector typo; slug stability says add the alias, not a duplicate.
- Deliberately **not** inserted: `six` (SIX Group — a 3-char slug that over-matches the English word "six"; it already produced false hits in `papers/2026-07-25.md`), `docling` / `mineru` (open-source tools, not vendors), `induction-labs` (1× only), `noestra` (merged above), and all 138 radar `breadth_orgs_7d` candidates — GitHub handles (`jcodesmore`, `tirth8205`, `luongnv89`, `dietrichgebert`…), geography (`china`, `usa`, `asean`, `south-korea`), and brand/slug variants (`togetherai`, `thinkingmachines`, `lmstudio`, `arcprize`) per the "when in doubt, skip" rule.
- Hot events recorded today: **6 records across 3 orgs** (etched ×3, norm-ai ×2, cxmt ×1).

### Out of scope for this skill — reported, not acted on

- **`github_collector.watched_repos` 301 redirects** (`anthropics/anthropic-cookbook` → `claude-cookbooks`, `block/goose` → `aaif-goose/goose`, `joaomdmoura/CrewAI` → `crewAIInc/crewAI`, `affaan-m/everything-claude-code` → `affaan-m/ECC`, the last a duplicate double-counting 233,570 stars). Real and worth fixing, but `watched_repos` belongs to **ai-github-sweep** (`sources.json.github.bak`, `github_changes.log`). This skill's authorised change set is six lists over `enterprise_vendors` / `deep_watch_vendors` only — touching `github_collector` here would corrupt the other sweep's rollback target.
- **Broken vendor feed URLs** (meta, openai HTML, servicenow, oracle, langchain, llamaindex). `meta`/`openai` are `priority_vendors` (sacred); `servicenow`/`workday_oracle` are manual `enterprise_vendors` entries (sacred). `langchain`/`llamaindex` are `_auto_added` but active, so no rule fires. Editing `blog_urls` on an existing entry is not one of this skill's six change-set operations.
- Pre-existing tally duplicates observed but not merged (no authorised merge operation beyond aliasing): `sakana` / `sakana-ai`, `fireworks` / `fireworks-ai`, `red-hat` / `redhat` / `redhat_ibm`, `monday` (weekday over-match).

## Sources scanned this run

- discovered_orgs.json (last_updated 2026-07-24 → 2026-07-26; the 07-25 gap closed in this run)
- Source files: news, papers, blogs, jobs, linkedin, daily for **both 2026-07-25 and 2026-07-26** (12 files) — `## Sources scanned` methodology sections stripped before counting so WebSearch query dumps don't inflate mention counts or manufacture hot events. 202 orgs matched.
- Hot-event keyword matching is word-bounded and gated on a strong funding/M&A term; bare `billion` / `$1B` does not qualify on its own, and `valuation` is guarded against matching inside "evaluation" (a false-positive source that fired 7× across today's files).
- Radar JSONs in window: **30** (2026-06-25 → 2026-07-26); `breadth_orgs_7d` consumed, 138 candidates filtered out.
- sources.json snapshot before edit → sources.json.vendor.bak (**3 changes applied**: hot-event-add `etched`, hot-event-add `norm-ai`, expire-remove `reflection-ai`).
